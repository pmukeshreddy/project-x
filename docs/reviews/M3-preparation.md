# M3 preparation review

Independent reviewer: `/root/review_m3_preparation`. Reviewed 2026-09-19 on `implementation/feature-rl`, observed HEAD `3a4f5aa88a068b255899d1b293417fd19e8bb5de`. The sandbox investigation identifies its own earlier research HEAD, `0dc354797016b6f2f9ccbbb03c923876e769d378`; neither revision represents an implemented M3 runtime.

**Specification compliance verdict: PASS for the assigned preparation scope.** The archived executions support the initial Docker no-service boundary, and the dependency work remains accurately labeled inert acquisition and static validation. No P0/P1 defect was identified in those completed preparations. Compliance of the future M3 module with specification §§7–8,10,17 remains **unverified**.

**Implementation quality verdict: PASS as preparation evidence, with the production reuse gates below.** M3 production code does not exist in the reviewed workspace. This is **not runtime/module approval**, permission to treat the research driver as a production runner, or evidence of successful historical builds, baseline/reference health, candidate execution, grading, or qualification. Future M3 implementation needs a separate complete review.

Read scope: the review policy, M3 brief, implementation plan, execution prompt, specification §§7–8,10,17, investigation and dependency reports, all four archived probe drivers, applied/upstream seccomp profiles, image manifests/pin/pull receipt, checked-in and original local Docker receipts, acquisition/validation scripts, dependency metadata and existing coordinator integrity receipt. Baseline `pyproject.toml` and `tests/conftest.py` were read as bounded inert tar members; no source was extracted or imported.

**What the evidence actually establishes**

| Boundary | Independently checked evidence | Limit and production requirement |
| --- | --- | --- |
| Image and explicit policy | The two small manifest files hash to the recorded OCI index/platform digests. Original image inspection reports Linux arm64, Python 3.12.14, and no entrypoint, OnBuild hooks or declared volumes. All 11 original effective seccomp configurations equal the archived applied profile, SHA-256 `005f6ae1a0f3f9d1a0c044f83289e2ea54180c97a105b2539422588eac2fde44`. Its differences from upstream are exactly the reported syscall removals and constrained `socket` entries. | Retain immutable platform-image and profile identities, explicit application and effective configuration validation. An unconfined daemon default is not an acceptable fallback. Image inspection is not an installed-package or solver-artifact leakage inventory. |
| Privileges and namespaces | UID/GID 65534, five zero capability masks, `NoNewPrivs: 1`, `Seccomp: 2`; `setuid(0)`, user-namespace creation, ptrace and mount attempts return EPERM. Effective configuration has private IPC/cgroup and Docker default private PID/UTS modes; `/proc` contains PID 1, and namespace links are recorded. | Preserve all capability removal, nonroot identity, no-new-privileges and private namespaces. These are configuration plus exercised effects; sequential namespace inode numbers alone do not prove separation from a simultaneously running controller. No arbitrary escape-resistance claim follows from finite probes. |
| Filesystem and source boundary | Read-only root/system/cgroup mounts deny direct, traversal and symlink writes. No bind, named volume, additional device or host workspace/socket mount was configured. Writes fill `/workspace`, `/tmp`, `/dev/shm` at exactly 16/8/1 MiB, and executing written scripts on the first two mounts fails with EACCES. | Retain bounded writable areas, read-only system state and no private mounts. The symlink test proves the mounted target is read-only; it does **not** test source/archive validation or safe artifact extraction. Those APIs still need controller-owned path, type, link, count and byte limits. |
| Network | Effective `NetworkMode=none`; external IPv4/IPv6 TCP and UDP DNS return ENETUNREACH; a real DNS lookup fails; the IPv4 route table is empty; AF_ALG and AF_VSOCK socket creation is denied. | Retain actual no-egress configuration on setup/build/run workers. Refusal at loopback port 2375 is a narrow observation, not a general host-listener test. Local services, service reset and endpoint allowlists remain unsupported/unexecuted. |
| CPU, memory and processes | Half-CPU cgroup configuration produces about 1.014 CPU seconds in 2.027 wall seconds with 20 throttled periods. Allocating 256 MiB against 128 MiB causes exit 137 and `OOMKilled=true`; swap allowance is zero. The 32-task cgroup rejects the next fork after 31 children; the probe kills and reaps those children. | Preserve externally enforced cgroups and distinguish OOM, ordinary limit exhaustion and infrastructure failure. `nofile`, `core` and `fsize` values are inspected configuration only; their individual denial behavior was not exercised. Remeasure current capacity and validate chosen production limits with the actual setup. |
| Output and deadline | The nonterminating parent/child probe is killed in 2.066 seconds against a 2-second attached-execution deadline. The output probe retains 16,384 bytes, observes 24,576 bytes, records `output_limit`, and kills the container. Its attach CLI requires a separate kill and returns -9 after 5.423 seconds. Docker logging is disabled. | Retain byte caps, bounded draining, external container termination and separate CLI/container statuses. The 2-second setting is not an end-to-end setup/cleanup deadline. The actual runner must cover its entire input, execution, export and cleanup lifecycle. |
| Cleanup and reset | Every one of the 11 attempts has `docker rm --force` exit 0 and a subsequent specific `No such object` response, including both failed observers, OOM, timeout and overflow. Recorded label inventories are empty. One later container has empty workspace/tmp and only PID 1. | Cleanup of these attempts is supported by the command results, not merely by the `absent_after_cleanup` boolean. This is one empty-state smoke check, not three fresh reference runs or three restored historical-source interruption runs. No interrupted source submission was preserved. |
| Dependencies and setup | Acquisition code reads metadata and archive members without executing fetched packages. The existing coordinator receipt verifies three baseline manifests and six wheels; unchanged wheel hashes were not rerun. Inert B metadata agrees with the documented Python 3.12 test group, wheel build, warnings-as-errors and `not stress` default. The flit 3.11.0 choice is explicitly a neutral reconstruction, not a historical lock recovery. | Keep approved artifacts immutable and separate from B/H/candidates; do not put H or any target-package wheel into the reusable dependency layer. The proposed offline pip/build/install commands and import-source checks remain unexecuted. |

The archived payloads match the AST literal payloads in the four corresponding trusted probe scripts: 8 + 1 + 1 + 1 attempts. Checked-in result records match their original local receipts after only the documented seccomp placeholder substitution. Both failed observer runs remain visible as exit 1; the successful observer corrections did not relax policy. No unexpected-success output was found in the completed boundary observations.

**Concrete production reuse gates — not failures observed in the preparation runs**

1. **Cover input and terminal paths with deadlines; do not copy the smoke driver's control flow unchanged.** [probe.py:24](../evidence/M3/probe.py#L24) writes stdin synchronously before entering the deadline/selector loop. [probe.py:29](../evidence/M3/probe.py#L29) stops monitoring when both output streams reach EOF, then uses a separate five-second wait; killing the attach client does not itself stop the container. A future large input whose reader stops consuming can block before deadline enforcement, and an early attach disconnect can reach `reason='completed'` while the container still runs. Use bounded input/output streaming, monitor authoritative container state independently of stream EOF, and record execution and cleanup deadlines separately. The small trusted stdin payloads and recorded limit probes do not exercise these cases.

2. **Fail closed on uncertain cleanup and persist ownership before launch.** [probe.py:7](../evidence/M3/probe.py#L7) does not serialize Docker control timeouts, and [probe.py:44](../evidence/M3/probe.py#L44) can leave cleanup/evidence unfinished if `rm` raises. [probe.py:47](../evidence/M3/probe.py#L47) treats every nonzero inspect result as absence; a daemon connection failure would satisfy that expression without proving removal. Production must distinguish verified absence from inaccessible daemon, persist owned container identity before create/start, retain partial-failure receipts and recover cleanup after controller interruption. All actual reviewed removals have exit 0 plus specific absence responses, so this does not invalidate them.

3. **Make boundary observations actual admission checks.** [probe.py:58](../evidence/M3/probe.py#L58) prints `UNEXPECTED_SUCCESS` instead of failing, and other payloads similarly print syscall results for human inspection. Their zero exit status is therefore insufficient as a reusable boundary gate. Future qualification must check required observations, expected denial reasons, effective configuration and completion; missing output or an unexpected successful operation must reject the boundary. This review checked the actual recorded observations rather than interpreting exit 0 as a blanket pass.

4. **Preserve a source submission before destroying the development workspace.** [attempt1-probe.py:124](../evidence/M3/attempt1-probe.py#L124) writes the interruption marker into tmpfs; normal limit handling kills and removes that workspace without export. That is appropriate for this disposable probe and supplies no snapshot evidence. Before real episodes, implement the M3 brief's bounded freeze/export or explicit last-saved snapshot policy, record the exact source version/time, and validate the recovered source in the controller. Ordinary episode limits must leave an attributable submission for later grading.

5. **Complete and test the offline setup/import policy before historical execution.** [dependency-preparation.md:67](../evidence/M3/dependency-preparation.md#L67) through its following steps correctly separate source-free dependency construction and fresh per-snapshot builds, but specify no executed staging, import or extraction API. Production must select the exact approved wheel and source bytes, constrain manifest/backend changes through the task's dependency policy, and record the bundled pip version, actual commands, full import search path and resolved test/backend/Click locations. Keep the source tree, test directory and ambient/user import paths from unintentionally shadowing the chosen installed wheel. Use the same recorded path/setup policy for B, H and candidates. `click.__file__` and distribution metadata are useful diagnostics inside an untrusted worker; candidate-supplied reports are not authoritative proof of artifact identity or a grading verdict. Controller-owned source/wheel bindings and fresh isolated builds remain necessary. The host's later Click installation is unrelated and must never become setup or discovery input.

**Required later verification remains separate from this verdict.** The actual M3 implementation must provide strict typed recipes/results, fail-closed Docker policy validation, safe source transfer/extraction, interruption-safe lifecycle control, and fresh B/H imported-path and baseline-health evidence. It must run the three fresh reference and three interruption/reset smoke repetitions through its own API, plus adversarial command/output/path/cleanup checks. M4/M5 own feature absence, reference feature behavior, private comparison integrity and qualification. Public pytest output alone cannot establish a tamper-resistant regression pass under specification §8. Service support requires its own implementation and independent service-state observations.

Review actions were read-only source/data inspection and narrow trusted standard-library diagnostics. The final payload/receipt/profile/manifest consistency diagnostic exited 0. An initial diagnostic stopped at the checked-in seccomp placeholder; the corrected diagnostic compared the original profile bytes and normalized only that placeholder. No historical, candidate, generated or downloaded program was executed, no new container was created, no network acquisition or installation occurred, and no unchanged wheel/model integrity or full test suite was rerun. The only written path is this review report; product files, user configuration, Git index and commits were untouched.
