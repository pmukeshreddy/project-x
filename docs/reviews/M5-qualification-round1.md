# M5 qualification independent review — round 1

Reviewed `b343b8cb9ab39f000bfe639618c01f529ab06755` and `3cc9f11f4d2665f2872906a8c00521255eb252eb`, all `qualification/**` code and dedicated tests, and the actual M3/M4/Registry/TaskBuilder calls against the M5 brief, admission protocol and binding specification. All 40 files in M5's final binding manifest match their recorded size and SHA-256.

**Specification compliance: FAIL.** The intended-failure and dependency-quarantine requirements have the concrete gaps below.

**Implementation quality: FAIL.** Both gaps affect admission validity and are absent from the supplied regression coverage. Return fixes to the existing M5 owner. The explicit H projection, full canonical T0/Tn and P/Q comparisons, selected execution identities, external enrollment boundary, and provisional missing-prerequisite behavior are otherwise supported by this review.

1. **P1 — A process crash can satisfy a targeted semantic omission.** At `src/feature_rl/qualification/controls.py:35`, the gate requires only completed cases and equality of failed requirement IDs. `evidence.py:136` permits a nonzero process-mode exit and recomputes its ordinary comparisons; M4 intentionally represents these observations as completed comparisons (`grading/service.py:125`). Consequently, a lazy import or syntax failure in the feature path, with compatibility cases still passing, counts as a runnable omission. A focused in-memory observation probe supplied exit 1, empty stdout, and respectively `SyntaxError: invalid syntax` and `ModuleNotFoundError: No module named required_dependency`; both passed `_assert_case_observation` and returned `GateOutcome(passed=True, code='accepted')` for F1. These were synthetic diagnostics, not worker evidence. M5 needs an enforced, evidence-bound distinction between the intended semantic failure and execution breakage, including legitimate missing-public-symbol cases. The existing tests only vary `CaseResult.status`, so they miss M4's process-mode behavior. This violates §§7/9 and can give an inadequate checker its required omission coverage.

2. **P1 — Consumed opaque dependencies escape quarantine and affected-version tracing.** `service.py:291` registers a run configuration against the `m4-submission` envelope without its `changes` blob; `admission.py:209` and `admission.py:269` likewise omit the detached signature blob from verification/admission dependencies. Registry deliberately does not discover references inside byte payloads (`registry/core.py:82`); accepted revalidation at `admission.py:358` also never checks the signature's current usability. Read-only inspection of the retained actual fixture found both baseline and fresh-H `m4-source-delta` references unregistered (`UnknownIdentity`). A separate temporary **failed-verification-only** diagnostic quarantined an `m5-sshsig` blob: its envelope and verification record remained usable, and its verification job was absent from the signature trace. No human record, key, successful verification or approval was created. The earliest safe M5 boundaries are the new run configuration before grade-job enqueue, and attestation ingestion before native verification. Resolve/register/check the consumed inner refs there and include them in M5's verification/admission dependency records; retain current checks on reuse. Existing Registry dependency declarations are immutable, so do not retrofit them. These fixes need no general M3/M4/M6 reopening. Otherwise a valid cryptographic check or unchanged source bytes can reuse evidence already quarantined under the lifecycle protocol.

Verification used narrow `.venv/bin/python` probes and source inspection. The signature-graph probe initially hit M0's expected rejection of macOS's unresolved `/var` symlink; rerunning with the resolved temporary path produced the result above. The retained Registry was unchanged. No full suite, Docker run, model call or GPU work was repeated. Existing evidence records 69 focused passes and readback of nine grade ledgers, three reset ledgers and 30 rejection probes; those results do not cover these two findings.

Execution limitations remain separate from these defects: the actual TEST-only Docker fixture is provisional and is neither generated-feature evidence nor human approval. Genuine external human acceptance, complete controls, independent alternative authorship and authentic global construction history remain unexecuted prerequisites, not reasons for either failed code verdict. Coordinator integration must still verify M6's complete history producer and downstream legal-transition consumers; this review does not reopen the independently approved unchanged M3/M4 core or M6 builder.
