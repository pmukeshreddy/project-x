# M4 authoring independent review — round 1

Coordinator review of `4086ee69a1001f3cd6b49be14ddad5bc27707261`: new checker/control authoring, finalization, provider publication recovery, control attachment, and the extracted bundle-validation interface. The previously reviewed mechanical grader remains closed except for this directly affected loader refactor.

**Specification compliance: PASS. Implementation quality: PASS.** No confirmed finding in this slice.

Generated worker source remains inert controller data. Input/comparison proposals use the existing closed language and the same mechanical validation as grading. Actual M2 evidence resolution checks contract/request/B/discovery/recipe joins; generated fields cannot replace controller permissions, identities, provenance or costs. Coverage, mandatory flags, oracle origins, operand types and forbidden verdict observations reject before final publication.

Negative control reference excerpts bind the actual supplied SourcePair B/H and bounded source lines. Alternative prompts contain only B, the frozen contract and admitted public checks. Source proposals pass the existing source-only delta policy; imported hostile archive controls retain an exact outer B join and an explicit unverified status. Control attachment creates a new verifier preserving the existing cases and adapter. Expected validity and recorded contexts grant neither semantic validity nor independent/human origin.

Fresh and recovered provider outcomes use the existing exact M2 request/schema/archive/usage/cost validation. Preparation and component/journal/final publication failures retain explicit replay capabilities; the focused tests verify replay without new dispatch. Local stage repair bounds supplement, and do not replace, M6's aggregate candidate/stage accounting. M6 must bind the actual selected journals and control-record evidence when constructing repair history and M5 diagnoses.

Independent command: `PYTHONPATH=src .venv/bin/python -m pytest -q tests/test_checker_authoring.py tests/test_control_authoring.py -p no:cacheprovider` — **28 passed in 1.16s**, exit 0. The owner's combined 84-test receipt remains in `docs/evidence/M4/authoring/validation.json`; the unchanged core and Docker matrix were not repeated. Diagnostic provider/process outputs establish implementation behavior only. No native task generation, original H inspection, human approval or GPU execution occurred in this review.
