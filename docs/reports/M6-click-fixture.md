# Labeled Click fixture through actual services

This is an explicitly synthetic TEST feature on the pinned actual Click B archive.
It is not model-generated, not a historical requested feature, and not evidence
that the exhausted real Click authoring attempt produced a contract or scenarios.
All helper source is inert host data and executes only inside the reviewed M3
Docker boundary. No human attestation or independent alternative is created.

The dedicated [driver](../../tests/run_factory_click_fixture.py) publishes complete
resolvable M0 CandidateRecord/SourcePair/contract/scenario/verifier artifacts, creates
the actual pinned M3 recipe, freezes T0 through TaskBuilder and validates its final
solver package. B lacks `src/click/m5_test_feature.py`; synthetic H adds only that
module. Its disclosed `transform(text)` returns `"fixture:" + text` (F1), preserving
ordinary echo text/newline/zero exit (C1). Cases are `"alpha"`, `""`, and `" spaced "`.
The M5 reference projection must include exactly that one implementation path.

The omission control returns unchanged text. The extra positive uses a join to
construct the prefix. Both were authored by the same fixture author as H; the
positive is not an independent solution and cannot satisfy that admission gate.
The controller comparison uses M4's closed observation grammar. Only exact helper
module absence becomes an explicit absent observation; unrelated import failures
remain actual worker failures.

The [consolidated receipt](../evidence/M6/fixture-runtime/receipt.json) and
[stdout](../evidence/M6/fixture-runtime/stdout.txt) retain source hashes, run intervals,
full OperationResults/GradeReceipts, immutable artifact identities, partial/unknown
costs and reset evidence. Task-private CAS, SQLite journal and runtime records stay
in the new ignored path recorded there. Cached pinned image and wheel bytes are
reused; every candidate grade still rebuilds and executes in fresh isolated workers.

The runtime phase passed in **82.45 seconds**, 2026-09-19 **22:34:59–22:36:21 UTC**,
exit 0. All captured product/driver/source-input hashes were unchanged during that
phase. Its observed Git revision was `c7a2edb387409dc3f0555453918419edf15cd275`;
the receipt separately binds actual bytes, including the then-uncommitted M5 code.

| Actual grade | Reward | Completed cases | Failed requirements |
| --- | ---: | ---: | --- |
| B, helper absent | 0 | 3 | F1 only |
| Exact H implementation projection | 1 | 3 | None |
| Omit-prefix control | 0 | 3 | F1 only |
| Saved-mutation, reset, original B | 0 | 3 | F1 only |

Every grade preserved C1. The saved helper mutation matched the exact projected H
artifact; reset restored the complete original SavedSource and advanced workspace
generation. Each grade confirmed cleanup; the final recovery query found no owned
resources pending cleanup.

T0: `259b16c9faf122474ecd548e342987098aa4ba5ddd7c217c6fd33986f109b875`.
Final solver archive raw SHA-256:
`4b7b3668eee89be12c9aa51901cec046f28f4d1db67a0a0e4955ddac61765729`.
The solver archive contains B without the helper; raw synthetic H remains private
in the exact SourcePair reference. These identities are for this test fixture only.

The actual M5 qualification phase passed its test assertions in **195.19 seconds**,
2026-09-19 **22:38:04–22:41:19 UTC**, exit 0, using M5 production checkpoint
`b343b8cb9ab39f000bfe639618c01f529ab06755`. All captured source hashes remained
unchanged throughout execution. The result is **provisional**, with no human review,
review request or admission output. Report identity:
`991f39ef749df88a5c6c2c1718b8b5514dc56c60ee94f128267d7691e07dcaff`.

M5 completed all nine actual grades: baseline 0; three fresh H runs 1; omission 0;
same-author positive 1; three interruption/reset H regrades 1. All 27 cases completed,
and every executed qualification assessment passed. Each reset includes an actual
saved mutation, bounded worker timeout, cleanup, restored initial source and advanced
generation. Together with the first phase, this is 13 grades and 39 completed cases.
Completed qualification replay returned the identical OperationResult without new
registry events or worker execution.

The report explicitly retains missing authenticated human review, alternative
independence, verified global repair history, four control categories, nine
adversarial attacks and the C1 omission control. These are required gates, not
diagnostic successes. No task was qualified, calibrated or released. At this
checkpoint M5 admission was still deny-all; this execution exercises its actual
provisional qualification path only.

The selected qualification accounting reports construction wall **43.6823 s** /
CPU **17.770084 s** and execution wall **132.2669 s** / CPU **52.042563 s** as partial
measurements. Verifier overhead is explicitly unknown, as are remaining unmeasured
channels. The receipt preserves each initial grade's full costs, the saved mutation
cost and all nine selected M5 results plus original per-attempt cost records; do not
add repeated projections of those same costs. A bounded post-run readback copied
the selected immutable grade/reset records into the same receipt, with claim
capabilities omitted. No execution was repeated for consolidation.

Reproduction commands (each new runtime invocation requires a new receipt path):

```sh
PYTHONPATH=src .venv/bin/python tests/run_factory_click_fixture.py runtime --receipt docs/evidence/M6/fixture-runtime/receipt.json
PYTHONPATH=src .venv/bin/python tests/run_factory_click_fixture.py qualification --receipt docs/evidence/M6/fixture-runtime/receipt.json
```

The recorded receipt already exists; the runtime command deliberately refuses to
overwrite it or repeat an uncertain attempt. These are explicit diagnostic driver
commands, not a production Factory or CLI. The second phase reuses the frozen T0;
it does not rerun the four milestone grades. An interrupted qualification requires
the actual M5 recovery API and is not automatically redispatched by this driver.
