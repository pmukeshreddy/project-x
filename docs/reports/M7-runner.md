# M7 runner checkpoint

Implemented `feature_rl.agents.AgentRunner` with the actual same-store M3 runtime, M4 grader, M6 builder/Registry and either concrete TaskLifecycle or multi-policy ReleasedTaskResolver. The backend receives only the admitted frozen SolverView and public system prompt. Candidate reads, source writes, commands and public tests execute through M3 development workers. The runner preserves actual contexts, sampled tokens and loss masks, distinguishes malformed candidate actions from invalid or aborted inference, stops collection at declared limits and grades the last confirmed source.

Each explicit invocation selects one Registry `operation=run` job; its ID is exactly `RolloutRecord.run_id`. Completion publishes one RolloutRecord plus one frozen receipt. `validate_record` checks the selected completed attempt, request, configuration, outcome and current quarantine. Actual pre-grading invalid/infrastructure controller receipts now enter TrainingDataGate through an explicit actual runner, without fabricated M4 null measurements. Unknown external work is retained for reconciliation and never resampled automatically. Submission, M4-grade, controller-outcome and frozen-record publication have retained replay paths. Only confirmed M4 infrastructure results may trigger up to two additional grades of the exact same submission/case seed; prior receipts/costs remain attributable, while the selected terminal M4 receipt is singular.

The lazy SkyRL backend uses the exact pinned `/inference/v1/generate` token protocol and one bounded HTTP request. It avoids the stock client's automatic 30-request retry loop. Complete bounded local tokenizer bundles are hashed, including class-selection config and vocabulary/template files; custom model code and downloads are disabled. Raw native logprobs count as behavior probabilities only with unit temperature, full support (`top_p=1`, `top_k=-1`) and acknowledged policy identity. Other supported engineering sampling modes have no training trace. Aborted replies are infrastructure outcomes; unknown finish reasons are invalid; ordinary stop/length replies remain gradeable. The endpoint request/response fields and retry distinction are bound to the existing pinned SkyRL audit (`remote_inference_client.py:300–337,365–519`, commit `f5bc3b78dfddfb352870d5d7430cd226e5785838`); no new source retrieval occurred. Native CUDA/server execution and actual inference/weight synchronization remain unverified.

## Verification

CPU command:

```sh
PYTHONPATH=src .venv-training-cpu/bin/python -m pytest tests/test_agent_runner.py tests/test_agent_backend.py tests/test_training_core.py tests/test_training_torch.py tests/test_training_data.py tests/test_training_state.py tests/test_training_skyrl.py -q
```

**68 passed in 19.03 seconds.** Recorded output: `docs/evidence/M7/runner/focused-tests.txt`. Importing the runner in the core environment loads no Torch/SkyRL/vLLM/Transformers modules. Tests use explicitly labeled service/HTTP/tokenizer doubles for runner mechanics, including actual unmodified TaskLifecycle denial on BUILT input. They check one selected Registry record, explicit/default case seeds, replay, fresh conversations/workspaces, ordinary command failures, malformed actions, missing probabilities, stale/aborted generation, bounded same-source grading retries, opaque dependency/cost joins and publication recovery. Earlier real Torch optimizer/checkpoint tests remain included.

One actual Docker diagnostic was run with `PYTHONPATH=src .venv-training-cpu/bin/python tests/run_agent_docker_diagnostic.py`. It reused the cached image, retained exact TEST fixture T0/package/Q and original CAS. An isolated Registry copy preserved the original CAS binding; the original Registry's bytes remained unchanged. Actual unmodified M5 verification and runner admission rejected the provisional fixture first. Only M5.verify_accepted was then substituted on that isolated test service; **no HumanReview or accepted QualificationReport was created**.

The fixture grants one input/output token. A disclosed one-shot diagnostic backend therefore emitted one scripted public-test command: M3 read B, wrote the synthetic prefix helper, ran a disclosed public assertion and saved the source. The declared token limit stopped collection. Exactly one actual M4 terminal grade rebuilt and evaluated that source: **reward 1, three cases passed**, with case seed 19. Fresh baseline workspace/reset readback confirmed absence of the helper. This is execution/limit/publication evidence, not real token accounting, model competence, admission or learning. `training_eligible=false`, no behavior probabilities and no model call. Retained receipt/log: `docs/evidence/M7/runner/docker-receipt.json`, `docker.log`. No repeated qualification or historical matrix. The receipt binds exact draft source hashes; later recovery/validation/accounting changes were CPU-checked without repeating Docker.

## Limits and next slice

The bounded runner profile allows at most 262144 cumulative input tokens, 65536 output tokens and 128 tool calls. Tool feedback exposes at most 65536 bytes per stream with explicit omitted counts; full bounded output stays in M3 evidence. Context is never silently compacted or retokenized. Recorded generation contexts are the exact contexts passed to the backend.

M3's fixed resource quotas are per operation. The runner checks cumulative measured CPU between actions and conservatively caps command wall by remaining CPU under an admitted <=1-CPU quota. Existing command timeout does not cover staging (20s), dependency setup (30s), control probes, export or cleanup (`environments/runtime.py:161,232`; `docker.py:294`). Therefore **hard aggregate episode CPU enforcement is not complete**; the coordinator has the concrete M3 request-level remaining-CPU override needed for the session monitor. Wall collection checks likewise coexist with M3 setup/cleanup allowances. Terminal grading has its separate declared M4 budget. No stronger bound is claimed.

Full native training launcher/service, synchronous admitted batch collection, supervised renderer/data orchestration, native policy publication/worker synchronization, optimizer/checkpoint publication/resume and reproducible train/resume commands remain the next M7 implementation slice. This runner checkpoint enables M6 CLI and M8 evaluation integration; it does not mark M7 complete. No GPU/model downloads, new task-generation calls, native inference or feature experiment occurred.
