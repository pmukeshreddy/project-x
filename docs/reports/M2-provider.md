# M2 provider implementation report

The local authoring-provider slice is implemented under `src/feature_rl/generation`. Fix rounds 1 and 2 addressed the earlier findings, and fix round 3 addresses both P2 findings in `docs/reviews/M2-provider-round2.md`. Exact output-Literal enforcement now derives from the pinned Pydantic core validation graph rather than an independent JSON-schema branch heuristic. It carries the raw-type validator's chosen model branch into ordinary validation, so Boolean-only unions, patterned mapping values, and mixed Literal/model alternatives cannot validate on one path and return on another. JSON string transport for string-enum-valued Literals remains valid. M2 request/response/prompt identities are bounded to 128 ASCII characters across requests, events, attempts and call records, with public-boundary revalidation for unchecked model copies/construction. Initial authoring remains limited to request/B/public checks. The later checker stage accepts a frozen contract and scenario plan plus explicit attributable B/public-check data without exposing H to the first draft.

Risk-based tests use a complete authentic-shaped protocol-v3 success stream and one-field mutations across hashes, dependencies, seed, offline/request identity, measurements, text fragments, completion IDs, timing, and logprob domain. Fix-round 3 coverage adds valid and numeric-invalid Boolean unions, a mixed Literal/integer-model union with an exact returned-branch assertion, patterned Literal mappings, string-enum-valued Literal transport alongside tuple and UTC validation, single-execution lifecycle checks for defaults and post-init hooks, explicit unsupported-schema refusals before backend verification for plain validators and callback-bearing union alternatives, genuine valid decoder baselines before event mutations, all three identity bounds, revalidation of copied/constructed requests, and maximum-length identities through preflight publication recovery. Exact final counts and hashes are recorded in `docs/evidence/M2/provider-v3-fixround3-verification.json`.

The earlier provider verification receipts remain immutable. The fix-round 3 receipt separately binds current source/test hashes, exact commands, exit statuses, and raw-output hashes before commit and does not claim a committed product revision. Coordinator verification will add that binding after re-review.

The preserved revision-1 production smoke and rejection receipts are unchanged and are not evidence for protocol v3. No inference occurred in any provider fix round. A separately declared coordinator run after review must bind and exercise the committed current protocol with an actual archived source.

This remains a partial M2 delivery. It does not author or freeze the real Click requirement contract, scenario plan, checker, reward, or experiment. Larger production envelopes remain explicitly unqualified on this host and require their own bounded measurement before a construction batch.
