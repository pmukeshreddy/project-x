# M2 provider implementation report

The local authoring-provider slice is implemented under `src/feature_rl/generation`. Fix round 1 addresses all five findings in `docs/reviews/M2-provider.md`: unconditional bounded cleanup and fail-closed watchdog observation, strict JSON output transport, complete exact protocol-v3 events, pre-execution attributable attempts with replayable publication failure, and an explicit false usage-acceptance flag for every failed envelope. Initial authoring remains limited to request/B/public checks. The later checker stage accepts a frozen contract and scenario plan plus explicit attributable B/public-check data without exposing H to the first draft.

Risk-based tests now use a complete authentic-shaped protocol-v3 success stream and one-field mutations across hashes, dependencies, seed, offline/request identity, measurements, text fragments, completion IDs, timing, and logprob domain. Focused coverage also exercises monitor exceptions, controller interruption and the first lost observation, JSON tuples/enums/UTC values plus invalid counterparts, attempt-registration refusal, repeated unavailable publication, post-execution archive failure and replay without a second runner call, and failed-envelope usage/cost consistency. Final verification passed 46 focused tests and all 201 repository tests.

The earlier `provider-v2-verification.json` remains immutable evidence for revision 2. `docs/evidence/M2/provider-v3-fixround1-verification.json` binds the protocol-v3 source/test hashes, exact commands, exit statuses, and raw-output hashes before commit; its associated stdout/stderr files retain the raw output. It distinguishes the assigned fix-round base from the shared workspace HEAD observed at verification and does not claim a committed product revision. Coordinator verification will add that binding after re-review.

The preserved revision-1 production smoke and rejection receipts are unchanged and are not evidence for protocol v3. No inference occurred in this fix round. A separately declared coordinator run after review must bind and exercise the committed current protocol with an actual archived source.

This remains a partial M2 delivery. It does not author or freeze the real Click requirement contract, scenario plan, checker, reward, or experiment. Larger production envelopes remain explicitly unqualified on this host and require their own bounded measurement before a construction batch.
