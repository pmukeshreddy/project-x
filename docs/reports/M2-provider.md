# M2 provider implementation report

The local authoring-provider slice is implemented under `src/feature_rl/generation`. Fix round 5 addresses the two P2 findings in `docs/reviews/M2-provider-round4.md` while preserving the closed scalar/model/nested union behavior and identifier/recovery boundary. Schemas with only string, string-enum or null Literals now use the original strict JSON validator unchanged. Numeric or Boolean Literal schemas use the exact branch-preserving validator only for a small callback-free structural subset. Raw integer and float alternatives in scalar unions, nested scalar unions, Boolean/model unions and patterned mappings retain their actual Python and serialized JSON types. M2 request/response/prompt identities remain bounded to 128 ASCII characters across requests, events, attempts and call records, with public-boundary revalidation for unchecked model copies/construction. Initial authoring remains limited to request/B/public checks. The later checker stage accepts a frozen contract and scenario plan plus explicit attributable B/public-check data without exposing H to the first draft.

The guarded subset supports strict primitives/enums, models, scalar/model unions, nullable values, lists, tuples, definitions/references, typed objects and string-keyed dictionaries. It refuses all callback and chain forms, defaults and lifecycle hooks, custom initialization or serialization, dataclass/call and unknown forms, non-string dictionary keys, set/frozenset values, and models with `extra='allow'` before backend verification. Non-JSON-native Literal values and non-string-valued enum Literals refuse at the same boundary. Tests begin from ordinary strict-JSON baselines and cover both reviewer callback-dependency cases, equal and unequal model values in direct and nested set/frozenset fields, nested and root extra-allow models alongside supported ignore/forbid controls, a Decimal Literal, supported string-Literal callbacks/defaults exactly once, exact numeric union types, patterned mappings, publication recovery, and actual M0 `RequirementContract` and `ScenarioPlan` transport. R1 scalar/model/nested controls and R2 identity/recovery cases remain covered. The final focused suite passes 98 tests and the affected full suite passes 253 tests. Exact RED/final commands, exit statuses and mechanical file/log identities are recorded in `docs/evidence/M2/provider-v3-fixround5-verification.json`.

All earlier provider verification receipts remain immutable. The fix-round 5 receipt separately binds current source/test/document bytes and raw RED/final test logs before commit and does not claim a committed product revision. Coordinator verification will add the committed current-source native binding only after independent re-review.

The preserved revision-1 production smoke and rejection receipts are unchanged and are not evidence for protocol v3. No inference occurred in any provider fix round. A separately declared coordinator run after review must bind and exercise the committed current protocol with an actual archived source.

This remains a partial M2 delivery. It does not author or freeze the real Click requirement contract, scenario plan, checker, reward, or experiment. Larger production envelopes remain explicitly unqualified on this host and require their own bounded measurement before a construction batch.
