# Factory source prerequisite

`Factory.screen_source(candidate)` consumes the complete actual M0 CandidateRecord and its resolvable source/evidence closure, then selects an attributable Registry `construct` result. Source candidate rejection and ineligible license remain rejection; unsupported/infrastructure/provisional source findings remain unresolved with their original disposition. Unknown license or unassigned partition blocks. Eligibility asserts no built, qualified or released task.

The private `m6-source-disposition` records the selected claim/job, exact candidate/family/lineage/screening/license/partition, original aggregate source costs and the reconciled source-import/controller costs. Source costs are imported once per exact immutable candidate across Factory revisions. A newer implementation reuses the selected existing same-protocol source result, retaining its original revision. Actual distinct M1/provider attempts are not invented from the candidate's aggregate CostRecords. Future authoring attempt imports remain separate work.

The Registry and CAS remain the durable authorities. A nonblocking POSIX `flock` beneath the controller store's private `.m6-candidate-locks` directory excludes competing candidate imports; it contains no ledger state. Unsafe symlink/hardlink/writable lock paths reject, and contention rejects before an event or dispatch. This assumes a local filesystem with the same POSIX locking semantics as the reviewed SQLite deployment, not cross-host coordination or a power-loss guarantee.

Unknown pre-freeze work remains explicit and is not rescreened. A queued job that never acquired a claim can resume. Post-freeze recovery reuses exact source bytes/costs; a lost CAS reply retains bounded replayable bytes. Source snapshot metadata is bounded before any claim, and publication respects the reader cap. Current quarantine traces from M1 evidence to the result; historical readback remains historical and grants no current admission.

**11 focused CPU tests passed.** [Exact receipt and source hashes](../evidence/M6/factory-source/receipt.json), [pytest output](../evidence/M6/factory-source/focused.txt). Checks exercised actual M0/Registry state, failed/lost publication, unknown attempt preservation, queued recovery, cross-version cost deduplication, actual file-lock contention and quarantine. Faults are explicitly synthetic; no Docker, model, source execution or human approval occurred. This checkpoint unblocks M8 rejected-source audit binding. Full construction/history/orchestration/CLI remain in progress under the same owner.
