# M5 implementation handoff

QualificationService now implements actual M4 control/repeat orchestration, explicit
H implementation projection, M3 saved-mutation/interruption/reset verification,
frozen external-review requests, authenticated acceptance, accepted-consumer
revalidation, typed failure/quarantine and publication-only recovery. It uses
reviewed Registry selection/accounting and actual M6 solver-package validation.
See [the exact interface](../interfaces-M5.md). Independent review is pending.

Round-1's two confirmed P1 findings are fixed for delta review: normal process
completion is now required before a comparison counts as a runnable semantic
omission, and new M5 records bind/check consumed inner source and attestation refs
against Registry quarantine before use and on reuse. Exact absent-symbol adapter
observations remain supported; M4 grading classification and existing Registry
declarations are unchanged. The affected suite passed **72 tests**, including
13 new CAS/Registry regressions. See the [concise fix receipt](../evidence/M5/review-fixes/receipt.json).
No new Docker, model, key, human approval or retained-record matrix was executed
for this fix. The following 69-test/source manifest and runtime evidence describe
the original handoff checkpoint; they have not been rewritten as current results.

The final focused suite passed **69 tests**. It covers projection exclusions and
rejections, semantic failure attribution, repair caps, all missing control/attack
categories, every immutable report field and T0/Tn field, forged identity/callback
rejection, native wrong-message/signer/namespace checks, idempotent qualify/Registry
retries, typed failures/quarantine, exact frozen report recovery, and lost
post-commit acknowledgements. No successful human approval was created or simulated.
Positive externally enrolled human acceptance is not empirically exercised.

The root-authorized explicitly **TEST-only Click fixture** executed actual
M3/M4/M6 at the captured source checkpoint: B failed exactly F1, H passed, omitted
prefix failed F1, and reset restored exact B; ordinary echo C1 passed throughout.
M5 then executed nine grades (B, three fresh H, two controls, three interrupted/reset
H runs) in 195.19 seconds. All eligible gates passed, but its report remained
provisional for missing independent authorship, full controls/history and human
review. Same-author alternative is diagnostic only. Completed qualify replay
returned identical result without new Registry events. M6 owns that
[execution receipt](../evidence/M6/fixture-runtime/receipt.json) and fixture driver.
Those Docker results bind the earlier `b343b8c` M5 production bytes and captured
HEAD/revisions, not subsequent admission code.

After admission changes, read-only validation of those retained records passed
nine exact grade ledgers and three reset ledgers, and rejected 30 duplicate/raw
input-output alterations. It changed no Registry events and ran no Docker/model.
This validates the stricter evidence join against actual recorded M3/M4 output;
it is not another set of independent controls.

All attempted focused checks, including failures, remain in
[implementation evidence](../evidence/M5/implementation/). Corrected findings were
a test fixture name, required Registry reconciliation and category aggregation,
`TraceReport.jobs`, strict JSON fixture loading, and M3's actual Docker container-ID
command identity. Raw failure logs preserve original trailing whitespace. Final
source/test/evidence hashes and exact command outcomes are in
[final bindings](../evidence/M5/implementation/final-bindings.json).

No original private historical H, model inference, user key, ssh-agent, personal
config, actual human enrollment/signature, accepted task, release or GPU execution
was used or manufactured. The earlier [OpenSSH preflight](../evidence/M5/attestation-preflight.md)
proves the native signature mechanism only. External enrollment and real human
review remain a required gate. M6's full Factory must supply authentic complete
global repair history; M5 exposes and enforces that actual seam without inventing
upstream usage. Complete control coverage and an independently authored alternative
remain prerequisites for the first eligible review request.

The implementation is ready for independent review, with no deny-all admission
placeholder left. Empirical historical-task acceptance remains incomplete for the
explicit missing real artifacts and human gate above. This owner retains confirmed
review fixes; no additional Docker matrix is required for this handoff.
