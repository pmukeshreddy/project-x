# M5 qualification implementation

Implementation is in progress. The provisional execution checkpoint now uses actual M4 grading, explicit H-source projection and M3 saved-mutation/interruption/reset/regrading. Missing controls, independent alternative provenance, global repair history and human review accumulate as precise provisional gates after eligible executions. It consumes actual `TaskBuilder.solver_package(T0)` and reviewed Registry jobs/cost snapshots; it does not package solver bytes or authorize release.

The current checkpoint has 38 focused diagnostic tests passing. Native SSH checks sign only new synthetic `NO_TASK_APPROVAL` messages; root-owned external reviewer enrollment is required by the human-verifier boundary and has not been configured. `accept` and `verify_accepted` still reject all inputs while authenticated admission is being completed. This checkpoint must not be interpreted as complete M5 code or successful feature qualification.

Owner corrections retained in `docs/evidence/M5/implementation/`: a semantic-test fixture variable typo; a history-test setup initially missing required Registry reconciliation; the actual M4→M5 cost aggregation/completion mismatch, fixed by retaining original costs as CAS evidence and using the exact reconciled aggregate; and the read-found `TraceReport.jobs` join correction. All failed attempts remain preserved.

Root-authorized TEST-only M3/M4/M5/M6 execution is next, with fixture source and expected behavior explicitly disclosed. No original private H, model generation or human approval is authorized or claimed. Complete authenticated acceptance/replay validation, broader focused negative cases, independent review and final interface documentation remain in progress.
