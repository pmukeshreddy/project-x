# M4 grading core checkpoint

Accountable owner: `m4_grading`, including review fixes and later checker-generation/finalizer integration. Implements the independent M0/M3 core authorized after preparation. No M0/M2/M3/registry/config code was changed. M2 requirement/scenario service helpers are not imported. Reviewed dependency source is unchanged from `4530ad7` for contracts, artifacts and environments.

The core now performs strict closed input/comparison decoding, deterministic complete seeded manifests, exact M0 contract/scenario/case/requirement/permission joins, source-only submission reconstruction, real M3 clean builds and fresh installed-worker probes, external comparisons, complete case ledgers, zero-versus-null classification, immutable receipts and publication replay. Its actual API is in `docs/interfaces-M4.md`.

## Verification

- Focused core suite: `PYTHONPATH=src .venv/bin/python -m pytest -q tests/test_grading.py tests/test_verifiers.py tests/test_submission.py --tb=short` — **45 passed**. Covers source delta/snapshot replay, archive attacks, strict JSON/types, duplicate/unknown/incomplete observations, deterministic inputs, actual M0 joins, unsupported policies, missing submission infrastructure, malformed candidate zero, receipt completeness and receipt-publication replay.
- Real diagnostic Click-B route: `PYTHONPATH=src .venv/bin/python -m pytest -q -s tests/test_grading_docker.py --tb=short` at the initial route checkpoint — **12 passed, 192.71 s**, 14 grade attempts. Baseline and dependency-shadow/state checks passed; regression, hard-coded output, early exit, crash, nontermination, malformed/duplicate JSON, forged reward and excessive output received zero. The actual missing-socket attempt was null; retry used the identical submission/seed/manifest and passed. Every completed operation verified cleanup. `real-route-attempt1.log` retains exact receipts and source hashes.
- Eight hostile archives subsequently passed through actual `GradingService` with diagnostic M0 tasks: traversal, absolute path, symlink, hardlink, device, reward path, build/dependency change and compressed invalid archive. All received zero before building. This test passed in `build-and-archive-red.log` alongside a failing build-test assumption described below.
- Confirmed build-accounting regression: a source delta adding `src/click.py` alongside `src/click/` made pinned Flit reject the build. The original grader retained zero/evidence but omitted the construction cost. `build-cost-red-confirmed.log` records the failing assertion; after the fix, the focused real build check passed in **6.72 s** (`build-cost-green.log`), retaining measured wall and cgroup CPU plus the complete not-run case ledger.

The build check first used invalid Python, then removed `__init__.py`. Both were test assumptions: Flit packages without compiling and accepts a directory without that file, so these candidates built and failed at import. Both correctly received zero; their logs (`build-and-archive-red.log`, `build-cost-red.log`) are retained, not replaced by the final pass. Source inspection of the already pinned Flit wheel's `common.Module` identified its explicit conflicting-file/directory error before the final reproduction. No Flit/application code was imported on the host.

Across these focused runs: 26 diagnostic grade calls, including eight archive preflight rejections, with no H or model calls. There were five independent M4 test contexts; their private CAS/runtime paths, task/recipe/baseline identities, source hashes, costs, seeds, statuses and evidence references are in the retained logs. These are additional bounded M4 checks, not reruns of consumed M2 provider/native or historical broad suites. After the confirmed fixes, no unchanged broad suite or native audit was repeated. The final unit run also covers the corrected unsupported-policy classification. The final accounting-only guard keeps unavailable CPU unknown and never reuses successful build CPU for a failed execution call.

## Evidence meaning and remaining work

All contracts/checkers in this checkpoint are explicitly labeled hand-authored **diagnostic ordinary echo preservation** fixtures. They invoke actual Click B through `click.command`, `click.echo` and `CliRunner.invoke`; they do not define the requested missing feature, inspect H, claim model authoring, prove semantic qualification or grant admission. M4 used only the sanitized coordinator B ref and six pinned wheel files. No H/private-history/reference-test contents, M2 private execution data, native model calls, downloads or other owners' workspaces were used.

No genuine frozen model-authored contract/scenario exists yet. Once the reviewed M2 handoff arrives, the same owner must complete checker proposal/finalization integration, grounded evidence validation, genuine feature/preservation case generation and subsequent M5 qualification support. This checkpoint supplies usable mechanical grading, not stubs or fabricated feature success. M5/M6 own task authentication, lifecycle admission, human attestation and quarantine; a BUILT diagnostic task can be mechanically graded without any admission claim.

Supported semantics are intentionally limited to flat ordinary typed observations and closed comparisons; no callbacks/object identity, implicit normalization, floating tolerance, arbitrary executable generator/controller code, stateful services, arbitrary additional artifacts or dependency changes are accepted. M3 per-operation resource bounds are enforced; the additional grader budget is checked between operations, not a new hard aggregate deadline. Grade receipt-publication replay has diagnostic coverage; the M3 pending-publication forwarding mechanism has not received a separate native storage fault injection. No training/evaluation or full requested-feature qualification evidence is claimed.

Raw logs are in `docs/evidence/M4/core/`; private immutable payloads remain in their recorded `.feature-rl/research/M4/core-*` stores. `summary.json` consolidates those logs and the final source identities. Independent review of this core and immediate downstream integration are the next steps.

## Core review round 1 corrections

Addressed the two confirmed findings in `docs/reviews/M4-core-round1.md`. The loader now rejects any fixed recipe wall/CPU/memory/PID/disk/output limit above its contract grant, with an unsupported/null grade before worker creation; equal or larger grants remain supported and solver token/tool budgets remain separate. Submission resolution now validates trusted B once before candidate parsing, converting malformed or size-rejected B into `ArtifactIntegrityError` and therefore infrastructure/null. Candidate-controlled delta violations still produce zero, with original submission/seed and unexecuted case records preserved.

The new focused regressions first produced **9 intended failures and 2 passing controls**; the isolated resource fix then passed 7 checks. After both fixes, the unchanged independent review reproductions plus `tests/test_grading.py`, `tests/test_submission.py` and `tests/test_verifiers.py` passed **58 tests in 0.53 seconds**. Evidence, exact commands, exit statuses and changed-source hashes are in `docs/evidence/M4/core-round1-fixes/`. No Docker/native/model/H/network work or broad historical suite was run for these preflight changes; existing real route evidence was not rerun. This correction changes only the two reported boundaries; same-reviewer closure and integration follow.

## Remaining authoring code completed for review

Implemented checker proposals, exact B/contract/scenario/evidence finalization,
actual configured-provider authoring with diagnosed repairs and authenticated
archive recovery, and immutable control attachment. `VerifierBundle` construction
uses the same mechanical validator as grading. Generated adapter/source bytes are
inert on the controller; only existing M3 workers execute candidate code.

Added separate native-capable control and alternative authoring services using
M2's explicit stage API (`29412648`). Negative controls may consume explicitly
bounded reference excerpts only after exact SourcePair B/H joins. Alternatives
consume B, visible contract and public checks only. Real source-only submissions,
M0 ControlPatch values and private control records expose actual consumed contexts,
provider evidence and costs to M6/M5. Expected validity and independence remain
unverified; no model or human-origin assertion is manufactured.

Focused validation: **84 passed in 1.34 seconds**, including 28 new authoring checks
and existing M4 loader/submission/grading checks affected by the shared validator.
Tests exercise actual LocalGenerationProvider request/schema/protocol/archive/cost
processing with explicitly diagnostic backend/process responses, exact immutable
publication replay, provider success/failure recovery, preparation outages,
diagnosed changed-input repair, rejected-journal recovery, family/oracle/type/
mandatory rejection, source-policy rejection, control attachment, and synthetic
private reference versus alternative separation. Raw command/result and source
hash manifest: `docs/evidence/M4/authoring/`. No original H reads, native model
calls, Docker calls or GPU calls occurred in this slice. The previously completed
real Click grading matrix and M6 diagnostic integration were not rerun.

Code now exposes the remaining M4 APIs needed for full factory orchestration.
Actual model-authored feature construction, semantic checker challenges, native
independent alternatives and human-qualified task admission remain empirical gates,
not code success claims. The phase explicitly defers new task-generation model
calls. M6 must orchestrate the supplied services, global budgets and frozen control
records; M5 must diagnose complete control coverage, actual independence and human
review. The complete API and concrete native evidence fields are in
`docs/interfaces-M4.md`.
