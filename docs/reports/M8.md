# M8 status

The independent pre-execution checkpoint is implemented. It freezes source lineage, locked-test roster, A/B/C/D protocols, matched budgets, paired trials and explicit case seeds; validates complete trial accounting; computes honest all-assigned and valid-only arm rates and task/family paired uncertainty; records an external-corpus adaptation funnel; and performs authenticated historical audits against real Registry, rollout and M4 grade records with M5 external human trust and quarantine tracing.

The initial checkpoint had 13 passing owned CPU diagnostics. The round-1 audit/statistics correction adds focused hand-derived and actual-Registry checks; its 21-test receipt is recorded in `docs/evidence/M8/audit-round1-checkpoint.md`. These diagnostics are not evaluation results, task qualification, human approval, or evidence that any learned arm exists. The metadata-only SWE-Bench++ feasibility review remains in `docs/evidence/M8/baseline-feasibility.md`; no dataset rows, solutions or locked-test payloads were acquired.

`EvaluationService.evaluate(config)` now composes M7's inert claimed-session factory, native activation receipts and actual `AgentRunner`. It authenticates current released roster tasks and selected M7 training checkpoints, starts workers only after the evaluate claim, validates live activation before each trial, passes each frozen case seed once, and consumes selected M4 evidence without regrading. A frozen execution receipt lets report recovery finish after pending owned-session cleanup without another rollout. Four focused CPU checks use a labeled native-session boundary double; no CUDA worker, GPU evaluation or empirical result was produced.

The external-row adapter is independently usable with caller-supplied private rows, authentic M1 evidence, a frozen locked-evaluation roster and aligned actual M6 `BuildInputs`. It rejects family/lineage overlap, validates request/B/license and source-inspection joins, and delegates source screening plus construction to the real Factory. Eight focused diagnostics use labeled local fixtures, including an actual local Git-to-M1 intake; no SWE-Bench++ row was acquired or adapted. Release patches and native tests never become trusted reward evidence.
