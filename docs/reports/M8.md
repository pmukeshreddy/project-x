# M8 status

The independent pre-execution checkpoint is implemented. It freezes source lineage, locked-test roster, A/B/C/D protocols, matched budgets, paired trials and explicit case seeds; validates complete trial accounting; computes honest all-assigned and valid-only arm rates and task/family paired uncertainty; records an external-corpus adaptation funnel; and performs authenticated historical audits against real Registry, rollout and M4 grade records with M5 external human trust and quarantine tracing.

The initial checkpoint had 13 passing owned CPU diagnostics. The round-1 audit/statistics correction adds focused hand-derived and actual-Registry checks; its 21-test receipt is recorded in `docs/evidence/M8/audit-round1-checkpoint.md`. These diagnostics are not evaluation results, task qualification, human approval, or evidence that any learned arm exists. The metadata-only SWE-Bench++ feasibility review remains in `docs/evidence/M8/baseline-feasibility.md`; no dataset rows, solutions or locked-test payloads were acquired.

`EvaluationService.evaluate(config)` is the remaining implementation dependency. It will be wired only to the actual M7 runner API after publication, passing each frozen case seed exactly once and validating the returned rollout and M4 receipt. It will not use an injected or always-blocked adapter. GPU training/evaluation, external-corpus adaptation, frozen held-out task construction, real human audits and empirical comparisons remain unrun.

The external-row adapter code is now independently usable with caller-supplied private rows and authentic M1 artifacts. It validates the frozen family/dedup/rights frame and delegates source eligibility to the actual M6 Factory. Its two focused diagnostics use labeled synthetic rows; no SWE-Bench++ row was acquired or adapted. Real construction remains gated on actual M2/M3/M4 inputs and cannot use the release's patch or native tests as trusted reward evidence.
