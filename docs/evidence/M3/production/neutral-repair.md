# Click environment repair 1: system pager

Construction ledger: baseline-health attempt `click-initial-1` FAILED (1 failed, 1394 passed, 23 skipped, 30000 deselected, 1 xfailed, 189 errors). This is environment-stage repair **1 of 2**, candidate repair **1 of 4**. Focused diagnosis is diagnostic expenditure, not another invented task attempt. Product interface fixes do not reset these counters.

`baseline-diagnosis-1.json` and `baseline-diagnosis-2.log` preserve a single original B pager test with an external JUnit wrapper. `/usr/bin/less` is absent; the fallback path closes pytest's captured stdout, causing ValueError and subsequent capture/teardown failures. `/tmp` had 2048/2048 free blocks before this case and 2043 after, and cgroup memory.events reports no max/oom/oom_kill events. Original assertions remain intact. The earlier diagnosis launcher failed on missing controller PYTHONPATH before any container launch; that failed log is retained separately.

The exact official Debian bookworm arm64 package is `less 590-2.1~deb12u2`, 128172 bytes, SHA256 `eb430d92921f98b163031ee3ac81a96110d1f20bd84f67faab06dad50c75d744`. Acquisition used bounded HTTPS reads from [Debian package metadata](https://packages.debian.org/bookworm/arm64/less/download) and [the official archive](https://deb.debian.org/debian/pool/main/l/less/less_590-2.1~deb12u2_arm64.deb); URLs, timestamps, bytes, SHA256 and package control data are in `less-acquisition.json`. No solver source, package hook, sdist, or target-package wheel was involved. Host processing decoded inert bounded ar/tar members only.

Package dependencies are libc6 >=2.34 and libtinfo6 >=6. The pinned Python base has libc6 2.36-9+deb12u14 arm64 and libtinfo6 6.4-4 arm64, observed through hardened trusted inventory (`system-inventory.log`). The repair copies only the exact `/usr/bin/less` regular ELF (215000 bytes, SHA256 `2dd8d7734f5b43961b1d7243198ffaf1037539b122f2e266fccc9621104ed33b`) and its copyright notice into a new image. It is a binary-distribution subset, not a claim of running dpkg or installing package hooks; auxiliary lesspipe/lesskey programs are not added.

The Dockerfile contains FROM pinned Python + two COPY operations and a descriptive label, with no RUN. Build used network none, pull false, arm64, no provenance, a 219.78 kB allowlisted context, and completed exit 0 in 0.368 s. Exact command/Dockerfile/build output and all base/derived layer identities are retained. The derivative's first layers exactly equal the original Python base, with two appended COPY layers; no historical source or target wheel enters it.

Docker Desktop's containerd image store exposes a real locally resolvable RepoDigest `feature-rl-m3-less@sha256:b28d3b1eeaa6251e82354fa61dc28a29afead9e2d7179436e77ca8ba612032fa`. Build's config digest is separately `sha256:59cff2cafc7819fee5cdef4ab8f76fa5ec574d16d78a2661953dfd1498aaf185`. They are different identities; no invented registry digest or push. Runtime must use the manifest RepoDigest with --pull never, requalify the effective boundary, and bind the repair to every B/H/candidate/development recipe. Removal of this owned local image makes that recipe unavailable until explicitly reconstructed and its actual resulting digest checked; exact digest reproduction across builders is not promised. Public acquisition and local trusted build cost $0, CPU not separately measured. Actual repaired baseline compatibility is still an open gate at this note's creation.
