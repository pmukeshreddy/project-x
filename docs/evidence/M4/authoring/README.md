# M4 authoring completion evidence

`validation.json` binds the exact owned Python source/test hashes to the command,
exit status and raw `focused-pytest.txt` result: 84 passed, 28 new authoring checks.

These are CPU diagnostics using actual M0 storage, reviewed M2 evidence resolution
and LocalGenerationProvider request/schema/protocol/archive/recovery/cost code,
and M4 finalization/submission/grading-controller validation. The backend identity
and process event stream are explicitly test doubles from the existing generation
test helpers; they do not execute inference. Reference tests use synthetic private
source bytes and an explicitly synthetic SourcePair, never original H.

New checks cover complete checker construction/loading; requirement/family/oracle/
operand/mandatory rejection; provider success and failure replay; all final
publication stages; inert preparation and rejected-journal outages; diagnosed
meaningful repairs; separate control/alternative stages; exact source-only deltas;
synthetic reference excerpt joins and alternative exclusion; source-policy
rejection with retained cost; hostile outer-manifest import; and immutable
control attachment with duplicate/target/version checks.

No native task-generation, original H/history, Docker or GPU operation occurred
in this slice. Prior real Click/M3 grading evidence and M6's closed diagnostic
integration remain separate. The diagnostic responses cannot establish semantic
control validity, independent model authorship, feature qualification or human
approval. API/evidence consumption is documented in `docs/interfaces-M4.md`.
