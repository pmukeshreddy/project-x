# M0 narrow local image identity extension

Read this first. Same accountable M0 owner, base `fe8ad2e`. M3's first actual Click baseline build/import succeeded but its baseline suite failed because the immutable Python base lacks `less`; external JUnit/resource evidence isolated that cause. A trusted package-only local derivative is an authorized neutral environment repair. An unpushed Docker image has a content-addressed image config ID (`sha256:<64 lowercase hex>`), without a registry `RepoDigest`. M3 must not invent a repository digest or push externally merely to satisfy our schema.

Implement a backward-compatible extension only to `EnvironmentRecipe.image_digest`: accept either the existing repository-at-sha256 form or a complete local Docker image config ID. Preserve strict string validation, exact SHA-256 length/lowercase requirements for the new form, canonical serialized bytes for existing artifacts, and all other recipe/resource/provenance rules. No mutable tag-only references, short IDs, arbitrary prefixes, implicit resolution or successful defaults. A schema-valid string still needs M3's actual allowlist/base/package/image/boundary verification; M0 does not attest Docker identity. Keep `ServiceRecipe` unchanged because this dispatch does not establish service-image support.

Writable: the necessary M0 contract model change, corresponding focused tests, `docs/interfaces.md`, new `docs/reports/M0-local-image-id.md` and `docs/evidence/M0/local-image-id/**`. Do not edit any other module, M3 untracked files, root progress, prior evidence or shared dependency configuration. M3 is paused from product/test edits during this slice, but trusted image research/evidence continues. No subagents, Docker, model/inference, network, assets or native provider reruns.

Use meaningful RED/GREEN cases for the new accepted local ID and invalid lookalikes, plus retained repository-digest round-trip/storage identity. Run focused contract/store checks. If running a broader suite, use only already tracked module test files; M3 is incomplete and must not be imported or claimed as validated by this slice. Do not rerun consumed native model gates.

Record actual commands, UTC, exit/results, source/test/doc hashes, limitations and commit. This is a shared-interface update, not a new task artifact or image build. Commit only owned paths and return FINAL for independent spec/quality review. Same owner fixes any findings; M3 resumes product implementation after review and coordinator handoff.
