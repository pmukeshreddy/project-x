# M3 environment construction and lifecycle owner

Read this first. Implement Task 4 from `docs/implementation-plan.md`, spec §§7-8,10,17. Use approved M0/M1/M2 interfaces from their docs and actual code, never invent upstream APIs. Base revision supplied on dispatch. No subagents.

Writable: `src/feature_rl/environments/**`, corresponding tests, `docs/reports/M3.md`, `docs/evidence/M3/**`, `docs/interfaces-M3.md`. Shared schema/config changes go to coordinator/M0. No product edits elsewhere; only stage own paths.

Docker Desktop was started and is available, server 27.4.0 Linux arm64 with ~8 GB memory; host has ~27 GB free. Existing images/containers/volumes belong to unrelated work. Never prune/alter them. Task authorization covers choosing and pulling a small trusted public Python image by verified immutable digest and local sandbox runs; no new approval or paid resources. Do not install SkyRL here. Training is externally blocked; first runtime is Click's actual B/H recovered by M1.

Before historical execution, exercise established Docker isolation with trusted probe code: explicit seccomp (daemon reports unconfined), all capabilities dropped, nonroot identity, no-new-privileges, private PID/mount/network, read-only root plus bounded writable workspace/tmp, enforced CPU/memory/pids/time/output/disk, no host sockets/home/secrets/private artifacts. Validate effective restrictions and actual denial behavior. `--network none` is sufficient for initial no-service track when tested; Harbor nftables isn't required for direct Docker no-network runs. No fallback to host subprocess candidate execution.

Infer historical interpreter/setup from archived CI/manifests. Pin base digest, interpreter, dependency wheel hashes and test tools; acquire inert approved dependency bytes separately, execute any package hooks only in disposable workers. Baseline/reference/candidate share explicit neutral setup policy. Keep H out of reusable dependency layers. Verify actual imported Click path points to supplied snapshot, not installed future package. Prefer minimal faithful setup; never alter application semantics to make tests pass.

API must support: constructing/persisting a reproducible recipe; bounded isolated commands against source snapshots; fresh development workspace; reset and interruption cleanup; observable stdout/stderr/exit code/resource results; controlled extraction of allowed source artifacts without executing them. M4 must be able to run generated adapters with ordinary serialized input and receive observations while keeping private comparisons out of workers. Expose concrete typed request/result and lifecycle API in docs/interfaces-M3.md.

First real evidence: hardened sandbox probe; Click B baseline regression health; imported package paths for B/H; three fresh baseline/reference executions and three reset-after-interruption checks as feasible; deliberately nonterminating process/output overflow cleanup; filesystem state restored, processes removed, no external network access. Feature absence/reference feature checks belong to M4/M5, not fake runtime success. Record local-service semantics unsupported until actual independent Redis/service cleanup route exists; don't mark broader service support executed without proof.

Tests: invalid/unpinned image/recipe, undeclared network/mounts, unsafe paths, missing daemon typed failure, process timeout/no orphan, output/disk caps, interruption cleanup, import shadowing detection, command failure classification. Actual Docker tests must fail explicitly if unavailable, not blanket skip or mock away. Test doubles stay in tests and are not gate evidence.

Preserve full commands/status/image+dependency hashes/receipts/revision/cost in docs/evidence/M3, report limitations precisely. Real environment code must be usable by M2 discovery and M4 grading. Commit and independent review; same owner handles integration fixes and later service slice if needed.
