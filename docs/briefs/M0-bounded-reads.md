# M0 bounded artifact reads for M3

Same accountable owner `/root/m0_contracts`; no subagents. Read this brief, the actual artifact store and tests, existing M0 interfaces and `docs/evidence/M3/production-interface-handoff.md` when published. M3 identified a real upstream gap: `_read_file` calls unbounded `stream.read()`, then `get_bytes` decodes the entire base64 payload. The runtime cannot apply source/dependency input caps before those allocations through the public API. Root confirmed the source. Assigned integration base is supplied at dispatch. M2 provider product e14f679 is independently under review; no other product owner edits.

Implement a backward-compatible explicit bounded-read option through the authoritative M0 store APIs, rather than letting M3 duplicate secure store-path handling. Expose a serialized-envelope byte cap on `get_bytes` and `get_artifact`, and a decoded-payload byte cap on `get_bytes`. Choose clear keyword names and publish their exact semantics. Existing uncapped calls retain compatibility; M3 will explicitly supply the limits it needs. A decoded cap alone must not be documented as bounding envelope/JSON allocation. Do not change canonical identities, artifact schema versions, visibility, dependencies or unrelated models.

Validate cap arguments (including Boolean/noninteger/negative cases) and provide a public typed size-limit failure. Preserve authorization, descriptor-relative/O_NOFOLLOW/nonregular-path denial, reference/canonical/digest checks, strict payload validation and exposure checks. Check regular-file size before reading and use a bounded read that also catches growth after stat; an initial fstat check alone is insufficient. Reject oversized envelopes before JSON decode. For byte payloads, reject size excess before allocating the decoded result; preserve canonical base64 validation and accept exact boundaries including empty payload and all padding cases. No claim that Python JSON object expansion has a zero-overhead memory ceiling follows from a serialized byte cap. Keep duplicate-write/collision handling safe and scoped if it shares the read helper.

Meaningful tests: exact-boundary and oversized opaque/typed objects, cap argument failures, stat-underestimate/file-growth bounded-read behavior, oversize rejection before JSON/base64 decode, canonical/tampered/visibility/path defenses with bounded calls, and unchanged default-call compatibility. Test doubles are tests only; no real model/runtime/historical/download/install/remote execution. Run focused M0 checks and the affected full suite once complete, retaining exact command/UTC/exit/stdout/stderr/source identities. Commit only owned paths and return FINAL. Independent review plus downstream M3 consumption is required before claiming the gap closed.

Writable: `src/feature_rl/artifacts/**`, corresponding artifact tests, `docs/interfaces.md`, a new `docs/reports/M0-bounded-reads.md`, new evidence under `docs/evidence/M0/bounded-reads/`. No M2/M3 product edits, shared dependency changes, root briefs/plan/progress changes or subagents. Preserve all old evidence, Finder files and `.superpowers/`.
