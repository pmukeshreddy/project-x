# Independent M2 authoring/scenario code review

Read `docs/briefs/review-policy.md`, the current user scope in `docs/decisions/code-completion-scope.md`, `docs/briefs/M2-authoring.md`, the proposal/finalization ruling and the bounded authoring allocation. Assigned implementation base is `4530ad7` (reviewed/integrated M3 and provider). Root supplies the actual final target and complete diff once the owner is done. Do not begin against an assumed implementation or read H before an actual first draft is frozen.

Review the complete new requirement/scenario service and affected generation-stage integration, actual upstream M0/M1/M3 code and the owner's report/evidence. Do not reopen unrelated, already reviewed provider transport or runtime scopes without a concrete regression. No product/index/commit changes or subagents. Write only the assigned review report and necessary new review evidence; root commits those files. Independent specification and quality verdicts must distinguish implementation defects from unexecuted experimental construction.

Key correctness checks:

- Proposal fields preserve actual shared constraints; final M0 artifacts receive true controller-owned inputs/provenance/costs only after the real operation. Generated output cannot choose its own successful evidence or bypass final validators.
- Initial source access is bounded and exactly allowlisted by actual M1 authoring references. Quote and locator must match resolved bytes, retrieved contexts are attributable, license remains outside the behavior prompt, H/history/private views are excluded. Discovery executes actual baseline code through M3; host AST/source inspection is not substituted for execution.
- The explicit scenario-planning stage accepts the frozen contract without an existing scenario plan, refuses reference/private baseline data and scenario role relabeling, and preserves initial/checker stage boundaries. The service dereferences the exact contract, rejects duplicate/unknown IDs and omissions, and covers mandatory feature plus compatibility requirements under every supported seed.
- Unsupported public observables, missing discovery evidence and critical unresolved ambiguities produce explicit non-success. Strings like an observable's name do not prove semantic support. Runtime results must match the source/recipe/action claimed; worker text cannot attest controller success.
- The first grounded draft is immutable before separate privileged feasibility review. New semantics produce a new version and invalidate downstream evidence. A human-review requirement cannot be fulfilled by an agent or a model.
- Every actual call/failure/retrieval/repair retains its identities, known costs and typed disposition. Publication retry must not invoke the model again. Per-stage and total candidate limits, including the prior Click repair, are enforced through the documented actual API; no swallowed failures or silent bounds/model changes.
- The API is usable by M4/M6 with explicit artifact resolution and reproducible configuration/commands. If a bounded model attempt failed, the generic implementation still needs complete code and diagnostic verification; a handwritten fixture must not be presented as a generated contract.

Read existing raw results and source hashes before choosing new diagnostics. No full suite, native inference, tokenizer repeat, Docker historical suite, network or download merely to confirm an existing receipt. A narrow diagnostic for a concrete new concern is permitted within the declared local boundary. Any additional native/model run requires a separately allocated unconsumed call; the old provider smokes remain consumed.
