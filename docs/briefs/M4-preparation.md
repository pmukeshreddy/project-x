# M4 bounded interface preparation

You are the accountable M4 implementation owner. This dispatch authorizes a bounded read-only preparation while M3 is the sole product implementer. Base: 9e8434c33b0959648aae99b86cd17329a45af09e. Read `docs/briefs/M4.md` first for your eventual module requirements, then this narrower authorization. No subagents.

Read specification §§6,8–10, actual M0 `ScenarioPlan`, `VerifierBundle`, `TaskBundle`, `OperationResult` and artifact-store APIs, `docs/interfaces-M2.md`, and the coordinator's `docs/decisions/M2-proposal-finalization.md`. The real M2 contract/scenarios and reviewed M3 runtime do not exist yet. Do not implement against guessed methods or inspect H, private source history, reference tests or actual future checker inputs. Do not import/execute application source, start Docker, load models, run inference, acquire assets or network resources.

Resolve one concrete interface question: M0 scenarios contain grounded prose, while trusted controller comparisons must be deterministic and non-executable. Identify the smallest M4-local typed proposal/finalization boundary that preserves actual shared fields and permits generation of worker setup/adapter code, private seeded case inputs and external comparisons without executing generated controller code. Distinguish structural/type checks from evidence grounding and actual semantic qualification. Document the exact joins M4 must enforce across the frozen contract, scenario plan, case IDs and requirement IDs. Identify any genuine shared-interface blocker, without creating a duplicate shared schema or asking for speculative extensions.

Also identify the required M3 capabilities (in words until its reviewed API exists) for source-only rebuild, bounded ordinary observations, attributable source on limits, and cleanup/infra failure classification. These are dependencies to verify later, not claims that the current incomplete runtime satisfies them. No hand-authored Click feature answers or frozen contract semantics.

Write only `docs/evidence/M4/interface-preparation.md`, ideally one to two pages. Report concrete supported initial observation/comparison semantics and explicitly unsupported cases; source identities read; open joins to check after actual upstream handoff; and no product/native execution claims. Do not edit product, interfaces, root ledger or other evidence. Commit only that owned note and return FINAL with commit and concerns. Later the same owner will implement the complete M4 module against reviewed M2/M3 and handle its fixes.
