# M2 provider — round4, same current accountable owner

Read this first, then `docs/reviews/M2-provider-round3.md` in full and its retained reproducer. Current owner is `/root/m2_authoring_recovery`, following the explicit capacity-failure transfer. The user execution instructions say the same owner handles its fixes; this is that replacement owner's second fix cycle, so it retains ownership. No product edits by the coordinator, no new implementation owner or subagents.

Last product `20af743778815635bb4af521164199bd8f09dcb8` has66 focused/221 full owner tests with mechanically verified source/log identities. Assigned base for this fix is supplied at dispatch. R2 is independently closed: preserve all ID limits, forged-instance revalidation and bounded publication/recovery. The remaining findings are R1 (P2 scalar-union Literal coercion) and R3 (P2 callback/lifecycle semantics). Use the exact report and full-provider reproductions as acceptance inputs; no native inference is authorized.

R1: raw numeric1 in `Literal[True] | int`, and raw3.0 in `Literal[3] | float`, pass the inert guard then return a different Boolean/integer Literal during ordinary validation. Model-class restoration does not retain a scalar union branch. Fix the actual returned semantics, including nested forms, or make a precisely unsupported structural form fail before backend verification. Do not let one validation graph approve a value and a second silently reinterpret it.

R3: a first union model's rejecting `model_post_init` prevents the valid second model from being selected after inert branch pinning; a non-union before field validator that normalizes a string is stripped, so valid ordinary JSON fails only after execution. Correct the complete declared capability boundary. Account for related before/wrap callbacks, initialization/default behavior and definition references when the implementation analyzes union alternatives; do not silently skip a core form it cannot validate soundly.

Coordinator interface ruling: the provider need not promise arbitrary Pydantic callback compatibility. It must support the M0 schemas and explicit proposal shapes the pipeline actually uses, with exact Literal raw types and documented JSON enum/tuple/UTC transport. An unsupported structural schema may receive an attributable `GenerationSchemaUnsupportedError` before any backend verification or worker call. Such a refusal is a provider capability limitation, not an invalid model response after a wasted call. Preserve supported Boolean/patterned/model-union alternatives and single default/post-init/after-callback execution where supported. Do not merely reject every input union, relax raw types, substitute a backend, change shared M0 contracts, or introduce another incomplete validator approximation. Cost if this conservative subset excludes a needed future schema: that caller must use an explicitly supported proposal or this owner must implement and review the missing capability before the call; no hidden schema conversion or experiment change is allowed.

Tests must start from accepted ordinary strict-JSON baselines. For supported forms assert exact returned Python/JSON types and values; for unsupported forms assert typed refusal before backend verification/runner invocation with real artifact/recovery accounting. Include all concrete reviewer cases and the actual M0 requirement/scenario transport controls. Keep test doubles confined to tests, the source-attribution inventory complete, and prior receipts immutable. Do not re-run model imports, downloads, old real smokes or unrelated closed probes.

Owned paths remain generation/**, corresponding generation tests, M2 interface/report and new round4 evidence. Coordinator briefs/progress/plan and `coordinator-*` / `owner-transfer-*` evidence are excluded. Preserve unrelated `.DS_Store` and `.superpowers/`. Finish narrow corrections, focused and affected full checks, mechanical final file/log/command/UTC/exit receipts (separate outputs for each retained verification attempt), owned-path commit and FINAL. Independent scoped re-review and one subsequent current-source native coordinator check remain necessary before M3 production. Full M2 remains partial until runtime-backed authoring/scenarios.
