# M6 factory coordinator implementation owner

Current scope: read `docs/decisions/code-completion-scope.md`. Complete all orchestration, registry, CLI and integration code, available non-GPU checks and reproducible commands. Human qualification, collection pilots, GPU training and experimental comparisons are deferred execution milestones. Their absence must produce exact enforced prerequisite failures and must not stall independent code. Documentation must distinguish implemented operations from actually executed experiments.

Read this first. Scope Task 8 and later CLI integration from Tasks 9-11. Spec §§3,6.4,10,13-14,18 and approved M0-M5 actual interfaces are binding. Base supplied at dispatch. No subagents.

Writable: `src/feature_rl/pipeline/**`, `registry/**`, `cli.py`, `__main__.py` if approved by coordinator, factory end-to-end tests, README and runbook, `docs/reports/M6*.md`, `docs/evidence/M6/**`, `docs/interfaces-M6.md`. M0 owns package/dependencies/entry-point metadata; request exact wiring changes through coordinator. Other product defects return owners, never add shadow implementations.

Read `docs/lifecycle-admission.md`: qualification approves complete built root T0. Qualified/calibrated/released manifests must match every canonical T0 payload field except state/qualification. Keep new transition evidence/costs in OperationResult and the append-only ledger; do not silently exempt mutable metadata. Every released-task consumer resolves Tn→Q→T0 and checks qualification, equality and quarantine. The complete solver view must be frozen before human review; release export cannot change its contents.

M5's implementation review may precede this module while its real first-task qualification remains pending the M6-owned final solver package. Build and freeze the real T0 first, then coordinate that execution through the existing M5 owner/service. Do not borrow a provisional test fixture or an earlier approval for new solver bytes, and do not require an already-qualified task in order to construct the built root that qualification needs. This is an explicit integration dependency, not an exception to any qualification gate.

Connect real services for construct/qualify/release/grade. Use strict source/config/artifact identities and record rejection, unsupported semantics, infrastructure and cost separately. `construct(candidate)` can produce built/provisional task but must not call it qualified. State transitions require actual same-version evidence; calibrated is difficulty, not validity. Qualified/released outputs immutable; artifact changes create dependent rebuilds, known defects quarantine and trace consumed updates.

Implement SQLite task/run/job index plus durable append-only JSONL events with idempotent content-derived job identity, atomic publication, crash/resume and duplicate completion protection. Store attempts/costs separately from unique outputs/rewards; retries do not mint duplicate tasks or omit incurred cost. Bounded queue/backpressure and explicit retry budgets. Document durability across database/event publication interruption; don't silently drop partial attempts or rewrite past outcomes.

Read your preserved mechanism preflight `docs/evidence/M6/durability-preflight.md` and raw evidence at8b68d99. Its SQLite event/outbox authority and verified JSONL projection passed synthetic process-crash checks only; production I/O faults, bounded concurrent writers, path controls, real artifact reconciliation and admission still need implementation/review. Do not copy the prototype as an approved registry. Its suggested observation-before-publication order describes work controlled by that prototype. Existing M2 publishes its own attempt/response/cost archives inside generate() before returning, so actual M6 integration must attribute and reconcile those durable upstream receipts across crashes and publication replay, using the real reviewed API. Do not assume upstream services defer all artifact publication until M6 commits an observation or claim cross-service atomicity.

Build solver package from positive allowlist of B, public instruction/tests and solver-safe runtime fields. No future Git history, H, private references/checks/expected values, authoring sessions, caches/build products or installed target wheels. Hash and inspect actual package inventory plus M3 images/mounts. Do not copy a private task and delete a few filenames. Reject symlinks/path tricks and unexpected members at release. Never release a missing-human/alternative/provisional task.

First real integration: use actual Click source, generated contract/scenarios, pinned runtime, external checker and qualification. Execute every available gate through production APIs; preserve an exact blocked human or provider gate. Failure-path integration is real evidence for that path, not successful full conversion. No fake production factories, canned commands, or success return when a module is absent.

CLI is thin argparse/library wiring. Implement only real available operations; later same owner adds run/train/evaluate/audit when M7/M8 have reviewed APIs. Command help must expose dependencies/options clearly and return nonzero exit plus typed machine-readable failure for blocked operations. Document setup with explicit PYTHONPATH=src where this Mac's editable .pth issue requires it, and normal wheel installation path.

Tests: full request→construction→control→release→grade route when gates available; version drift rejection; idempotent/restarted jobs; crash between data/index/event publication; forged qualifications; solver leakage/allowlist; missing evidence; duplicate attempts and complete cost accounting. Actual historical execution only through M3. Unit doubles stay in tests, distinctly labeled.

Write runnable commands and evidence index in README/runbook; exact tested revision/exit/artifact IDs/receipts in report/evidence. Keep M6 partial until genuine agent/training/evaluation CLI integration and final cross-module review. Same owner retains all integration fixes.
