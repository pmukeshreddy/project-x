# M0 training dependency configuration and local CPU validation

Same accountable M0 owner; not dispatched until root assigns a free product slot. Read `docs/decisions/code-completion-scope.md`, `docs/decisions/training-dependency-overlay.md`, your metadata preparation at6e27c37578efbd637db5a083169188f9fc0c5065, actual project locks and the pinned SkyRL/Harbor metadata. No subagents.

Implement reproducible dependency configuration for two explicit environments: a separate local CPU diagnostic environment and a small project overlay for the deferred Linux CUDA environment. Preserve the current core/dev and MLX authoring pins and `.venv` bytes. Do not widen the project's Pydantic requirement, select generic PyPI CUDA packages, install Linux binaries on the Mac or claim CUDA compatibility from metadata.

Writable scope: necessary project dependency configuration/lock files, new exact wheel manifest and bounded dependency-acquisition/setup helper under `scripts/` if needed, `docs/setup-training-dependencies.md`, `docs/reports/M0-training-dependencies.md`, new `docs/evidence/M0/training-dependencies/**`, and dedicated configuration tests. No other module product edits. Root supplies active integration base before implementation; keep helper responsibilities confined to verified dependency setup, not training or evaluation.

For local CPU diagnostics use the exact ten-wheel metadata closure already recorded (91095546 bytes total): torch 2.11.0, filelock 4.0.1, typing-extensions 4.16.0, setuptools 80.10.2, sympy 1.14.0, networkx 3.6.1, jinja2 3.1.6, fsspec 2026.9.0, markupsafe 3.0.3 and mpmath 1.3.0. Reuse already verified cached wheels where exact identity matches. Add only the separately pinned core/dev tools needed by actual diagnostics. Do not copy or mutate the MLX environment. Create a dedicated `.venv-training-cpu` or explicitly private equivalent with the current compatible CPython3.13 interpreter.

At dispatch, bounded acquisition is authorized only for the exact selected wheels plus the recorded Linux Pydantic/core overlay wheels: at most110MiB newly fetched wheel payload, no model weights or CUDA stack, no source builds, no alternate versions. Archive URL/UTC/status/bytes/hash and refusals; verify full hashes and wheel metadata before installation. Installation must be wheel-only/offline/no-deps/require-hashes from that verified wheelhouse. No unbounded dependency resolution or silent online fallback. Current metadata receipts are sufficient inputs; no repetitive broad metadata research.

Run actual package consistency and an explicitly CPU-only import/tensor/autograd/optimizer/save-reload smoke in the isolated local environment. Fixed small tensors, no real model or feature outcomes. Record CPU device/dtype, package versions, commands/time/status and changed tensor/reloaded equality if measured. Label this dependency/CPU diagnostic, never GPU or feature-learning evidence. Coordinate native memory work with root so it does not overlap M2 authoring inference. Preserve failed attempts without widening constraints. Actual M7 mathematical/trajectory integration tests come later in this environment.

The Linux overlay is exactly Pydantic2.13.5 plus core2.46.5 at existing recorded hashes, over an exact frozen SkyRL FSDP/Harbor installation. Provide a real hash-pinned overlay file and precise future commands, including installed-metadata consistency/inventory verification and explicit no-sync runtime. The final feature-rl wheel is built and hashed from its intended actual revision; no placeholder hash or success-shaped manifest. Preserve the upstream/custom CUDA package identity set and clearly state that the complete Linux installation and CUDA imports/execution are unverified here.

Verify unchanged core/dev/MLX package pins and existing environment inventory. Run focused configuration/installation checks only; root coordinates broader tests after independent review. Publish the usable CPU interpreter and exact reproducible setup/overlay commands, artifacts and limitations, then commit only owned scope and FINAL. Same owner fixes independent review findings.
