# M5 qualification and admission owner

Read this first. Scope Task 7, binding spec §§6.4,7,9-10,17. Use approved M0/M4 APIs and actual artifacts; base supplied at dispatch. No subagents.

Writable `src/feature_rl/qualification/**`, dedicated tests, `docs/reports/M5.md`, `docs/evidence/M5/**`, `docs/interfaces-M5.md`. Shared schemas belong M0 and runtime/grading fixes return M3/M4. Commit own paths only.

Read `docs/lifecycle-admission.md`: qualification binds the complete built root T0; human approval authenticates both T0 and the frozen review-evidence package/policy. Later lifecycle manifests point back to its accepted report. This uses current M0 schemas and must not become a circular digest or an approval that survives changed controls, contract, runtime, source or solver packaging.

The final solver package is owned by M6 and does not yet exist when this implementation slice starts. Do not create a duplicate M5 packager or placeholder solver view to claim full qualification. Implement and review qualification against the actual available M4 primitives, preserving explicitly partial status; execute the complete first-task qualification with the same M5 owner once M6 has built and frozen the real T0/package through its production path. The module is not empirically complete before that integration. Human review happens only after that concrete task and its complete review evidence exist.

Implement a real `QualificationService.qualify(task_version: ArtifactRef) -> OperationResult` orchestrating actual grading/control/reset evidence and admission. Do not reduce it to checking booleans in a user-created report. Resolve artifact versions/digests, complete cases/requirements, actual grading receipts and declared control validity. B healthy preserved obligations, designated semantically missing feature, H passing, and independently authored alternative passing are mandatory. Equivalent mutants are excluded with diagnosis; syntax/import/infrastructure breakage is not a targeted semantic omission.

At least one targeted runnable negative per mandatory requirement; plausible incorrect solution, hard-coded public examples, regression-inducing implementation, early-exit/forged-verdict and the separate adversarial submission suite all accounted for. Any known false pass/false rejection/disagreement blocks release. Require at least three distinct fresh reference and three reset-after-interruption executions, with actual cleanup evidence. Evidence from another task/verifier/recipe version is invalid.

Pilot human review of contract/evidence and alternative validity is an external gate. A model/agent code review cannot sign it; a human-looking identity string is not authentication. Provide an explicit human-attestation ingestion/verification boundary backed by independently supplied review evidence and exact subject digest. Default absence/mismatch/unverified origin remains provisional/blocked. Test model signing and replay/version mismatch rejection. Never manufacture approval, human minutes, alternative independence or success evidence. Prepare concrete readable review package and smallest user action; coordinator requests genuine review once package is complete.

Bounded repairs: at most two repairs per failed stage, four total per candidate, initial and repairs all costed; diagnosed cause/change mandatory; semantic artifact change creates new version and invalidates dependents. Typed dispositions exactly cover accepted/provisional and specified rejection reasons. Quarantine known defective versions and return affected dependencies/updates for registry/audit trace, not silently mutate frozen artifacts.

Acceptance tests exercise every missing-gate bypass, duplicate/replayed receipts, wrong versions, false-pass controls, wrong semantic failure reason, forged human/model review, missing alternative, insufficient repeats, per-stage/total repair caps and idempotent attempts. Use actual M4 path for first Click qualification; if human review is absent, preserve all independently executed controls and report exact provisional gate rather than fabricate a qualified task.

Publish interfaces and complete evidence/report, distinguishing implemented gate enforcement from executed accepted qualification. Independent reviewer follows and same owner fixes.
