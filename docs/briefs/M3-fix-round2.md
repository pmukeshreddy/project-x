# M3 fix round 2: cleanup and terminal admission

Same accountable M3 owner. Product base `ad154f88013200d9c0134b808c9211032ed67a2b`. Read independent round2 report and `docs/evidence/M3/review-round2/terminal_cleanup.py` / `terminal-cleanup-result.json`. The original stale-source P2 is closed. One connected P2 remains: close pauses after pre-transition recovery; an intervening real development operation leaves an owned running container and `cleanup_pending` record after a missing-daemon cleanup fault; close then acquires its lock and returns normally with `closed=true` despite that pending ownership. The reviewer restored the daemon route, independently observed the exact live container, then recovered and removed it.

Make successful close/reset terminal admission include verified absence of relevant pending ownership while holding the same operation lock through the workspace read/check/write. Either recover/verify under the protected admission or explicitly fail if newly pending cleanup exists. Do not rely on the earlier recovery outside the lock. Preserve nonblocking overlap behavior, exact ownership checks, generation progression and the round1 source-save fix. Never mark pending records removed without actual verified absence. No new broad lifecycle architecture is required.

Writable only necessary M3 product/test changes, `docs/interfaces-M3.md`, `docs/reports/M3-fix-round2.md` and new `docs/evidence/M3/fix-round2/**`. Preserve every earlier report/receipt/diagnostic. No subagents, other module edits, downloads, model/native provider calls, historical B/H reruns or recipe changes. Actual Click counters remain environment1/2 and candidate1/4; this is a product defect fix. You are sole product writer after dispatch.

Add meaningful RED/GREEN coverage of close and reset with the confirmed recovery-to-admission interleaving. Exercise a real owned Docker cleanup failure, observe pending/live state, and require either verified recovery before terminal success or an explicit failed transition with unchanged source/generation/closed state. Verify subsequent recovery and intended retry; all owned containers must be removed. Retain the closed round1 regressions and test the affected M3 scope. Root will run the fresh whole integrated suite after independent re-review, so no duplicate full-project run here.

Record exact commands, UTC/exits, source/test hashes, states, live/absent container observations and actual partial costs; label any diagnostic scheduling/fault injection. Commit only owned scope and return FINAL for the same reviewer's scoped check of both terminal invariants.
