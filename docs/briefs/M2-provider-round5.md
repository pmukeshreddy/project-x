# M2 provider — round5, same accountable owner

Read this brief, `docs/reviews/M2-provider-round4.md`, both retained round4 reproducers, `docs/decisions/M2-literal-validation.md`, and the actual current provider before changing code. Current product is `44e9dcf1079a186a8a0432fa51333d1d07f6422f`; the assigned integration base is supplied at dispatch. Owner remains `/root/m2_authoring_recovery`. The user requires same-owner fixes; retain this available owner. The prior transfer was a service-capacity failure with a complete handoff. No subagents or cross-module changes.

Round4 independently closed R1 scalar/model/nested union coercion and retained R2 identifier/recovery bounds. Two P2 findings block clearance: R3 after callbacks can alter downstream chain or collection validation, so stripping them falsely rejects valid responses after execution; R4 restoring equal frozen models deduplicates sets/frozen sets after cardinality checks, producing successful invalid content. Both are full-provider reproductions with valid ordinary-JSON baselines and real archive resolution. The exact reports and receipts are acceptance inputs.

Use the coordinator's narrower validation-boundary decision. Actual M0 Literal fields are strings/string-enums; numeric and Boolean invariants use strict primitives. Keep the original strict JSON validator for schemas without numeric/Boolean Literal repair needs. For the numeric/Boolean Literal workaround, admit a small callback-free structural subset, preserve exact supported union alternatives, and refuse unsupported core/lifecycle/hash-sensitive forms before backend verification or runner invocation. Do not continue stripping arbitrary callbacks and approximate their semantics. A different equally small implementation is acceptable if its semantics are demonstrated sound. Reverting to unrestricted ordinary validation after a numeric guard reopens R1. Chain-only or set-only denial leaves another known failure.

Preserve actual M0 requirement/scenario transport, supported tuple/string-enum/UTC behavior, patterned string-key maps and scalar/model/nested alternatives. Plain string Literal schemas may retain original callbacks/defaults/JSON mode; do not add needless global refusals. For the guarded numeric subset, make the supported structural and lifecycle boundary explicit, including defaults, model reconstruction, references, dictionary keys and serialization. A refused schema receives attributable `GenerationSchemaUnsupportedError` and complete bounded publication/recovery with no backend/worker call. This is a disclosed provider capability limit, not weakened task semantics or rejection of a valid response after execution. No first feature has been drafted or frozen.

Write meaningful tests from accepted ordinary strict-JSON baselines. Cover both R3 examples, both set forms with equal/unequal models, nested cases, supported exact types, and refusal before execution. Retain all closed R1/R2 controls. Confirm M0 actual schemas and ordinary callback/default single execution where supported. No native, model load, download, dependency install, historical/candidate execution or remote compute is authorized in this slice. The coordinator's one native budget remains untouched.

Owned paths: `src/feature_rl/generation/**`, generation tests, `docs/interfaces-M2.md`, `docs/reports/M2-provider.md`, and new round5 owner evidence under `docs/evidence/M2/`. Root's `coordinator-*`, transfer evidence, briefs, plan and progress are excluded. Preserve prior receipts, unrelated Finder files and `.superpowers/`. Capture separate RED and final focused/full stdout/stderr, observed UTC/exit/command evidence, and mechanically generate final source/doc/log hashes. Commit only owned paths and return FINAL with product revision and exact evidence. This closes only a provider repair slice; independent scoped review, fresh coordinator full suite and one current-source native check remain before M3 production.
