# M7 agent execution and RL owner (two reviewed slices)

Read this first. Tasks 9-10, binding spec §§8,10-11,13-14,17-18. Use approved M0/M2/M3/M4/M6 actual APIs; base and active slice supplied on dispatch. No subagents.

Writable `src/feature_rl/agents/**`, `training/**`, dedicated agent/training integration tests, `docs/reports/M7*.md`, `docs/evidence/M7/**`, `docs/interfaces-M7.md`, training run configuration/runbook contributions. M0 owns dependency/shared schema changes, M6 CLI, M3 workers, M4 grading. Route fixes and schema requests through coordinator.

## Agent slice

Implement `AgentRunner.run(task_version, policy, limits) -> OperationResult` against qualified/released solver package. No production reference patch, canned answer or private-grader feedback. Fresh conversation/working state per episode, actual read/command/edit/public-test actions within M3 sandbox, source submission through M4. Authenticated Codex tool-free generation may be the explicitly selected nontrainable demo policy: trusted host retains auth, model returns structured actions, only M3 executes commands. Never copy credentials or host sockets into worker. Model output is data, never host shell code. If task is provisional, fail closed rather than bypass release gate.

Capture real per-turn prompts/contexts/actions/observations, seeds/model/policy/options, token usage, wall/resources and saved submission. If backend lacks exact tokenizer IDs/logprobs, use M0's honest nontraining representation and `training_eligible=false`; don't invent probabilities, tokens or weights. For actual training backend preserve sampled token IDs and behavior logprobs with exact per-turn contexts; mask prompt/tool/harness/padding, validate prefix/template and no silent compaction/truncation. Invalid serialization is invalid trajectory.

Limits stop action collection and grade saved artifact; it may still earn1 if complete. Candidate command/build/import/crash/limit outcomes remain valid counted trials. Proven infra failure has null measurement, at most two retries after initial and records lost work; retry same saved patch/cases, not a better policy sample. No hidden endpoint exposed live. Test malformed actions, limit completion, failed commands, forbidden paths, saved-artifact outage replay, fresh conversation/reset, missing logprobs and stale policy. Execute one real feature attempt only when released task exists; otherwise preserve exact qualification blocker and independent runner tests.

## Training slice

User confirmed no remote compute. Host M4 Mac has no CUDA; do not provision paid infrastructure or silently replace SkyRL/vLLM with another claimed training experiment. Complete independent implementation/verification and provide exact GPU unblock command. CPU tensor diagnostics are allowed only clearly labeled diagnostics, never feature-learning evidence.

Inspect actual published stable SkyRL/Harbor source and integrate exact API: preflight identifies SkyRL `skyrl-v0.3.0` f5bc3b78dfddfb352870d5d7430cd226e5785838 pinning Harbor3de07a0e01f3368921766437fc7afece3ddec23d (v0.13.1). This is proposed compatibility until an executable smoke passes. Do not assume current docs' changed imports/backends. Audit automatic artifact transfers/timeouts/group masking and use only allowlisted source transfer into second worker beneath external controller. Actual pinned framework import/execution gate remains distinct from source inspection.

Implement synchronous GRPO adapter with four fresh seeded episodes/task, fixed behavior-policy version, same private case seed within each group, fixed balanced sampler, one epoch, explicit clipping/KL/loss normalization, stale-worker checks and inference sync before next collection. Invalid infrastructure outcomes excluded with effective group size/lost cost reported; fewer than2 valid outcomes no update. Uniform rewards have zero task advantage and remain in costs/logs, not resampled without bound.

Feasibility probe max64 groups, require8 mixed groups spanning4 training tasks before larger experiment. No fabricated reward sequence counts as signal. Preserve true weight/optimizer/reference/config/task versions/data cursor/RNG/checkpoint identity and require nonzero gradients/changed tensors/save-reload/inference/resume evidence before successful training claim. A blocked command must name missing CUDA/checkpoint/qualified data/stack gate; no success-shaped checkpoint artifact.

Test mask/context alignment, stop classification, same-case grouping, invalid peers, uniform groups, stale policy, budgets, resume state and frozen data/partitions. Real installed CPU optimizer tests can verify mathematics/resume as diagnostic; actual feature GRPO is blocked until real tasks+compatible compute. Supply real executable integration code rather than shell scaffolding; if exact stack API cannot be verified, leave explicit unverified gate and do not claim it implemented/working.

Each slice gets full report/exact evidence/commit and independent review. Module remains partial until both implemented slices and all empirical gates are separately accounted for.
