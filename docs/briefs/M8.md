# M8 independent evaluation and audits owner

Read this first. Task 11, binding spec §§9,11-14,17-18. Use approved actual M0/M1/M4/M6/M7 APIs and models; base supplied on dispatch. No subagents.

Writable `src/feature_rl/evaluation/**`, `audits/**`, dedicated tests/experiment reports, `docs/reports/M8.md`, `docs/evidence/M8/**`, `docs/interfaces-M8.md`. Shared contract/config changes via M0; CLI via M6; grading/runner defects to owners. No cross-module edits.

Implement frozen independent evaluation using withheld repository families and feature lineages assigned before authoring. Detect forks/backports/copied code/descendants and overlap with external corpus; separate train/dev/locked-test retrieval and artifacts. Test-source frame and every adapter/exclusion must be recorded before filter. Final test cannot tune generator, sampling, checkpoint selection or training. Frozen model/config/roster/case sets validated before execution; after leakage freeze a new test set, don't rewrite old outcomes.

Implement actual `EvaluationService.evaluate(config) -> OperationResult` and `AuditService.audit(run_ids) -> OperationResult` using shared real runner/grader, not injected outcome constants. Offline aggregation of supplied observed receipts is allowed only explicitly labeled; no empirical claim without genuine evidence. All assigned trials accounted for, including failures and unresolved infra; same cases/task roster/inference limits across arms. Pass@1 is exactly one bounded episode, repeated seeds are paired trials rather than any-success-over-k. Report attempted/resolved/failed/invalid, all-assigned and valid-only rates, task-weighted and family-weighted paired differences, clustered uncertainty and limitations for insufficient families/training seeds.

Prepare/validate A starting model, B training-only verified SFT, C credible accessible external-task RL corpus, D factory-task RL; same initialization/harness/tools/optimizer/evaluation limits and declared development tuning. Inspect public primary corpus availability/license/overlap/adaptation requirements; archive concrete metadata only and don't invent a runnable external baseline. Separate matched training-resource comparison plus construction bill from matched total data+training budget; count actual incurred costs and external corpus adaptation/rejection funnel, not hypothetical sunk research costs. No CUDA/remote compute available: learned arms and empirical comparison remain blocked until real artifacts.

Audits: complete submitted patch + task/verifier version is unit, stratified random sampling by family/accepted status/failure category, known inclusion probabilities, weighted population estimates. Human adjudication with reproducible counterexamples; LLM cannot finally approve own checker. Distinguish invalid-among-accepted, accepted-among-invalid, rejected-among-valid with explicit denominators/uncertainty; mutation rejection is separate. Both accepted/rejected and rejected-source samples required. Empty/unadjudicated samples must not report zero error. Quarantine defective versions and trace affected rollouts/checkpoints; regrade saved artifacts and require unaffected restart or disclosed contamination after weight updates.

Tests: lineage leakage, changed frozen task/case/model, missing/duplicate trials, denominator manipulation, mismatched paired seeds/cases/budgets, pass@1 best-of-k confusion, all-invalid zero evidence, weighted sample bias, no audit adjudication, contaminated checkpoint tracing. Statistical functions should be checked against small hand-derived examples and seeded reproducibility, not tests merely mirroring implementation. Real evaluation requires independently authored qualified held-out tasks and actual policy outputs; absent dependencies are exact blocked gates, never synthetic success metrics.

Publish command/config/receipt interfaces and report what exists/ran/is empirically supported/is blocked. Help M6 owner wire only implemented entry points. Independent full review follows after module review; preserve raw results and study locks immutably.
