# M4 checkers, source submission and trusted grading

Current scope: read `docs/decisions/code-completion-scope.md`. Complete code and available non-GPU checks independently of successful feature generation or later human/GPU experiments. An actual first-feature path may retain a precise unresolved semantic/model gate. Clearly labeled diagnostic fixtures can verify the real submission/build/worker/controller path, but cannot be published as model-authored features or empirical task qualification.

Read this first. Scope Task 6 of `docs/implementation-plan.md`; binding spec §§6,8-10,17. Upstream exact APIs are M0 contracts/storage, M2 authoring/scenarios and M3 runtime, documented in their interface files. Read actual code after coordinator supplies approved base. No subagents.

Writable: `src/feature_rl/verifiers/**`, `submission/**`, `grading/**`, corresponding tests, `docs/reports/M4.md`, `docs/evidence/M4/**`, `docs/interfaces-M4.md`. Shared schemas/config go to M0 via coordinator; runtime defects go to M3. No product edits elsewhere.

Implement usable checker construction from actual contract/scenarios: generated worker adapter invokes real Click public interfaces, ordinary observations return to controller, expected relations and hidden case values remain controller-only. Model-produced executable code stays untrusted and only runs in M3. Controller comparison supports only explicitly declared observable semantics; reject unsupported rather than eval arbitrary expressions. Every assertion maps to disclosed requirement and expected oracle origin. Public tests are diagnostics; any preservation contributing to reward must have an externally controlled probe.

Implement validated source-only submission over declared contract roots. Reject traversal/absolute/ambiguous paths, duplicate entries, symlinks/hardlinks/device entries, decompression/size bombs, disallowed files/build/dependencies and attempts to replace controller code/results. Rebuild candidate from clean B using M3; never import candidate/build hooks/deserialization on host, never transfer installed packages/caches/agent runtime. H and private controls are only qualification-worker material.

Implement `GradingService.grade(task_version: ArtifactRef, submission: ArtifactRef, case_seed: int) -> OperationResult` with exact types integrated to M0. Freeze seed-derived complete case manifest; all mandated families always present. Enforce each expected probe exactly once with bounded output/schema/type/ID checks; missing/duplicate/skip/forged `passed` output cannot count as receipt. Binary reward one only for all required feature+preservation checks and no protocol violation. Distinguish candidate build/import/crash/limit failures (valid zero) from proven supervisor/platform failure (invalid measurement), with actual logs and preserved same submission/seed for outage retry.

First feature: execute actual B/H against same meaningful probes. B healthy regressions + diagnosed missing behavior; H all contract cases; generated private variations beyond public example. Report discrepancies rather than silently alter contract/checker. Do not make H output the implicit specification. Additional constraints found in H return to M2 and invalidate dependent evidence.

Tests and real route controls: empty/missing probes, duplicate IDs, forged reward file, early exit, malformed/excessive JSON/stdout, crash, nonterminating process, hard-coded known inputs, dependency shadowing, source/archive path/link tricks, and state retained between attempts. These must use real submission→rebuild→worker→controller path to count as adversarial gate evidence. Unit doubles alone don't count. Real failures can be investigated and repaired under explicit evidence; don't hide them.

Expose exact immutable grade receipt/submission/comparison API in docs/interfaces-M4.md, consumed by M5/M6/M7/M8. Capture actual commands/exit/results/seeds/adapter and task versions/costs in docs/evidence/M4. Commit complete tested scope and report unresolved limitations; independent security/spec/quality review follows. Same owner fixes findings.

Active independent core handoff (after M2's three failed contract calls): implement submission, the closed comparison/case language and real controller grading over reviewed M0/M3 interfaces first. M2's resolver/services are under re-review; integrate checker generation after that handoff. Test-only frozen diagnostic artifacts may exercise the real B submission/build/worker/controller route, with no claim of model-authored construction or qualification. No H/private-history reads or model calls in this initial core slice. This is disjoint complete core behavior, not a placeholder factory.

Mechanical grading may evaluate a frozen BUILT root for M5 controls. It does not confer admission or training eligibility. A completed grading measurement may be reward zero or one; the actual M4 API represents a measured zero with candidate_rejection and a pass with success, while infrastructure/invalid outcomes remain null. M5/M6 own authenticated Tn→Q→T0 and legal transition/quarantine checks; M7 must invoke those before consuming released tasks. M4 must bind its receipt to exact task/submission/verifier/cases/seed, without inventing a future admission callback or treating a released state string as authentication.
