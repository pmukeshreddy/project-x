# M2 ownership transfer — unfinished provider fix round3

Read this first, then `docs/briefs/M2-provider.md` for binding requirements. The user requires one accountable owner per module. Two consecutive service-capacity failures, including a same-owner recovery followup, prevent `/root/m2_authoring` from completing its turn. `/root/m2_authoring_recovery` receives exclusive M2 ownership, including later requirements/scenarios and integration fixes. The former owner must not edit unless ownership is explicitly transferred back.

The assignment base remains `b516c63799539c00ba711c07254de885a4a4310d`; captured coordinator HEAD was `4b9fd63`. Last committed M2 product is `3f859ca991c8965fda48e9d95588e9be12b0fdab`. The retained seven-file uncommitted diff and source hashes are in `docs/evidence/M2/owner-transfer-round3.diff` and `.json`. Preserve and continue those edits, inspecting them before changing anything. They already implement a proposed pinned-core-schema Literal guard, bounded local IDs, boundary revalidation, tests and report/interface edits. This is not a restart or a fourth engineering fix round.

Read the actual generation code, `docs/interfaces-M2.md`, `docs/reports/M2-provider.md`, `docs/reviews/M2-provider-round2.md`, and prior `provider-v3-fixround2-verification.json`. Two P2 findings remain unreviewed: exact raw Literal types must agree with actual union and patterned-mapping validation, and request/response/prompt identities must remain bounded through all rejection and replay paths, including forged Pydantic copies/construction. Correct values and supported tuples/enums/UTC transport must continue to work. Decoder mutation tests need genuine valid baselines before a single-field mutation, not AUTO hashes that already fail identity checks. The partial report's round3 receipt does not exist yet; finish verification and write it before asserting success.

Use the same owned paths as the provider brief. The coordinator owns `coordinator-*` and `owner-transfer-*` evidence, briefs, progress and plan; do not edit them. Preserve all earlier raw failed diagnostics and receipts. Unrelated untracked Finder `.DS_Store` files must be excluded from commits. No subagents, cross-module edits, downloads or native inference. The owner inference budget for this slice is exhausted; only a separately declared coordinator check follows independent review. Test doubles are confined to tests. No backend fallback, weakened check, fabricated cost or claimed native result.

Finish the narrow fixes, run focused and affected full tests, self-review, preserve exact UTC command/exit/raw-output/source/test identities in a new round3 receipt, commit explicit owned paths and return FINAL with status, product commit, test summary and any concerns. Independent reviewer `/root/review_m2_provider` will review the complete fix from the original assigned base. That review and a current-version native coordinator check are still required before M3 product dispatch. M2 as a whole remains partial until runtime-backed authoring/scenarios are implemented and reviewed.
