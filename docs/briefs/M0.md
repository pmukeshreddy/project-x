# M0 accountable implementation owner

Read this first: these are your scoped requirements. Implement Task 1 from `.superpowers/sdd/implementation-plan/task-1-brief.md`; global constraints in `docs/implementation-plan.md` and original spec §§3,6,10,11-13,17 bind this work. Project goal: actual feature-to-environment factory, real isolated grading and RL/evaluation, starting with Click PR 3228. No stubs or success defaults.

Base: `767e790`. Work in `/Users/mukeshreddypochamreddy/Desktop/project x`, branch `implementation/feature-rl`. You alone own M0 for this project, including future interface/dependency fixes. Do not spawn agents.

Writable: `src/feature_rl/contracts/**`, `src/feature_rl/artifacts/**`, package root `__init__.py`, `pyproject.toml`, lockfiles, `.gitignore`, shared test configuration, M0-only tests, `docs/interfaces.md`, `docs/reports/M0.md`, `docs/evidence/M0/**`. Do not alter input specs/plan/progress or other modules. Git commits stage only your files.

Implement strict Pydantic artifacts with real required typed nested fields for all eleven specification artifacts. Establish enums for visibility/actor/partition/dispositions/stops/state and explicit evidence/cost/resource/seed/provenance/review records. No loose all-purpose dict payloads replacing key schemas. Require evidence for successful operations and explicit null/unknown costs rather than claiming unmeasured free cost. Unit-test doubles stay in tests.

Canonical JSON: UTF-8, sorted keys, compact separators, no NaN/Infinity; reject unknown fields and ambiguous/coerced types. Immutable references bind SHA-256, artifact kind/version/visibility; validate all on read. `ArtifactStore(root: Path, role: ActorRole)` exposes `put_artifact(artifact) -> ArtifactRef`, `get_artifact(ref) -> ArtifactModel`, `put_bytes(data: bytes, kind: str, visibility: Visibility) -> ArtifactRef`, `get_bytes(ref) -> bytes`. Define exact roles and permitted visibility, including authoring/solver versus controller. Content-addressed bytes and typed JSON must coexist without unsafe deserialization. Atomic durable writes, idempotent duplicates, concurrent collision safety, corruption and symlink/path defenses.

Define explicit shared request/config models for operation signatures in plan (PolicyConfig, ResourceLimits, TrainingConfig, EvaluationConfig, OperationResult). Keep schema extension disciplined; if uncertain, prefer typed nonaccepted/provisional status rather than fields with permissive success defaults. A HumanReview record needs actor type/human identity/subject digest/evidence, and must reject model signing. Authentication attestation is later external gate, not solved by a string alone.

Acceptance tests: actual artifact round trips, strict invalid inputs including bool-as-int and nonfinite values, missing successful evidence, required requirement links, path/ref mismatch and tampering, deterministic identity, duplicate/concurrent writes, interrupted write behavior, visibility denial and private/public confusion. Write/run meaningful failing tests first; pin dependency versions after querying actual compatibility. System Python is 3.13.7, uv available. Prefer project `.venv` via uv; don't execute any historical repository code/build on host.

Publish actual API/field examples in docs/interfaces.md to unblock M1. Preserve test commands/output/exit/revision in docs/evidence/M0 and comprehensive report docs/reports/M0.md. Report implementation and execution separately. Commit tested coherent changes; final return only status, commit, focused test count, concerns and report path. Independent reviewer will follow, and you own fixes.
