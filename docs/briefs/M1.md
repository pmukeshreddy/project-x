# M1 accountable intake/history/split owner

Read this first. Implement Task 2 from `docs/implementation-plan.md`, global constraints there and original `feature_rl_pipeline.md` §§4-5,10,12,17. Follow execution prompt ownership rules. Base revision will be supplied on dispatch. No subagents.

Writable product paths: `src/feature_rl/intake/**`, `src/feature_rl/history/**`, `src/feature_rl/splits/**`; corresponding dedicated tests; `docs/reports/M1.md`, `docs/evidence/M1/**`, `docs/interfaces-M1.md`. Do not modify shared M0 contracts/config or other modules. Propose needed schema changes to coordinator. Commit only owned paths. Large/private source data belongs under ignored `.feature-rl/`, not solver-visible docs or Git.

Upstream: read approved actual `docs/interfaces.md` and contracts/storage implementations. Use their exact ArtifactRef/visibility/evidence/cost/source types. The coordinator will not accept duplicated shadow models for upstream fields.

Deliver real intake of https://github.com/pallets/click/pull/3228 and linked request #3107 through implemented code: archive HTTP response bodies with URLs/retrieval timestamps, metadata/commits/comments, available edit history status, license and CI snapshots. Source fetches use bounds/timeouts and fail explicitly. Labels are hints; distinguish discovery from acceptance. Capability matrix already published at `docs/capabilities.md`.

Before generation, assign the candidate and linked requests to train with family/lineage identity. Implement deterministic relation closure for forks, backports, copied code/monorepo/descendants and reject conflicting partition assignments; preserve recorded proof rather than assume repository names imply independence. RQ's Click dependency is a declared relation requiring review, not automatic test independence.

Recover B immediately before integration and complete H from actual Git ancestry and archived metadata. Normal merges must use integration first parent; squash must bind source diff/integration proof; rebase must require a verified contiguous integration span and reject ambiguity/interleaving. Never use today's PR base/head directly as B/H. Archive B/H with content digests and remove future history from B-only export. Use Git as data tooling only: never import historical code, execute repository scripts/build hooks, install its dependencies or run tests on host. Fetch bounded repository history, avoiding unrelated broad crawling.

Classify changed files into implementation, docs, tests/fixtures, dependency/build, unrelated; mixed-purpose cases require recorded manual/code review and cannot claim automatically resolved. Archive current edited request honestly: historical request needs defensible cutoff/edit evidence; if unavailable use reconstructed-specification provenance with explicit explanation. Contract authoring view contains only admissible evidence and B; keep H/diff out of that view. Do not author requirement contract here.

Meaningful acceptance tests: local Git fixtures with real commits for merge/squash/rebase, ambiguous and missing graph; edited text cutoff; archive corruption; forks/backports/shared-descendant split conflicts and idempotency. Real network/Click reconstruction evidence must be separate from synthetic test fixtures. Record commands, exit status, full B/H commit IDs/digests, changed classifications and costs (unknown cost labeled unknown), source locations and any unresolved classification/history gate.

Write exact downstream API and a runnable Python command example in docs/interfaces-M1.md. Full report docs/reports/M1.md, exact logs docs/evidence/M1. Return commit/test summary/report and unresolved gates. Independent reviewer follows; same owner handles fixes.
