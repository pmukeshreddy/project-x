# M2 provider slice — same owner retains authoring/scenarios

Read this first. Scope: Task 3 of `docs/implementation-plan.md`; spec §§5-6,6.4,10,13. Read approved actual `docs/interfaces.md`, `docs/interfaces-M1.md`, and upstream implementation before coding. The coordinator supplies base revision on dispatch. This is a partial module; completion of this slice is not completion of M2.

Writable: `src/feature_rl/generation/**`, corresponding tests, `docs/reports/M2-provider.md`, `docs/evidence/M2/**`, `docs/interfaces-M2.md`. You remain owner for `requirements/` and `scenarios/` in later slice. No subagents or cross-module changes; route shared schema/dependency requests through coordinator/M0.

Implement an actual explicitly configured generation backend with immutable prompt/model/options identity, per-call request/response archives, retrieval trace, usage/cost (unknown price remains unknown), status and deadlines/output bounds. No canned requirements, fake backend or silent fallback. Parse model output using strict M0 schemas or explicitly defined proposal schemas; reject malformed/extra fields, unsupported requirements, missing provenance, truncation and unexpected tools.

Available provider: authenticated `codex-cli 0.154.0` via existing ChatGPT login; no API keys configured. User authorized available configured services and no new paid infrastructure/API spend. Use existing subscription only if safely restricted; do not copy credentials into candidate workers. Preflight parser-verified flags in `docs/evidence/preflight.md`: ephemeral, read-only, ignore-user-config, ignore-rules, disable shell_tool/apps/hooks/multi_agent, web_search disabled. Verify additional inherited MCP/skills/tool access is actually disabled, not just instructed away. A configured CLI model backend is an explicit authoring selection, not the trainable learner. Check local help/schema and official OpenAI documentation for any uncertain options. Never print tokens/auth files.

Generation sees only admissible request text and B context. Stage inputs are explicit data allowlists and immutable source spans; never expose H/diff/private checks/authoring conversation to learner. Create a fresh sanitized empty nonproject directory for provider calls, with structured input over stdin. Never repurpose HOME/CODEX_HOME. Read the corrected preflight: actual-home loopback capture needed `--strict-config`, all tool-feature disables, plus `--disable default_mode_request_user_input` and `-c tools.experimental_request_user_input.enabled=false` to reach zero exposed tool definitions. The earlier empty-home probe is not evidence for production. Reject tool events or protocol drift. If no route can enforce the boundary, record exact blocked provider gate and finish independent validation code; do not use host shell agent browsing as a substitute.

Tests before implementation: request/response actual serialization, malformed output, truncated JSON, unknown requirements/provenance, forbidden reference source, deadline/output limits, provider exit failure, invalid usage and unsupported/missing configured model. Doubles confined to tests. Run a real small structured-output smoke with explicit pinned available model and bounded wall/output budget if the safe route is available. Archive exact request/options/backend version/model, complete event evidence, final data, usage and unknown billed cost; no invented model version.

Expose concrete provider protocol and retrieval/context interfaces for M3/M2 second slice. Full report/evidence and commit. Independent reviewer follows; same owner fixes findings.
