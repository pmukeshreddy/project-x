# M2 provider slice — same owner retains authoring/scenarios

Read this first. Scope: Task 3 of `docs/implementation-plan.md`; spec §§5-6,6.4,10,13. Read approved actual `docs/interfaces.md`, `docs/interfaces-M1.md`, and upstream implementation before coding. The coordinator supplies base revision on dispatch. This is a partial module; completion of this slice is not completion of M2.

Writable: `src/feature_rl/generation/**`, corresponding tests, `docs/reports/M2-provider.md`, `docs/evidence/M2/**`, `docs/interfaces-M2.md`. You remain owner for `requirements/` and `scenarios/` in later slice. No subagents or cross-module changes; route shared schema/dependency requests through coordinator/M0.

Implement an actual explicitly configured generation backend with immutable prompt/model/options identity, per-call request/response archives, retrieval trace, usage/cost (unknown price remains unknown), status and deadlines/output bounds. No canned requirements, fake backend or silent fallback. Parse model output using strict M0 schemas or explicitly defined proposal schemas; reject malformed/extra fields, unsupported requirements, missing provenance, truncation and unexpected tools.

Selected authoring backend: local MLX/MLX-LM with `mlx-community/Qwen3-4B-Instruct-2507-4bit`, model revision `50d427756c6b1b2fe0c0a10f67fbda1fc8e82c1b`. This is a disclosed selection before any contract or experiment freeze. The earlier authenticated Codex CLI path failed hard-token qualification: it emitted an over-budget complete response before reporting exhaustion. Preserve `docs/evidence/M2/rollout-budget-qualification.md`; do not substitute retrospective counters for hard limits or silently switch backends. Local metadata pins are MLX/MLX-Metal0.32.2 and MLX-LM0.31.3; actual qualification receipts and the M0-reviewed production dependency handoff must precede implementation dispatch. Model weights/tokenizer/config hashes, license, runtime source pins and full wheel closure are in `.feature-rl/research/M2` and the local-MLX evidence report. Do not load remote code or acquire packages during inference.

Generation sees only admissible request text and B context. Stage inputs are explicit data allowlists and immutable source spans; never expose H/diff/private checks/authoring conversation to learner. Use fresh bounded inference processes with structured input over stdin and no model tool execution, remote downloads, inherited task conversations or shared mutable prompt caches. Verify the exact local model/tokenizer bytes before use, constrain supported model architecture, and reject missing or changed files. Count the actual fully templated input tokens before inference and enforce positive generation limits in the verified native loop. Record sampled token IDs, output text, stop reason and actual usage; truncation cannot become a valid structured response. Enforce wall/output/memory bounds externally as well as any tested MLX allocator limit, and prove no descendant survives cleanup. Never repurpose HOME/CODEX_HOME or change unrelated user/global settings. Historical or generated repository code still executes only through M3, never in the inference host. If the boundary cannot be enforced, preserve the exact blocked gate and finish independent validation code.

Qualified local memory policy:3.5GiB MLX allocator/wired guideline, cache0,4GiB sampled physical-footprint kill threshold at20ms and1GiB guard band. Preserve its limitation: the watchdog terminates after observation and MLX's setting is a guideline, so neither establishes a mathematically strict zero-transient5GiB allocator ceiling. Reject watchdog observation failures; record peak samples, kernel lifetime peak where available, termination cause and cleanup. This trusted inference policy does not relax M3's untrusted-worker isolation requirements.

Tests before implementation: request/response actual serialization, malformed output, truncated JSON, unknown requirements/provenance, forbidden reference source, pre-inference input rejection, exact generation caps, deadline/output/memory limits, provider exit failure, invalid usage and unsupported/missing or tampered configured model. Doubles confined to tests. Run a real small structured-output smoke through the implemented provider with explicit pinned model and bounded resources; a prior qualification harness is not evidence that production code works. Archive exact request/options/backend/model/tokenizer identities, complete event evidence, final data, usage and honest cost measurements. Local compute is not CUDA feature-RL training, and no learning claim follows from this provider slice.

Expose concrete provider protocol and retrieval/context interfaces for M3/M2 second slice. Full report/evidence and commit. Independent reviewer follows; same owner fixes findings.
