# Independent M3 production review

Read `docs/briefs/M3.md`, `docs/decisions/M3-development-and-failure-semantics.md`, specification §§7–8,10,17, the final owner report/interface document, and the complete owned product/test diff and execution evidence supplied on dispatch. The implementation was assigned base `8f97f83b4d59db73c43f1a7a0507d35f5b1edaeb`; unrelated coordinator/M2 preparation commits followed. Review actual M3-owned files at the final supplied target and use recorded file hashes to bind uncommitted-source runs. Earlier M3 research review approved preparation only.

You are independent of the implementation owner. No product/test edits or subagents. Write only `docs/reviews/M3-production-round1.md` and, if justified by an unresolved concern, scoped diagnostic evidence under `docs/evidence/M3/review-round1/**`. Do not read task H semantics or author a feature contract/checker. No model, native provider gate, downloads, paid/remote compute, broad container changes, or repeated complete runtime experiments without an actual unanswered review question. Read and audit existing real execution evidence first. Any additional application/control execution requires the actual M3 Docker boundary, an exact bounded owned attempt and verified cleanup; no host imports or build hooks.

Require separate specification-compliance and implementation-quality verdicts. Include concrete file/line evidence and reproducible cases for substantive findings, distinguish missing required evidence from speculative hardening, and identify all unresolved P0/P1/P2 issues. Review these connected boundaries in particular:

- Bounded artifact reads, tar/zip expansion and file-count/aggregate limits, path/link/history/cache policy and canonical source identities. Controller source checks must not trust a worker's claimed hashes or import path. Test long-path serialization, forged in-memory values and allowed-change joins where relevant.
- Exact image/base/package/dependency/policy binding, immutable source-free shared image, offline builds and actual imported package observations. The original B regression failure remains failed; the recorded `less` repair is environment1/2 and candidate1/4, with unchanged assertions and consistent B/H/candidate/development application. Distinguish the actual local manifest RepoDigest from config and inspect IDs; no M0 image-schema widening was implemented.
- Effective sandbox settings and machine-checked denials before historical source execution. CPU quota is separate from cumulative CPU budget; input, output, command, export and cleanup have whole-path bounds. No default host credential/config, daemon socket or private artifacts reach workers. State/resource support must match the disclosed no-service scope.
- Persisted exact ownership before create, specific verified absence after termination, and recovery across interrupted orchestration. Daemon failure is not absence; client EOF/exit is not container/process termination. Cleanup failure must remain visible and cannot yield a successful normal receipt.
- Explicit last-confirmed source semantics with timestamp/version, bounded non-atomic capture interval, and preservation on ordinary limits or invalid export. Candidate-caused source/build/command failures are distinguishable from proven platform/monitor/storage/cleanup faults with evidence, without fabricating rewards.
- Development commands can inspect/repair unbuildable source and see edit-then-test current source. Fresh candidate grading uses a separately validated rebuild and cannot import solver-installed packages/caches or stale wheels. Unrelated mutable process/dependency state is not silently promised to persist.
- Actual complete B-health result and B/H build/import source bindings, fresh/reset-after-interruption evidence and owned cleanup. Native baseline skips/deselections must be explained against unchanged historical configuration; test counts alone do not prove reward-bearing regressions. Runtime checks do not establish feature absence, contract correctness or qualification, which remain M2/M4/M5 gates.
- Usable typed APIs, strict joined validation, attributable costs/errors, maintainability and tests of actual failure risks. Unit doubles are labelled and do not substitute for real lifecycle evidence. Local arm64 recipe support does not imply Linux CUDA training or other-platform/service support.

Report the tested/reviewed source identities, exact commands actually run, results and limitations. Do not accept owner summaries as proof. Same M3 owner receives all implementation fixes through the coordinator. Return FINAL with both verdicts, concise findings and the written review/evidence paths; the coordinator commits them. Follow `docs/briefs/review-policy.md`: no index or commit mutations by the reviewer.
