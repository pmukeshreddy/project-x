# M3 fix round 1: atomic workspace terminal transitions

Same accountable M3 owner. Product base `a9fec98ce2fec8cd8bc30c245a1100156532e847`; coordinator-only commits followed. Read `docs/reviews/M3-production-round1.md` first and its exact `review-round1/close_race.py` / `close-race-result.json` reproducer. Both independent verdicts fail for one P2; no P0/P1 identified. The root also verified 106 source/test/log/CAS bindings, which does not close the defect.

Confirmed behavior: close reads source v0/generation0 before the lock; one real Docker development command then saves v1/generation1; close resumes and overwrites durable state with v0/generation0 and returns it as the submission. No serial ordering permits both successes. Reset has the same read-before-lock pattern and must obey the same protected transition/generation invariant.

Fix the read/check/write transition under the existing operation lock, or revalidate/reload under that lock before committing. Close must preserve the latest confirmed source if it follows a successful save, or explicitly reject the overlapping operation. Reset must not reuse a stale generation that could admit an older execution. Preserve strict admission, cleanup recovery, durable source publication and the explicit last-confirmed policy. Do not silently serialize away a test synchronization point without checking the actual state/outcome, change source semantics, weaken assertions or add a broad new lifecycle architecture.

Writable only necessary `src/feature_rl/environments/**`, corresponding owned tests, `docs/interfaces-M3.md`, `docs/reports/M3-fix-round1.md`, and new `docs/evidence/M3/fix-round1/**`. Prior product/report/review/diagnostic evidence is immutable. No M0/M1/M2/other product edits or subagents. You are the sole product writer for this slice.

Add meaningful regression tests for both terminal transitions and generation/admission behavior under the demonstrated schedule. Run a bounded real synthetic Docker diagnostic confirming fixed outcomes and exact cleanup. Reuse the existing trusted pinned image/dependencies; no model/provider/native calls, downloads, remote compute or historical B/H reruns for this state-only fix. Run the focused M3 suite and any directly affected integration checks; root will run the fresh full suite after independent re-review, avoiding duplicate broad reruns. Retain actual RED/GREEN commands, UTC, outputs, source/test bindings and resource/cleanup receipts. This is an implementation fix, not another environment repair; the actual Click counters remain environment1/2, candidate1/4.

Commit only owned fix paths and return FINAL with target, test summary and any unresolved concern. Same independent reviewer will inspect the changed scope and regression evidence; root then performs current integrated verification before the M2 handoff.
