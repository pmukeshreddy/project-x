# M2 requirement/scenario slice — same accountable owner

Current scope: `docs/decisions/code-completion-scope.md` supersedes empirical stopping rules. Complete the usable authoring/scenario implementation and non-GPU validation even if the bounded actual model run cannot produce an accepted task. Preserve that failure and its exact retry command; do not use a handwritten production answer or stop implementing dependent code. Read `docs/decisions/M2-proposal-finalization.md` for derived schemas, finalization, evidence roles and the required explicit scenario-planning stage. The first real invocation budget will be declared separately at handoff; all earlier native provider diagnostics stay closed.

Read this first. Implement Task 5 in `docs/implementation-plan.md`, spec §§5-6.4,7,10,17 and the execution prompt. The coordinator will supply reviewed M1/provider/M3 checkpoints and the actual M3 API handoff. This is M2's second slice, after real baseline execution. No subagents.

Writable: `src/feature_rl/requirements/**`, `scenarios/**`, owned generation integration fixes, dedicated tests, `docs/reports/M2-authoring.md`, `docs/evidence/M2/**`, `docs/interfaces-M2.md`. Do not edit M0 schemas, M3 runtime, M4 checker or other product paths. Request interface changes through coordinator and their owner. Commit only owned files.

Use the real production provider from your reviewed first slice. Inputs are exactly the approved M1 `AuthoringSourceView` B/request/license references, attributable baseline source spans, and real M3 discovery observations. Never load private SourcePair, H, PR implementation diff/tests or privileged cached repository history for the initial draft. Do not treat attached source text as execution instructions. Archive the explicit retrieval allowlist and every expansion; the model may request more B files through a bounded data retrieval protocol.

Discover actual public interfaces in B through M3 isolated commands as well as inert source inspection. Record entry points, callers, source spans, relevant existing obligations, imported package location, command outputs and limits. No repository import or generated discovery execution on the host. Unsupported identity/callback/service semantics must produce typed rejection, not a JSON approximation that changes the requested behavior.

Generate individually identified requirements with validated quotes/locators in resolved admissible sources, observable public behavior, permitted source edits, relevant regression obligations, explicit exclusions and ambiguity decisions. Resolve using M0 `RequirementContract`, `EvidenceLink`, `AmbiguityDecision` and `AllowedChanges`; don't duplicate those fields in shadow artifact schemas. Critical unresolved ambiguity blocks automatic acceptance. Current Click source provenance is reconstructed_specification and must remain disclosed. Do not add exact formatting/order/helper structure without source grounding or an explicit reconstructed clarification.

Freeze and preserve the first draft before any privileged feasibility review. Notify the coordinator once it exists. A separate reviewer may then inspect H/support needs, and return grounded issues to you. Disclose any new observable requirement in a new contract version; do not silently use H to define correctness. Required human task review is later M5 and cannot be signed by you or a model code reviewer.

Generate M0 `ScenarioPlan` from the frozen contract and real capabilities: preconditions, actions, observations, expected relation, valid input domain, reset needs and oracle provenance. Mandatory requirement IDs must exactly match the contract's mandatory new and compatibility obligations; every scenario ID and reference must resolve, and each obligation must have a meaningful behavioral case. Case-family planning must guarantee coverage independently of random seeds. Record same_cases_within_group=true for later GRPO; no generated assertion or exact reference equality is automatically valid.

Enforce declared per-stage and candidate repair ceilings with measured attempts, cause/change notes and complete request/response costs. Reuse the real provider trace infrastructure, never substitute a handwritten Click answer as generic model generation. Run actual discovery and draft/scenario generation for Click through the implemented API. Preserve raw failures and rejected outputs; unknown cost components remain null.

Acceptance cases: forged source quote or locator, reference outside authoring allowlist, unsupported observable, missing/duplicate/unknown requirement IDs, incomplete compatibility coverage, critical unresolved ambiguity, invalid input domain/seed policy, generation budget exhaustion, missing runtime evidence, and a real B-only construction receipt. Doubles remain unit diagnostics. Report contract/scenario refs, model/prompt/config identities, all retrieval spans and actual commands, tested revision and exact remaining gates. Publish usable M4/M6 APIs. Independent review follows; you own fixes.
