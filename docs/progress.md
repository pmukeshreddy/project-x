# Feature-RL implementation progress

Read this first after a context reset. Both original attachments were fully read and remain unchanged: `feature_rl_pipeline.md` is the architectural/execution specification; `codex_multi_agent_implementation_prompt.md` supplies the module ownership/review instructions. This record consolidates progress; detailed evidence stays in the linked reports and Git, not duplicate packages.

## Scope and immediate status

Complete every module, actual service/CLI integration and available CPU/Docker verification. GPU training, real task collection and experimental results are deferred and **unverified**, not code blockers. Continue autonomously until code/integration is finished. **No new task-generation model calls in this phase.** Keep existing owners, one writer per module, independent review of changed code and affected interfaces. Do not reopen unchanged components without new evidence or rerun cached Docker matrices.

**The requested first integrated TEST fixture passed:** actual M3/M4/M5/M6 execution, baseline feature failure, correct implementation success, deliberate omission failure and exact reset restoration. Qualification remains provisional with no fabricated human approval. This proves integration, not successful real task generation. Remaining code is actively being completed below.

| Module | Accountable owner | Status / next deliverable |
| --- | --- | --- |
| M0 | `/root/m0_contracts` | Shared contracts/CAS/config/bounded reads reviewed. Needed next: optional `RunRequest.case_seed` and console entrypoint after actual CLI exists; only this owner changes shared schemas. |
| M1 | `/root/m1_sources` | Intake/history/split implementation reviewed; actual Click intake retained. |
| M2 | `/root/m2_authoring_recovery` | Original authoring/recovery6568a11 PASS round5. New control/alternative stages2941264 PASS round6,105 focused passes. Failed real generation retained; no inference retry. |
| M3 | `/root/m3_runtime` | Runtime2190af0 reviewed PASS; real Docker build/health/reset evidence retained. |
| M4 | `/root/m4_grading` | Mechanical core430df6f PASS. New checker/control/alternative services4086ee6 independently PASS;84 owner checks,28 independent focused checks. Ready for M6 consumption. |
| M5 | `/root/m5_qualification` | Complete qualification/acceptance3cc9f11 plus fixes e3d868a independently PASS round2.72 owner checks,13 independent fix checks. Actual external human gate enforced. |
| M6 | `/root/m6_factory` | Registry/package/fixture reviewed. TaskLifecycle8efdc89 independently PASS,18 focused checks. **ACTIVE:** full Factory construction/accounting/history, concrete per-task M5 service resolver, recovery and CLI. |
| M7 | `/root/m7_learning` | Training core17e43f4 independently PASS round2,38 CPU checks. **ACTIVE:** actual AgentRunner, native inference/training services, SFT/GRPO orchestration/checkpoint/resume and commands. |
| M8 | `/root/m8_evaluation` | Independent core35d3fcf has13 focused passes; coordinator review found6 confirmed defects. **ACTIVE:** audit fixes/source-audit path/durability, then actual EvaluationService on M7 runner. |

Current active slots: root coordinator plus M6/M7/M8 owners. No child agents. Root independently reviewed the new M4 authoring/M6 lifecycle and M8 core while owners implement disjoint code. Existing M2/M5 reviewers closed their deltas. Same owner fixes each module. Old `/root/m2_authoring` transferred ownership; never reactivate it as writer.

Latest review notes: `docs/reviews/M2-authoring-round6.md`, `M4-authoring-round1.md`, `M5-qualification-round2.md`, `M6-lifecycle-round1.md`, `M8-independent-core-round1.md`. M8 findings: incorrect false-acceptance/rejection denominators; rejected-source candidates forced into rollout-only audit; unauthenticated population frame/subset estimates; infrastructure outcomes incorrectly quarantining verifiers; best-of-k silently reported as pooled pass@1; policy seeds confused with training variability. Five equal-weight patch observations independently reproduced wrong rates `1/3,null,1/2` instead of `1/3,1/2,1/3`. M8 owner has the precise review and is implementing focused fixes. No empirical audit/evaluation claim follows from diagnostics.

## Integration evidence to reuse

`docs/reports/M6-click-fixture.md` and single `docs/evidence/M6/fixture-runtime/receipt.json` retain commit `da26580abe88987c3a139b78e75f85712a3aa201` execution. TEST helper `click.m5_test_feature.transform(text)` returns `"fixture:" + text`; F1 checks that feature and C1 preserves ordinary Click echo. Real cached Click B and six pinned wheels; fresh candidate builds/workers and existing qualified image.

- Four actual runtime grades in82.45s: B0/F1, correct source1, omit-prefix0/F1, post-reset B0/F1. All12 cases completed; C1 always passed. Saved mutation matched H and reset restored complete original B with a new generation; cleanup confirmed.
- Actual qualification in195.19s: baseline0, three fresh reference1, omission0, same-author diagnostic alternate1 and three timeout/reset reference1. Nine distinct grading bindings,27 completed cases, three real reset ledgers. Total13 real grades/39 cases across both phases. Captured code hashes unchanged.
- Completed qualification replay returned identical result without new events/workers. Q/P is provisional: missing human approval, independent alternative, complete controls/attacks and verified global repair history. Unknown repair count remains null. No review request, admission or released task was fabricated.
- T0 `259b16c9faf122474ecd548e342987098aa4ba5ddd7c217c6fd33986f109b875`; solver archive raw SHA `4b7b3668eee89be12c9aa51901cec046f28f4d1db67a0a0e4955ddac61765729`; provisional Q/P `991f39ef749df88a5c6c2c1718b8b5514dc56c60ee94f128267d7691e07dcaff`. M5 execution bound b343b8c, before later admission fixes.

Stable broader CPU milestone at exact detached da26580: **517 passed in35.86s**, exit0, tracked sources unchanged. `docs/evidence/integration/fixture-milestone/{receipt.json,pytest.log}`; retained worktree `.feature-rl/research/integration/milestone-da26580`. Explicitly excluded two Docker suites because the real fixture above provides the new Docker integration evidence. This is not verification of later changes. Run the next broader suite once at the next stable service integration milestone.

Earlier stable upstream evidence is closed: M0/M1 integration155tests, M3 integration342tests89.73s; M4 core58focused and26 real diagnostic grades across five contexts; M6 Registry36focused and packaging54focused/process-crash checks plus19 inventory-cap checks. See module reports/reviews. No repeated historical matrix. Owner live-tree M8 broad625pass/7optional skips was non-frozen and is not primary integration evidence.

## Agreed actual interfaces

Read `docs/interfaces-M2.md` through `docs/interfaces-M8.md` as needed; do not invent absent APIs.

M4: `SubmissionService.create/from_saved`, `GradingService.grade(task,submission,seed)->OperationResult`, `read_grade`. Success1, measured candidate rejection0, invalid/infrastructure null; source-inspection candidate0 is legitimate but not worker evidence. Mandatory families every seed via `m4-sha256-v1`. Only adapter/current input reach workers; private comparison stays on controller. BUILT grading grants no admission.

New M4 `CheckerAuthoringService` returns real VerifierBundle; `ControlAuthoringService` returns actual source-only `m4-submission`/ControlPatch and `m4-control-record`; `CheckerFinalizer.attach_controls` produces a new verifier preserving cases/adapter. Both use real M2 provider request/schema/archive/cost/recovery with diagnostic backend only in tests. Alternatives consume exactly B+visible contract+admitted public checks; negative reference excerpts bind exact supplied B/H SourcePair. Control record binds contexts, inputs, provenance, raw provider archive closure and explicit `qualification='unverified'`. M5 determines semantic validity/independence. No expected-valid flag grants approval. M2 stages add no repair allowance.

M5: concrete `QualificationService(store,registry,grader,builder,revision,policy,attestation_verifier)` implements `qualify(T0)`, `accept(request,attestation)`, `verify_accepted(T0_or_Tn,Q)`, recovery/publication and tracing. Complete H projection accounts all changed paths. Qualification requires real baseline/reference/control/reset evidence, all mandatory omission controls and nine attacks, independently authored alternative and exact global history. Targeted semantic omissions require normal adapter completion; syntax/import process crashes cannot substitute. Actual isolated human verification uses root-administered protected trust configuration and enrolled/pinned native SSH binary; no signing/enrollment writer. Current revocation/quarantine checked on every accepted consumer. New v2 records explicitly register/check consumed source-delta and detached payload/signature leaves; old opaque Registry dependencies remain immutable.

Binding lifecycle decision `docs/lifecycle-admission.md`: complete BUILT T0 and final public solver bytes before human review. Human signs exact T0+evidence P+policy/challenge. Accepted Q.task=T0. Every later Tn payload field except state/qualification remains byte-equivalent; transition costs/evidence stay in OperationResult/Registry. Current `TaskLifecycle(store,registry,qualification,revision).resolve_released(ref)->TaskBundle` validates actual M5 acceptance, exact selected completed BUILT→QUALIFIED→RELEASED chain and current quarantine/revocation. Calibration is a separate model-specific measurement, not a release prerequisite. Positive lifecycle tests substitute only M5's method in isolated diagnostics; actual missing-human denial also tested. No HumanReview/accepted Q created.

M6 `TaskBuilder(store,registry,revision)` builds `BuildInputs(source_pair,contract,scenario_plan,verifier,environment=<PreparedEnvironment>,baseline_files=<complete B allowlist>,invocation)`. `solver_package(T0)` validates selected frozen origin/components/inventory/quarantine and returns exact existing bytes, never repackages. Use exact construction revision. Full Factory now must aggregate actual M2/M4 journals, M3 repair records and Registry receipts: max2 per stage and4 per candidate. Mapping discovery→source, initial_authoring→authoring, scenario_planning→scenarios, checker/control/alternative→verifier; environment and qualification retain existing stages. Changed diagnosed inputs required for repairs. Authentic historical M2 records are imported as historical evidence, not pre-dispatch Registry claims. Candidate-specific M5 repair-history policies require concrete per-task service resolution; M6 is implementing it with M7, preserving exact policy/config and external trust checks.

M7 agreed `AgentRunner.run(task,policy,limits,*,case_seed=...)`: explicit frozen nonnegative seed passed through sole M4 terminal grade and RolloutRecord. Ordinary run may default to policy.seed; GRPO/evaluation always explicit. Infra retries same saved submission/cases at most2 after initial; candidate failures retained. RolloutRecord.run_id is actual Registry operation=run job ID. M8 uses existing `Registry.job(run_id)` and exact selected completed output, no second index. Pre-grading null outcomes require authentic runner stop/controller receipts, not arbitrary labels or invented M4 evidence. M6 wires actual CLI; M0 shared transport only after API exists.

M7 core17e43f4 has balanced sampler/signal gate64groups→8mixed/4tasks, actual admission gate, exact per-turn context/token/masks/behavior probabilities, real CPU SFT+GRPO/AdamW/KL/tensor changes/checkpoint/RNG/exact resume, policy barriers and pinned native SkyRL update bridge.38 CPU checks and round2 review close three P1s. Not yet full runner/native service or GPU execution. Native conversion/boundary CPU tests use explicitly labeled framework-resource doubles. Trusted SFT source, actual model/tokenizer gateway, full config/service/training checkpoint publication and reproducible commands still being integrated.

M8 must authenticate actual study frames/tasks/checkpoints/trials against runner and admission; offline numeric receipts are not authenticators. Source audits and patch audits have separate units/denominators. Reuse M5 external enrollment/SSHSig with canonical audit-specific payload, never crypto alone as human origin. New service configs explicitly declare consumed opaque refs; do not retrofit immutable declarations. External baseline metadata is pinned SWE-Bench++ release; no rows/weights/trials acquired. Preserve upstream `test` split in local training adaptation and exclude locked-test family/lineage overlaps. `docs/briefs/M8.md` and its corpus evidence retain exact source/license constraints.

## Failed real generation and execution limits

Real Click PR3228/issue3107 retained: one B-only discovery succeeded; three contract calls exhausted initial+two repairs (120.085s/2469output tokens,120.113s/2520,84.458s/1647). Last completed response rejected for duplicate `allowed_changes` at line142/column5, duplicate IDs and ungrounded evidence. **No valid contract/scenario, real checker, real constructed feature, H feasibility success or qualification.** No salvage/handwritten replacement/additional model calls. Repairs3/4 include one environment repair; contract stage2/2 exhausted.

Final journal `af7cee9c0af50eeddcf1a21f23b0e875964796648d20a34659e68a460806ace6`, store `.feature-rl/research/M2/authoring-production/store`; `docs/reports/M2-authoring.md` and existing `docs/evidence/M2/authoring-production/`. Only sanitized setup `docs/evidence/M2/coordinator-authoring-input.json` may be used for authoring. Earlier two accidental M2 metadata exposures are disclosed there; no H decoding/use according to owner, but do not claim fully blinded ownership. Greedy MLX selected-model logprobs are not behavior probabilities and that backend is nontraining. Root's prior literal-newline interpretation was corrected; actual failure is duplicate-key JSON.

## Environment and recovery

CWD `/Users/mukeshreddypochamreddy/Desktop/project x`, branch `implementation/feature-rl`; no ancestor AGENTS.md. CPython3.13.7, Mac arm64. Core command `PYTHONPATH=src .venv/bin/python -m pytest ...`; bare python unavailable. CPU Torch2.11.0 installed in `.venv-training-cpu` with19 locked distributions; actual imports/tensor/package consistency and38 M7 tests passed. No reinstall/research needed. Optional NumPy warning retained. `docs/evidence/integration/cpu-environment/` records exact wheels/hashes and acquisition under110MiB.

Actual Docker27.4 Linuxarm64/M3 Python3.12.14 image `feature-rl-m3-less@sha256:b28d3b1eeaa6251e82354fa61dc28a29afead9e2d7179436e77ca8ba612032fa`. Six actual pinned wheels under `.feature-rl/research/M3/dependencies`,1,753,266B. Worker pytest9.0.2 differs from host9.1.1; never substitute pins. Policy512MiB/noSwap,.5CPU/60CPU-seconds,64pids,128MiBdisk,120s+cleanup,2MiBoutput,1MiBstdin,8MiBsource/16MiBarchive. Nonroot/capsdrop/nnp/readonly/networknone/no host mounts/secrets. Existing less repair counted once, no prune/upload. Candidate builds remain fresh; reuse image cache. Runtime APIs `docs/interfaces-M3.md`.

Pinned native source: SkyRLskyrl-v0.3.0 `f5bc3b78dfddfb352870d5d7430cd226e5785838`, Harborv0.13.1 `3de07a0e01f3368921766437fc7afece3ddec23d`, local `.feature-rl/research/M7/{skyrl,harbor}`.59-file compatibility audit and exact LinuxPython3.12/Torch2.11cu128/vLLM.23cu129 overlay in M7 evidence. `docs/decisions/training-dependency-overlay.md` preserves exact upstream CUDA wheels and Pydantic2.13.5/core2.46.5 overlay. Explicit protected forwarded arm64 Docker Unix socket design `training-worker-endpoint.md`; no automatic SSH/provision/credentials. No Linux CUDA host, actual GPU/framework training or forwarded endpoint execution verified. These defer execution, not code completion.

All agents share repo/index. Use explicit `git add -- owned-paths` then `git commit --only -m 'message' -- owned-paths`; no broad staging/commit. Preserve `.DS_Store`, `.superpowers/`, untracked synthetic `docs/evidence/M4/review-core-round1/pytest-tmp/` and private stores/evidence. Earlier temporary725de10 was split preserving bytes into M2d267208/M69497b63; never anchor725de10. Approval never: omit sandbox_permissions. Commentary at least every60s, waits at most60s.

Skills were applied/read earlier: approved spec design, planning/worktrees/subagents/TDD/debugging/review/verification. User authorization overrides routine approval prompts; no goal-tool goal. Detailed predecessor progress remains at `80ca1a9:docs/progress.md`. After next substantial actual service checkpoint, review affected changes, run one broader stable suite, finish real commands/runbook and remaining CPU/Docker integration. Do not finalize while code remains. No real human-qualified/released task, GPU update or held-out result exists.
