# Feature-RL progress and recovery

Read this file, the active brief, actual upstream interface/code, latest report/review and Git log before dispatching work. The immutable user inputs are `feature_rl_pipeline.md` and `codex_multi_agent_implementation_prompt.md`; both were read in full. The complete earlier ledger, including every M0/M1 finding, repair, failed command and superseded artifact identity, is preserved byte-for-byte in [history/progress-through-b3ce070.md](history/progress-through-b3ce070.md) with a hash receipt. The later ledger throughb516c63 is also preserved byte-for-byte in [history/progress-through-b516c63.md](history/progress-through-b516c63.md), SHA83b84ef740a093cadf9c7e363c0b4293b6f0eb942d6b44aaf2a925ebd731b6ef, with its JSON hash receipt. Detailed evidence remains in `docs/evidence/`; nothing was deleted or relabeled.

## Current authorization and execution rules

- User explicitly requires real module owners, independent review, continuous execution and completion of independent local work. Latest clarification: **no remote compute configured; finish all independent local work**. Do not provision paid services or infer a compute budget.
- Empty initial workspace contained only the two input files; no ancestor AGENTS.md. Git branch `implementation/feature-rl`, package `src/feature_rl`. One product owner edits at a time; independent read-only investigation/review may run alongside. Coordinator `/root` owns interfaces, scheduling, integration evidence, briefs and this ledger. Same module owner handles fixes; no fake delegation.
- Four concurrency slots include root. `/root/m3_runtime` was stuck `pending_init`; interrupt alone did not change it. A recovery followup reached the same owner, who acknowledged availability in FINAL without changing files. Use that existing owner for runtime. Optional M5 spawn and M0 availability calls failed at the thread limit before this recovery; no work was falsely assigned. M5 preflight successfully spawned afterward. Agents must not spawn further agents; finish with FINAL when handing off.
- Shared directory and index: commit only explicitly owned paths, preferably `git commit --only ... -- <paths>`. Preserve user changes, all reports and `.superpowers/sdd` review packages.
- Project checks: `PYTHONPATH=src .venv/bin/python -m pytest -q`. Host is CPython3.13.7/macOS26.3/arm64; this Mac hides the editable `.pth` file, so explicit PYTHONPATH is required for some source commands. A normal wheel import was independently verified in M0. Core Python requirement remains>=3.11.
- Do not execute historical, submitted or generated repository code on host/controller. M3 Docker workers alone execute it. Do not expose H/private checks/authoring history/later target wheels to learner. Initial contract model gets request+B only; privileged H review follows draft freeze.

## Owners and gates

| Module | Accountable implementation owner | Current state | Next gate |
| --- | --- | --- | --- |
| M0 | `/root/m0_contracts` | Core, provenance v2 and optional MLX dependencies independently approved | Own shared contract/config fixes when requested |
| M1 | `/root/m1_sources` | Reviewed and integrated for documented reconstruction scope | Own downstream fixes; Click task still provisional |
| M2 | `/root/m2_authoring_recovery` (transferred from `/root/m2_authoring`) | Fix round3 product20af743 committed;66 focused/221 full owner tests; independent re-review active | Re-review, current-version native verification, later runtime-backed authoring/scenarios |
| M3 | `/root/m3_runtime` | Trusted Docker research and inert dependency preparation complete; no product | Reviewed M2 provider then actual lifecycle/B health |
| M4 | Unassigned | Brief ready, no product | Reviewed M2 authoring and M3 runtime |
| M5 | `/root/m5_qualification` | Human-signature mechanism preflight complete75ed88a; no product | Reviewed actual grading/controls; human review remains external |
| M6 | `/root/m6_factory` | SQLite/journal durability preflight complete8b68d99; no product | Reviewed M0-M5 then real first construction/grading integration |
| M7 | `/root/m7_learning` | Pinned SkyRL/Harbor source audit complete; no product | Runner/training implementation after upstream; CUDA execution blocked |
| M8 | `/root/m8_evaluation` | External-corpus metadata audit complete; no product | Evaluation/audit implementation after upstream; empirical arms/data gates remain |

Independent reviewers retained: `/root/review_m0` (M0 all slices PASS), `/root/review_m1` (M1 round2 PASS), `/root/review_m3_preparation` (preparation scope PASS, `docs/reviews/M3-preparation.md`, no P0/P1 findings). M3's brief requires its concrete production reuse gates; no new Docker execution or runtime/product approval followed from this review. Preflight `/root/execution_preflight` complete. New slices require actual independent review, not owner assertion.

## Active handoff: M2 generation provider — round3 independent re-review

**Latest handoff:** product `20af743778815635bb4af521164199bd8f09dcb8`, owner `/root/m2_authoring_recovery` FINAL complete for fix scope only;66 focused and221 full tests, both exit0, no native/remote calls. The final mechanically generated `provider-v3-fixround3-verification.json` binds the committed code/tests/docs and raw stdout logs. Root independently matched all12 recorded file/log identities against20af743. A draft receipt had a hand-copy error in models.py SHA and stale report/test identities; root caught it before handoff, owner regenerated final inventory and reran affected tests. Do not treat the superseded220-test run as the final221-test run. Independent reviewer `/root/review_m2_provider` is actively reviewing `docs/reviews/M2-provider-round3.md`; no verdict yet. Interim reviewer finding at20af743: selected model classes do not preserve scalar union branches. `flag: Literal[True] | int` accepts raw1 and returnsboolTrue; `value: Literal[3] | float` accepts raw3.0 and returnsint3 in the second ordinary validation. Full-provider evidence and consolidated report are pending; no fix dispatch until reviewer finishes the changed-scope checks, no native execution. Complete owned-scope package `.superpowers/sdd/implementation-plan/review-b516c63..20af743-M2-owned.diff`,66275B/SHA48117c671b014ee2477837f48d5d19bc65dfc89fcc0413b3181330f749681fab; full range retained adjacent177385B. No product implementer is editing while review runs. The coordinator fresh suite/native gate remains unexecuted.

**Fix round3 ownership transferred to `/root/m2_authoring_recovery` after two consecutive model-capacity failures, including a same-owner recovery attempt. Prior owner `/root/m2_authoring` no longer has write ownership.** The complete seven-file working diff and file hashes are preserved in `docs/evidence/M2/owner-transfer-round3.diff/json`; no completed edits are discarded. The assigned fix base remainsb516c63799539c00ba711c07254de885a4a4310d. Read `docs/briefs/M2-owner-transfer.md` before resuming. A replacement is permitted by the user execution instructions when its brief, decisions, code and evidence are transferred explicitly. Skill model-selection guidance selects `gpt-5.6-sol` with high reasoning for this multi-file integration/debugging task; this is an agent service change, not a project generation-backend change. Prior product3f859ca991c8965fda48e9d95588e9be12b0fdab (protocolv3), owner58focused/213full tests, zero native calls, had the two findings below; new20af743 awaits independent closure. Read `docs/briefs/M2-provider.md`, actual generation/**, `docs/interfaces-M2.md`, `docs/reports/M2-provider.md`, `provider-v3-fixround2-verification.json` and independent `docs/reviews/M2-provider-round2.md`. Round2 verdict was specFAIL/qualityCHANGES REQUIRED for twoP2 findings; no P0/P1 identified in that scoped review. Same reviewer now owns active round3 re-review.

1. **R1 generic structured-content validation:** event Literal guards now reject all23 wrong-type substitutions across15fields with seven valid baselines. Flat content is corrected, but the new JSON-schema walker validates a different union branch from Pydantic, and skips patternProperties. Full-provider diagnostics accept numeric1 as True for Enabled(flag:Literal[True])|Disabled(flag:Literal[False]) and patterned dictionary values. Fix actual raw-type/branch agreement, preserve valid alternatives and JSON tuple/enum/UTC transport, and never silently skip unsupported forms. Root union reproduction `coordinator-literal-union.py/json` independently matches the reviewer. Tests must begin with valid events; the bare AUTO-digest fixture is not itself a valid decoder baseline.
2. **R2 bounded rejection/recovery:** ordinary oversized instruction/schema rejection now records typed failed attempts/costs and replays publication without backend/runner calls. But a validated1048577-byte request_id produces attempt1050670B/preflight1049215B/status1049499B plus unbounded recovery/error fields. Bound local M2 request/response/prompt IDs or use a bounded attributable representation; preserve ordinary identity/digests and test maximum valid boundaries and replay. Revalidate public input if model_copy/model_construct can evade new local bounds. No shared M0/dependency change without a concrete request.

Owner may edit generation/**, corresponding tests and owned M2 interface/report/evidence only, no subagents. Coordinator-* evidence stays root-owned. Keep source-attribution inventory complete if new generation modules are added. Tests first, focused/full checks once fixed, exact code/log/status receipt, owned-path commit and FINAL. This is engineering implementation repair, not a candidate construction retry; no real candidate construction has started.

**Round3 coordinator follow-up:** the transferred partial implementation still used an exact-type prevalidator followed by the ordinary Pydantic validator. At provider SHA22829d5daec2eed4d8fec942ba1c03a6bda33002b56b086616b007e37292762e, a union `Enabled(flag:Literal[True]) | Number(flag:int)` lets the precheck accept numeric1 through Number, then the ordinary validator returns Enabled(flag=True). `coordinator-literal-mixed-union.py/json` retains the narrow reproduction (no provider/runner/native call); the active replacement owner was notified before final verification. This remains the same R1 branch-agreement defect within round3, not a completed fix or a new candidate attempt. After the first replacement-owner correction selected/restored the guarded branch, root also reproduced a valid-branch false rejection: Declined(flag:Literal[True]) has a rejecting after-validator; Allowed(flag:bool) is valid for raw true. Stripping callbacks selects Declined, then final validation prevents falling back to Allowed. The same owner acknowledged the cause and is adding explicit pre-execution refusal for the unsupported Literal/union/callback combination, keeping M0 UTC and enum-Literal transport, and removing model_construct side effects from intermediate restoration. These corrections are in20af743, not yet independently approved. The original M3 owner completed an availability recovery and can execute tools when the provider gate clears; no M3 preparation was repeated. Root also prepared and syntax-parsed `coordinator-provider-gate.py` to capture the fresh suite and one subsequent native check; it has not executed.

**Next coordinator gate:** after final owner handoff, create a tight review package from the assigned fix base to the product commit (prior larger packages retain unrelated already-reviewed evidence). Existing reviewer checks complete changed scope and remaining R1/R2. Only after PASS, reconcile `docs/evidence/M2/coordinator-v3-native.py` with final API, run fresh affected full suite and one declared current-source native check. Driver is syntax-parsed only and unexecuted. It permanently claims one call, publishes an actual synthetic M0 source, uses a new nonce, binds product/review/source hashes, observes the real native runner without changing it, and checks tuple/enum content, exact token IDs, costs, HOME/cache preservation and cleanup. Old smokes cannot stand in for it. Close only Task3/provider slice, then hand actual M3 product work to its existing owner. M2 remains partial until runtime-backed discovery/contract/scenarios.

**Live provider contract and resource limits:** write-only archive callback; initial authoring gets request/B/public checks only. Later checker generation may get frozen contract/private scenarios plus B/public context, never H in the initial draft. Caller/M2 authoring must resolve source existence, locators, quotes, provenance and frozen joins; the provider cannot read the private store. Protocolv3 exact worker identity/hash/dependency/offline/request fields, fresh isolated Python process/caches, HOME preserved, raw per-call archives and attributable UTC attempt/source metadata. Replay publication does not rerun inference. Greedy output stores selected_model_logprobs, exact input/emitted IDs, sampling_policy=greedy_argmax, behavior_logprobs=None: it is nontraining evidence.

**Native budget:** no further owner inference authorized in this provider slice. Prior owner two-call budget was used. Later coordinator check permits one worker call, <=2048 actual input tokens, <=128 emitted tokens,120s wall/CPU,1MiB stdin/output/file caps. Named memory policy:3.5GiB MLX allocator/wired guideline, cache0,4GiB sampled physical-footprint kill every20ms with1GiB guard. Failed observations reject. This does not prove a zero-transient5GiB ceiling. Larger envelopes remain explicitly unqualified; model architectural capacity262144 is not host measurement. No API, paid or CUDA compute, silent backend fallback, fabricated usage or missing-cost-as-zero.

**Selected local backend:** mlx-community/Qwen3-4B-Instruct-2507-4bit, revision50d427756c6b1b2fe0c0a10f67fbda1fc8e82c1b.11 allowlisted files2,278,969,697B under `.feature-rl/research/M2/model/`; weightsSHA2a73c6c248601ab904e035548abd8e6abb65ea27dcb5f342fb0a8910eb44173f, tokenizerSHAaeb13307a71acd8fe81861d94ad54ab689df773318809eed3cbe794b4492dae4. Model-manifestSHA697253a717e5857f1dfe3c14594f747c9c8118e6bc9c877bfc0c6faa6a7f50a0; dependency-manifestSHAd2db652d0634ff87b38ea93de0c54cb75560b209c783e6409937903a03f5a831. Exact34-package closure/pins are approved below. No custom Python/remote-code loading. Multi-GB weights are external pinned assets, not falsely claimed as M0 JSON-byte artifacts.

**Preserved history, never rerun or relabel:** old Codex0.154.0 structured subscription probe worked but rollout_budget did not enforce a provider token cap; explicitly disqualified as production fallback. Local research qualification ran one26-input/16-emitted truncated smoke plus pre-load rejection and64MiB watchdog canary. Two actual protocolv1 production smokes emitted83tokens includingEOS, succeeded, and stayed below sampled4GiB; separate input rejection occurred before load. Original v1 UTC/source binding is unavailable; their synthetic input used a raw text SHA/unpublished ref and incorrect historical label. HOME/raw-score/input-ID/archive omissions were later fixed in source, not rewritten in receipts. Main evidence `local-mlx-investigation.md`, `production-provider-smoke.md`, earlier reviews and archived ledgers retain exact failures/costs/memory distinctions. Product8a46375 v2 had16/171 owner tests and48 root static checks; fix7239379 v3 had46/201 owner tests and14 root source/log-binding checks. No fresh root full suite or current-v3 inference has yet occurred.

## Approved upstream evidence

**M0:** original core cb53f99 independent PASS (`docs/reviews/M0-round1.md`),103 root tests. Strict canonical SHA envelopes, version/visibility/role gates, atomic immutable storage, bounded types, explicit missing/unknown measurements. Provenance extension e16493b PASS (`M0-provenance-v2.md`),124 root focused tests: CandidateRecord/SourcePair v2 require provenance_label, SourceSnapshot requires explicit redirect_chain ornull. No silent v1 migration; other9 typed artifacts and raw bytes remainv1. Actual API: `docs/interfaces.md` and code.

**M0 Literal analysis at39aac02:** `docs/evidence/M0/literal-type-analysis.md` and full92-case diagnostic receipt. Pydantic2.13.5/core2.46.5 reproduces coercion for synthetic bool/int Literals, but all46 actual M0 Literal fields contain strings/string-enums. Current artifact versions, rewards and Boolean gates reject tested numeric/bool substitutes in JSON and Python modes. No M0 numeric-gate defect or schema change found. Matching Python strings are accepted by enum-valued Literals (a representation-documentation caveat, same declared labels, no observed visibility escalation); JSON strings remain the intended representation. No product changes, actual tasks or full-suite rerun. M2 should guard its own fixed bool/int fields before Literal conversion, preserving valid schemas and inputs.

Optional MLX configuration18bd8f34ad531ad7570ba1942e45bf9eaca518c5 independent PASS (`docs/reviews/M0-mlx-dependencies.md`). Opt-in authoring-mlx group, exact34-package wheel closure, unchanged core/dev12versions, CPython3.13/macOS26+/arm64 support and explicit M2 refusal elsewhere. MLX/Metal0.32.2, MLX-LM0.31.3, Transformers5.17.0, NumPy2.5.3. Source pinsMLX1f8e74e3f12f31365464a6867c6579f0e9b29d85, MLX-LMed1fca4cef15a824c5f1702c80f70b4cffc8e4dd. M0 executed offline binary-only hash-required installation, pip-check, native imports and155tests. Frozen dry-run no-change command includes `--inexact --no-install-project --no-build --offline`. Full details `docs/reports/M0-mlx-dependencies.md`. Root independently verified65 config/wheel/model/raw-receipt/native-import checks at18bd8f3, exit0/2.544s: `docs/evidence/M2/coordinator-local-mlx-verification.py/json`. No additional inference or repeat full suite.

**M1:** final producta658ae4d0ffba45d1fbde6c4edad71da33ea570e, reportc2bbd3d8b6c3303402bf63aae6bd4309ab81e2eb; independent spec/quality PASS `docs/reviews/M1-round2.md`. Root fresh155tests atc2bbd3d exit0/17.294s. Two cached actual Click ingestions at2026-09-19T09:35:26.709993Z, exit0/1.961s and1.989s, identicalstdout SHAdeca99ae7076bef8636a8b755832ad6a8df2ab9b91503f1bf9e0a6cadb0b2ea7; before/after Gitobject inventory unchanged39b3cd91625845754138380506e5af9357ecd227f9b878646ece96f7ceba6f05. `docs/evidence/M1/coordinator-round2-integration.md` supersedes the older report's pending-coordinator sentence; raw suite/ingestion/inventory receipts retained.

Current actual Click PR3228/issue3107:

- B19fd4d6e18bc9fce451f92f422696b11169faa57; H831c8f0948af519e45b90801d7430ff25451f972. Verified explicit B-rooted squash chain with source-head tree equal H. Historical-request timing unproved, so **reconstructed_specification** across all current artifacts,24 legacy redirect chains explicitly unknown. Three mixed-purpose paths still require later privileged/human review.
- CandidateRecordv2b0d7cfaa9d81aa1adbbdf6b76056c198b8f3250bd6e2fd40a79f1b604e6134d4; SourcePairv274d7e0ee98012c0fffab451e1664a4446ffdec1ce1fa38ec406e554d3456045b (private).
- Initial-authoring references ONLY: B archive4ff1d8a5d478ffb12950ec2661f3b38be5b7c9925691726c27b83234c5814575(kind=source-archive); requestd8d8a97861a597c7bfe181dfd3bbd6dbf72b7854ee13d555add1b1e07f0d2c61(kind=authoring-request); licenseb84ace5d4d01f55ab2db4e25ffddb9239104176e99a73e4799dcadbd8e612ab9(kind=source-response). All v1 raw bytes, authoring visibility. Use exact refs from `coordinator-round2-intake-1.json`, never infer kinds from labels. Root's guessed request-snapshot kind was correctly rejected on a read; exact authoring-request resolved the same stored digest. The archived feature asks for similar-name suggestions for misspelled CLI subcommands. H archivee4f10101549a77916c1898d3a4ed9ccc43dddbe1e6aa5c6c7cdcc896a232679c is private and excluded from initial draft.
- Store `.feature-rl/research/M1/production-store`; readonly Git `.feature-rl/research/M1/git/click.git`. Read actual `docs/interfaces-M1.md`. Old v1/v2 receipts remain preserved; never substitute their IDs for current validated artifacts.

M1 supported scope is explicit, not comprehensive: conservative B-rooted squash proof; bounded inert Myers rewrite proof with unique context and byte/path/mode equality. Reject uncertain/interleaved/unsupported histories. All original seven findings and later cleanup/wrong-location/unbounded-match findings have been independently cleared for this scope; no reopening without new concrete evidence.

## Prepared downstream work and interface decisions

- **M3 research:** `docs/evidence/M3/investigation.md`, production not implemented. Trusted Docker27.4 Linuxarm64 probes exercised explicit seccomp, nonroot/caps/nnp, PID/mount/network/read-only/tmpfs/resource/output/deadline denial and cleanup;11ownedcontainers removed, unrelated assets untouched. Pinned Python3.12.14 imagepython@sha256:eb5be8e5b4d0a159c237946bbdd06356dda5d19c30fc4f7843e8046d3a590333; appliedseccomp005f6ae1a0f3f9d1a0c044f83289e2ea54180c97a105b2539422588eac2fde44. Recheck available capacity and production application of every control. Local-service track remains unsupported until real service isolation/reset exists.
- **M3 inert dependencies:** docs/evidence/M3/dependency-preparation.md, root hash validation PASS. Six private wheels1,753,266B: baseline-lock pytest9.0.2,iniconfig2.3.0,packaging26.0,pluggy1.6.0,pygments2.20.0; flit_core3.11.0 explicitly neutral reconstruction pin within B range, not recovered historical bytes.13requests1,833,622B include retained failed-observer/repeat. No imports/install/build/B execution yet. `.feature-rl/research/M3/dependencies`. Host MLX closure includes unrelated Click8.5.0; never let it shadow historical B/H or enter a shared target layer. M3 must preserve saved source on ordinary limits before destroying workspaces; killing Docker client is not container cleanup.
- **Lifecycle:** `docs/lifecycle-admission.md` from M0 read-only analysis. Existing schemas suffice. Freeze full built T0 and solver view; human attestation binds T0 plus reviewed evidence-package/policy. Accepted Q refers T0. Later manifests differ from T0 only in state/qualification; new transition costs/evidence go to OperationResult/M6 ledger. Consumers resolve Tn→Q→T0, exact payload equality and quarantine. Actual enforcement/tests pending M5/M6; no admission claim from schema alone.
- **Packaging/qualification scheduling:** M5 code review can precede M6 while its first complete qualification remains pending the M6-owned real solver package/T0. M6 constructs and freezes T0, then the same M5 owner closes the actual qualification integration. No duplicate M5 packager, placeholder solver view or reused approval for changed bytes; M5 stays explicitly partial until then. Both briefs now state this dependency.
- **M5 independent preflight complete75ed88a677a84d38a5b0244846008f045fc46c65:** `docs/evidence/M5/attestation-preflight.md`, raw receiptsSHAced28e788d10d4a72e3f75c525fe4ea0b4a12d3d52f60f1f1d1c6f6a539a8d5e. Existing Apple-signed OpenSSH verified synthetic SSHSIG;16 negative probes rejected255, identical replay accepted0, check-novalidate accepted an unallowed key as documented.43commands/2.286s summed subprocess wall; no human/CPU/currency measurement or claim. Two diagnostic keys remain ignored0600; no user-key/agent/config access, real approval, enrollment, product change or inference. Final owner integrity323/323; earlier observer failure after root changed M5 brief at0785bb1 retained, captured original matches33359c8. Root independently checked174 embedded stream hashes/observed exits/replay/no-human claims at2026-09-19T11:03:06UTC, exit0. This establishes mechanism feasibility only. M5/M6 must authenticate external human enrollment and reviewed tuple, keep identical retries idempotent, require distinct execution receipts, enforce revocation and never use cryptography alone as human-origin proof.
- **M6 durability preflight complete8b68d99bd7bf07b381f50e74ee84f7fe994b89fd:** `docs/evidence/M6/durability-preflight.md`, raw receiptSHA68871473cbf6010e4c080639860f9ae6d558b48021f0ea9350cce27f0b76f7b4 (338213bytes). One trusted stdlib run at2026-09-19T11:21:29.680610–11:21:32.450658UTC,169 assertions across16 publication positions/3 corruption cases,2.770s driver/2.817s outer wall (overlapping, not additive). Exact partial journal prefix completed by suffix append; duplicate acknowledgement/result retries add no history; distinct attempts retain costs; missing observations stay unknown; corrupt or missing acknowledged bytes preserved and blocked. SQLite3.50.4/CPython3.13.7/macOS26.3/APFS, observed DELETE/EXTRA/fullfsync, no power-loss/I/O-error/concurrency/security or product claim. Owner static296checks; root independently bound committed/live/captured script, receipt identity/counts/scope and outer exit with9checks PASS at15:02:47UTC (`coordinator-preflight-integrity.py/json`), no probe rerun. Keep SQLite/outbox as authority, JSONL as verified projection; actual implementation/review and upstream M2 publication reconciliation still required, now explicit in M6 brief. All diagnostic state retained ignored; no real artifacts/approvals/rewards/inference.
- **M7 source audit:** docs/evidence/M7/compatibility.md,59sourcehashes independently verified. SkyRLskyrl-v0.3.0f5bc3b78dfddfb352870d5d7430cd226e5785838, Harbor3de07a0e01f3368921766437fc7afece3ddec23d/v0.13.1; proposed jointLinuxx86_64/Python3.12CUDA stack, no imports/training. Stock loss/group/limit/retry/sync/checkpoint/automatic-transfer behavior requires project replacement. Source-only worker transfers, true behavior logprobs, four same-case episodes and validity masks are mandatory. Deferred import/CUDA smoke script exists and passed bash syntax only. Actual update command awaits real code and qualified data. Max64signal groups,8mixed across4tasks before larger training; no manufactured rewards.
- **M8 metadata:** docs/evidence/M8/baseline-feasibility.md,15responsehashes/3pinnedpairs root verified. SWE-Bench++ preferred external-C candidate datasetda364537055b9bb5091783af78a02b6a3bc0e130, harnessf938edd189049806fef7a76fdf01f0da55baa565. Public500row upstreamtestsplit, not papertrainingtrajectories; custom noncommercialresearch/education terms, per-repository rights/adaptation later. No rows/solutions/tasks acquired. Authorized local train assignment must preserve upstream_split=test and exclude every related lineage from locked evaluation; no invented blanket human-permission gate or external leaderboard claim. Live metadata has no asserted revisionbinding.

## Exact remaining execution order

1. Finish M2 provider, independent review and affected coordinator checks. Same owner handles fixes.
2. M3 production lifecycle/recipe, actual B health/import identity/fresh/reset/timeout/network probes, independent review.
3. Same M2 owner: actual B-only discovery and model-generated contract/scenarios; freeze first draft before a separate privileged H feasibility review; no handmade answer substituted for generation. Independent review.
4. M4 actual source submission/rebuild/worker/controller/checker and B/H probes; M5 actual semantic/adversarial controls, blind alternative, repeats and concrete human review package. Missing human evidence stays provisional while independent code proceeds.
5. M6 actual first request-to-construction/grading path, registry/journal/leakage/release gates. M7 runner against released tasks, then training adapter and diagnostic checks. Pilot~20candidates only after first path usable, never to evade a blocked first task.
6. M8 independent evaluation/audit implementation and real corpus/data preparation permitted by qualified upstream gates; M6 wires only real reviewed entry points. Final independent integrated review and coordinator fresh verification.

## External or empirical gates still open

- **CUDA execution blocked:** AppleM4/16GiB unified memory; no Linux NVIDIA host or remote budget configured. Do not substitute local MLX inference/CPU tensor diagnostics for SkyRL/vLLM optimizer/update/reload evidence. Finish independent runnable integration code and precise future command.
- **Human task qualification not yet possible:** no concrete contract/control package exists yet. Once it does, request actual human review; agents cannot sign it. Blind alternative authoring is independent work, not automatically external.
- **Empirical first feature and all training/evaluation gates unexecuted:** actual historical B/H application code has not yet run in production M3; no generated contract/private grader/qualified task/solver episode/feature optimizer update/held-out comparison exists. Source, sandbox and provider research are their precisely scoped evidence, not task or project completion.
