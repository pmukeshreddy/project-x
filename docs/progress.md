# SDD ledger — plan: docs/implementation-plan.md

Recovery: read this file, docs/interfaces.md if present, latest reports and git log before dispatching work. The two root input documents are immutable authority.

## Current state

- Workspace inspected: two user Markdown files only; no prior Git repository or AGENTS.md in ancestor directories.
- Both supplied documents read in full. M0 implemented/reviewed; later implementation and empirical gates remain open as listed below.
- Git initialized on `implementation/feature-rl`; sequential module edits will use the fresh project directory.
- Owner map and execution order: docs/implementation-plan.md. Owner agent IDs recorded below on dispatch.
- Preflight `/root/execution_preflight` complete (no product ownership).

| Module | Accountable owner | Latest state | Next gate |
| --- | --- | --- | --- |
| M0 | `/root/m0_contracts` | Core/provenance v2 and optional MLX dependency slice18bd8f3 independently reviewed | Own downstream shared fixes; MLX runtime preflight remains M2 |
| M1 | `/root/m1_sources` | Complete for documented scope; round2 review PASS and fresh155-test/Click verification atc2bbd3d | Own future integration fixes; downstream candidate remains provisional |
| M2 | `/root/m2_authoring` | Local MLX qualification and M0 dependency handoff approved; Task3 provider implementation next | Implement/review production provider; later runtime-backed authoring |
| M3 | `/root/m3_runtime` | Trusted sandbox probe and inert dependency preparation complete; no product code | M2 provider review then lifecycle/baseline implementation |
| M4 | Unassigned | Scoped brief prepared | Reviewed M2/M3 |
| M5 | Unassigned | Scoped brief prepared | Reviewed M4 and real controls |
| M6 | Unassigned | Scoped brief prepared | Reviewed M0-M5 |
| M7 | `/root/m7_learning` | Pinned-source compatibility audit complete; no product yet | Real runner, training code/diagnostics; CUDA execution externally blocked |
| M8 | `/root/m8_evaluation` | External-baseline metadata/license investigation complete; no product or test data | Reviewed upstream, independent data/checkpoints for empirical runs |

## Decisions

- User explicitly approved architectural scope and continuous real-subagent execution. Use supplied spec rather than reopen design/plan approval.
- Work in the empty project directory on an implementation branch. No unrelated code exists to isolate.
- Preserve ledger/reviews/evidence indefinitely as requested; do not follow skill scratch-directory deletion guidance.
- Python package uses `src/feature_rl/` consistently. Product code belongs to module agents, coordinator owns interfaces/scheduling/integration.

## Interface preflight

| Producers → consumers | Contract / overlap | Resolution |
| --- | --- | --- |
| M0 → all | Models, content IDs, errors/config, storage | M0 establishes actual documented API before downstream dispatch |
| M1 → M2/M3/M6/M8 | Split sources and B/H archives | Read-only source snapshots; authoring has B only |
| M2 provider → M3; M3 → M2 scenarios | Provider precedes runtime; discovery needs runtime | Split M2 into two reviewed slices, same owner |
| M2/M3 → M4 | Scenarios, adapters, bounded workers | Runtime invocation stays in worker; expectations stay controller |
| M4 → M5/M6/M7/M8 | Trusted grade receipt | Same grade path across qualification, rollout and evaluation |
| M5 → M6 | Qualification evidence | Missing human/alternative evidence forbids release |
| M6 ↔ M7/M8 | CLI/library operations | M6 alone edits CLI; M0 alone edits dependencies/config |
| M0-M8 self-consistency | Own product/test paths and scoped reports | Nonoverlapping except documented coordinator-approved interface files |

## Checkpoints

- M0 optional MLX slice18bd8f34ad531ad7570ba1942e45bf9eaca518c5 independently approved: docs/reviews/M0-mlx-dependencies.md, spec/quality PASS, all34 wheel versions/hashes/dependencies checked, existing core/dev pins preserved. Opt-in `authoring-mlx` group plus exact platform wheel lock; tested CPython3.13.7/macOS26.3/arm64. Offline binary-only hash-required installation and155-test pass recorded by M0; reviewer intentionally did not repeat full suite. M2 must explicitly reject unsupported platforms and missing/tampered models. This is dependency loading, not production inference or CUDA qualification.
- M2 local qualification complete with one inference attempt:26 actual input tokens,16 emitted/length-truncated output tokens,4.524s/5010B/exit0/cleanup verified. A4105-token request was rejected before model load/inference. MLX peak2,392,554,830B; sampled physical footprint2,721,450,408B and kernel lifetime max2,726,381,992B (owner prose initially conflated the two; raw receipts are exact). External watchdog64MiB canary terminated at74,465,760B and cleaned its group. The selected production memory policy is explicit:3.5GiB MLX allocator/wired guideline, cache0,4GiB sampled physical-footprint kill threshold at20ms,1GiB guard band; it does not establish a strict zero-transient5GiB allocator ceiling. No second smoke was needed. `docs/evidence/M2/local-mlx-investigation.md` and raw receipts preserve source/license/runtime/dependency provenance and initial pip-cache uncertainty. Root independently verified65 hash/config/raw-receipt/native-import checks at18bd8f3, exit0 in2.544s, including all34wheels and11model files: `coordinator-local-mlx-verification.py/json`. No additional inference. Qualification does not substitute for Task3 production-provider smoke or feature authoring.
- M2 local backend acquisition handoff: exact model revision50d427756c6b1b2fe0c0a10f67fbda1fc8e82c1b,11-file runtime allowlist2,278,969,697bytes; weightsSHA2a73c6c248601ab904e035548abd8e6abb65ea27dcb5f342fb0a8910eb44173f. Owner reports hash/license/safetensors checks and34-wheel-only dependency installation/pip-check pass,96,889,626 download bytes. Exact locks/acquisition receipts under `.feature-rl/research/M2`; public evidence report pending. MLX0.32.2/source1f8e74e3f12f31365464a6867c6579f0e9b29d85; MLX-LM0.31.3/sourceed1fca4cef15a824c5f1702c80f70b4cffc8e4dd. M0 owner assigned optional production authoring dependency locking at3761b3f, preserving core/Linux portability and wheel-only installation. M2 independently qualifies hard token/memory/process bounds; no production provider or real contract yet. Updated docs/briefs/M2-provider.md reflects this explicit selected route.
- Task 2: complete (initial basebd1361f through producta658ae4/reportc2bbd3d, independent review clean for documented scope). `docs/reviews/M1-round2.md` passes spec/quality and clears all remaining findings. Root independently ran155 tests at `c2bbd3d8b6c3303402bf63aae6bd4309ab81e2eb`, exit0, pytest17.16s/wall17.294s; exact receipt/raw logs `docs/evidence/M1/coordinator-round2-suite.*`. Root then ran two cached Click ingestions, exit0 in1.961s/1.989s, actual recording time2026-09-19T09:35:26.709993Z. Output hashdeca99ae7076bef8636a8b755832ad6a8df2ab9b91503f1bf9e0a6cadb0b2ea7 identical; before/after object inventory still39b3cd91625845754138380506e5af9357ecd227f9b878646ece96f7ceba6f05. Current CandidateRecord v2b0d7cfaa9d81aa1adbbdf6b76056c198b8f3250bd6e2fd40a79f1b604e6134d4 and SourcePair v274d7e0ee98012c0fffab451e1664a4446ffdec1ce1fa38ec406e554d3456045b. Root checked B/H identities, equal reconstructed labels, explicit24 unavailable redirects, all author-view checks, internal retry, and provisional mixed-path state. Old receipts/artifacts remain preserved. Current integration note: docs/evidence/M1/coordinator-round2-integration.md.
- M2 experimental budget qualification found a real limitation: 32 sampled-token budget emitted a complete270-byte response before error/turn.failed, no usage; the1024 control reported189 output/21 reasoning tokens. Loopback sent zero tools and no provider-side token-limit field. `project_doc_max_bytes=0` suppressed a staged AGENTS canary. Full preserved evidence: docs/evidence/M2/rollout-budget-qualification.md. CLI remains unsuitable for hard-capped construction calls; no retrospective check is called enforcement.
- Ruling: qualify a local MLX + mlx-community/Qwen3-4B-Instruct-2507-4bit authoring backend before any contract/experiment freeze — the configured CLI failed hard-token enforcement; a local token loop may enforce it without remotecompute/API spending. Limits:3GB model/500MB dependency downloads,5GiB inference memory, initial2048 input/128 output actual tokens,120s/1MiB per inference, at most2 diagnosed smokes. Same M2 owner owns qualification/product; private wheel-only probe environment, no user/global config or unrelated cache changes, no remote-code loading or historical execution. Compatibility/limits remain unverified until measured. Cost if unsuitable: bounded download/storage and engineering time, all retained; do not switch again silently. M0 will own production dependency locking after exact proposal.
- Current user direction: no remote compute is configured; finish all independent local work. CUDA execution stays externally blocked; this is not permission to stop the provider/runtime/factory/training-adapter/evaluation implementation.
- M1 fix round1 review complete (`docs/reviews/M1-round1.md`): spec/quality CHANGES REQUIRED for wrong-location rewrite matching and unbounded text matching. Six other original findings and corrected descendant fixture addressed. Fix round2 assigned to same owner at base7f20923; bounded inert Git diff parsing with unique-context proof, new rejection/resource regressions, explicit squash scope/report. Root will independently execute the final suite and current-revision cached intake after scoped approval; no old receipt is reinterpreted as current validation.
- M2 owner assigned read-only provider budget-control preparation after M1 reviewer completed, no product edits/inference. Root confirmed local exec help exposes no direct output-token option; actual enforceable controls versus post-completion counters need evidence. Results will be `docs/evidence/M2/provider-budget-preparation.md`; Task3 implementation still waits M1 approval.
- M2 budget preparation complete: installed CLI0.154.0 has no stable documented per-request hard token cap; terminal usage checks are retrospective. Experimental rollout_budget is off/under development, weighted prefill/sampling, with installed/current-doc field drift. Root authorized same owner to qualify its exact installed configuration using credential-free loopback capture, then at most two tiny20s/1MiB subscription calls only if request/protocol evidence supports potential hard enforcement. This is provider engineering evidence, no candidate context or product code. Ruling: the previous read-only task bound is not a new user-permission gate; user already authorized local work/configured services. No API keys/new paid spend/config mutation; unknown enforcement remains explicitly unavailable. All attempts and costs retained.
- M1 full re-review identified an unresolved rebase false match: equal removed/added bytes at different semantic locations accepted because the fingerprint drops context/position. Owner awaits consolidated report before fix round2. Root also measured unbounded current text matching: a trusted20,004-byte repeated-line input exceeded3s and was externally killed (`docs/evidence/M1/coordinator-repeated-line-diagnostic.json`); replacement proof must be bounded. The reviewer's ordinary target-advanced squash example is rejected by the explicitly narrow B-rooted squash proof. Ruling: retain this as a disclosed unsupported topology in the pilot, with a concrete rejection case and no comprehensive squash claim — §5 allows rejecting uncertain histories; expanding support can follow separately and has no bearing on the recovered Click pair.
- M1 timeout-test readiness correction committed `86e25ef` then `18a735b`: bounded2s setup now requires complete positive PID and a live child before the unchanged0.2s execution timer, and cleans failed setup. Owner narrow test passed1 in0.66s; full reviewer must approve supplement and root full verification remains pending the product rebase fix. No test skipping or weakened cleanup assertion.
- M1 v2 evidence checkpoint `f6b3ef2c0db0f6d492b055a920d29acdf38ebf4f`: two timed cached intake runs through product `dec8118` returned identical v2 CandidateRecord `596660eda068ce707ddc3778f2c9aeed24a76453f37a7c74c87bc87183bfdc83` and SourcePair `fd244bdfb9d4a4efcf8db24f550618aa8c539c354dfa7fa78a0f8e2d14bb98a8`, reconstructed provenance, all24 legacy redirect chains explicitly unknown. Root compared both stdout hashes and equal before/after object inventories (20 files/505392 bytes). Full seven-finding re-review assigned to `/root/review_m1` on `52aaf16..f6b3ef2`. Fresh root suite at this HEAD:149 passed/1 failed in12.09s, timeout fixture tried reading a PID before helper startup was established. Exact original failure receipt `docs/evidence/M1/coordinator-v2-final-suite-failure.json`. Owner diagnoses a 0.2-second startup assumption and is preparing a bounded, deterministic test-only readiness fix without weakening the execution timeout or descendant-cleanup assertion. This run is not a passing integration check.
- Planning baseline committed `767e790`.
- Task 1 / M0: owner `/root/m0_contracts`, base `767e790`, implementation checkpoint `313a463`, review pending. Brief `docs/briefs/M0.md`; report `docs/reports/M0.md`; 65 tests and wheel import recorded in `docs/evidence/M0/verification.json`. Shared contracts not yet approved for downstream use.
- Preflight `/root/execution_preflight` complete: `docs/evidence/preflight.md`. Actual authenticated-home configuration was checked using a loopback capture; zero tool definitions after disabling request-user-input, no inference performed. Earlier empty-CODEX_HOME probe was explicitly corrected as nonrepresentative and must not be reused. Do not repurpose HOME/CODEX_HOME.
- M0 independent reviewer `/root/review_m0`: changes required, report `docs/reviews/M0.md`. Open: author-visible reference solution; invalid trajectory marked trainable; zero-measurement evaluation success. Owner fix round 1 pending.
- M0 fix round 1 checkpoint `583d963`: 99 tests reported. Coordinator found new stop-status regression before re-review: limit/malformed-action stops forced reward zero despite spec requiring grading the available artifact (which may pass). Returned to owner for supplemental focused fix; not approved.
- Task 1: complete (commits `767e790..cb53f99`, independent review clean). Fix round 1: all three original findings addressed plus root-discovered ordinary-stop regression corrected. `docs/reviews/M0-round1.md` passes spec/quality; coordinator independently ran 103 tests, exit0, at `cb53f99`. M0 owner remains `/root/m0_contracts` for shared interfaces/config changes.
- Task 2 / M1 implementation authorized against `cb53f99`; owner `/root/m1_sources`. Read-only proof recovered B `19fd4d6e18bc9fce451f92f422696b11169faa57`, H `831c8f0948af519e45b90801d7430ff25451f972`, squash proof with identical PR-head/H trees. Provenance remains reconstructed specification due unavailable historical body-edit proof. Actual implemented intake/reconstruction and review still pending.
- M1 actual dispatch base is `bd1361f` (docs-only advance after M0 `cb53f99`).
- M1 checkpoint `3976698` includes product commits `6e269cd` and `2c801f6`, report/interfaces and real Click evidence. Independent reviewer `/root/review_m1` is reviewing the full `bd1361f..3976698` diff; no product changes pending. Coordinator independently ran 119 tests (exit 0) and the cached real-intake command (exit 0), reproducing the candidate/source pair and idempotency. Exact receipts: `docs/evidence/M1/coordinator-verification.json`. Candidate remains provisional for three mixed-purpose paths; no qualification or runtime claim.
- M1 review established five open findings: unproved historical-request label/chronology, license text not bound to verified Git blob, promisor lazy-fetch mutating the read-only cache, repository-local external diff execution on host, and disconnected squash source ancestry accepted by equal-tree test. Owner received all five; full reviewer report pending, then fix round1. No further historical intake execution until fixed/reviewed. Current actual Click cache has no matching repository-local diff/filter/fsmonitor/include config keys under explicit global/system exclusion, recorded in `docs/evidence/M1/coordinator-config-audit.json`; this is current configuration evidence only, not a generic safety pass or historical execution proof.
- M1 final review `docs/reviews/M1.md` adds genuine rewritten-rebase intake and redirect/whole-deadline bounds (seven findings total). Spec/quality both CHANGES REQUIRED. M0 owner is first implementing a reviewed schema extension: CandidateRecord/SourcePair v2 with required provenance_label; SourceSnapshot requires explicit redirect_chain (known full chain including original/final URL, or null for unavailable evidence). No default labels, fabricated redirect history, or silent v1 migration. Then M1 owner updates producers and fixes all findings; original artifacts/evidence remain preserved.
- M0 extension `e16493b` approved by `/root/review_m0`, spec/quality PASS in `docs/reviews/M0-provenance-v2.md`. Root reproduced124 focused tests, exit0; exact receipt `docs/evidence/M0/provenance-v2-coordinator-verification.json`. The full suite's two M1 producer failures remain pending, not a passing integration claim. M1 owner now implementing all7 fixes against current base52aaf16; commit and short independent Git boundary check must precede corrected real cached Click execution. Rebase proof must preserve whitespace-sensitive deltas, not patch IDs alone; historical provenance needs preimplementation snapshot chronology, not just premerge timing; HTTP absolute deadlines include DNS/connect/streaming. Old artifacts/receipts remain unchanged.
- M1 fix source checkpoint `5587c6b`; owner reports146 tests pass and clean scoped diff. No corrected actual cache execution yet. `/root/review_m1` is performing the short P0/P1 inert-Git boundary check (`docs/reviews/M1-round1-git-boundary.md`) before the real rerun; full seven-finding re-review follows new evidence/report. Full diff `.superpowers/sdd/implementation-plan/review-52aaf16..5587c6b.diff`; isolated Git diff `M1-round1-git-boundary.diff` in same directory.
- Short boundary review returned CHANGES REQUIRED: external diff/textconv and lazy fetch fixed, but GitHistory._kill skips group cleanup after the direct child exits, leaving a descendant alive in the review reproduction. Report `docs/reviews/M1-round1-git-boundary.md`; actual cached intake still paused. M1 owner is correcting cleanup and adding success/timeout/output-limit descendant regressions, then a focused recheck precedes the real run and full re-review.
- Cleanup correction `dec8118` independently cleared by `/root/review_m1`: `docs/reviews/M1-round1-git-boundary-cleanup.md`, scoped spec/quality PASS. Owner reports149 full tests pass; reviewer separately reproduced output-limit cleanup with no live non-zombie descendant. Root authorized corrected bounded cached Click intake through that code, no new network/application execution: preserve v1 artifacts/receipts, record actual new artifact time and per-attempt UTC/wall/status, compare object database before/after, publish v2 labels/explicit unknown redirects, then complete report/interfaces and full7-finding re-review. This is boundary clearance only, not full M1 approval.
- M3 owner `/root/m3_runtime` assigned bounded trusted Docker/image/security probe research only; no product or historical execution yet. Report `docs/evidence/M3/investigation.md`; implementation awaits M1/M2 provider gates. Existing user Docker assets must remain untouched.
- M3 trusted investigation completed: explicit seccomp/nonroot/caps/namespaces/filesystem/network/PID/CPU/memory/tmpfs/timeout/output controls exercised; all 11 owned containers removed. Python arm64 pin `python@sha256:eb5be8e5b4d0a159c237946bbdd06356dda5d19c30fc4f7843e8046d3a590333` (3.12.14) and applied seccomp hash `005f6ae1a0f3f9d1a0c044f83289e2ea54180c97a105b2539422588eac2fde44`. Raw receipts/report in docs/evidence/M3. This is not M3 production implementation or baseline execution.
- M3 additional read-only dependency preparation authorized using the already archived B tar, no Git/current GitHistory while M1 repairs its boundary. Five exact B-lock test wheels acquired; no exact flit_core backend appears in the lock, so root authorized one explicitly disclosed reconstruction pin within B's declared range, never a claim to recover historical backend bytes. Owner reports six wheels totaling1,753,266 bytes with static metadata closure; final report/verification pending. No acquired package import, installation, build, Docker source execution or product changes.
- M3 dependency handoff complete: `docs/evidence/M3/dependency-preparation.md` and `dependency-metadata/`. Exact test pins pytest9.0.2, iniconfig2.3.0, packaging26.0, pluggy1.6.0, pygments2.20.0; proposed neutral flit_core3.11.0 (released2025-02-19), no recovered historical backend claim. Root independently verified B tar/three manifest hashes and all6 wheels/core-metadata hashes, totaling1,753,266 bytes; exit0 in `dependency-metadata/coordinator-verification.json`. Thirteen acquisition requests including one retained observer-failure/repeated wheel fetch totaled1,833,622 response bytes. No installs/builds/source execution; runtime compatibility remains pending actual M3 implementation. Private wheels `.feature-rl/research/M3/dependencies/`.
- M2 owner `/root/m2_authoring` assigned one bounded tiny real configured-subscription provider probe only; no product or Click contract yet. Report docs/evidence/M2/investigation.md. Implementation waits for M1 reviewed checkpoint.
- M2 probe actual result: gpt-5.6-luna, Codex0.154.0, tool-free configuration, first HTTP400 invalid JSON Schema then one typed-schema repair; second call exit0 in3.118s, input5821/output25 tokens, no forbidden events. Backend weight revision and billed USD not exposed and remain unknown. Docs/evidence/M2 contains full traces/config/status. This is provider feasibility, not feature construction or training.
- M7 future accountable owner `/root/m7_learning` assigned bounded read-only source audit of pinned SkyRL/Harbor interfaces and artifact transfers while M1 review proceeds. No installation, product implementation, model download or CUDA execution authorized in this investigation. Handoff report will be `docs/evidence/M7/compatibility.md`; actual adapter implementation waits for reviewed upstream modules.
- M7 source handoff complete: `docs/evidence/M7/compatibility.md`, 59 hashed source records, line excerpts, deferred exact import/CUDA allocation smoke (not executed). Root independently verified all59 hashes and shell syntax, exit0; `docs/evidence/M7/coordinator-verification.json`. Stock framework requires explicit validity/group/loss/seed/sync/checkpoint and source-transfer adapters; neither source inspection nor the proposed command is runtime/training evidence. Owner remains available for product slices after upstream review.
- M8 future accountable owner `/root/m8_evaluation` assigned bounded read-only external baseline release/license/schema metadata audit (`docs/evidence/M8/baseline-feasibility.md`). No task solutions, locked-test cases, dataset payloads, package installs or experiment execution. This is independent feasibility research while M0/M1 review fixes proceed; product implementation waits for reviewed upstream APIs.
- M8 metadata handoff complete. SWE-Bench++ official public release pinned at dataset da364537055b9bb5091783af78a02b6a3bc0e130 and harness f938edd189049806fef7a76fdf01f0da55baa565; declared500-row upstream test split, not released paper training trajectories. Report records custom non-commercial terms, source reconstruction/adaptation and local split/dedup requirements. Root corrected an inferred blanket permission gate: controlled local train assignment is permitted within actual license scope, with upstream_split retained and all related lineages excluded from locked evaluation; no external leaderboard comparison claimed. Pinned README/license/card bytes equal retained initial snapshots; live datasets-server metadata remains timestamped without claimed revision proof. Root verified all15 response hashes/byte counts and3 pinned/main pairs, exit0 in `docs/evidence/M8/coordinator-verification.json`. No task rows, solutions, test cases or empirical results were acquired/generated.
- M1 owner `/root/m1_sources` assigned bounded read-only Click source investigation only, report `docs/evidence/M1/investigation.md`. Product implementation awaits approved M0.

## External gates

- No authoring API-key presence detected in environment. The configured Codex subscription route passed structured output but failed hard-token enforcement. Explicitly selected local MLX has passed bounded qualification and dependency review; production adapter remains pending. No remote authoring API or new paid service is required for that local slice.
- Docker Desktop was stopped. Coordinator executed `open -a Docker` (exit 0); server is `27.4.0`. M3's explicit trusted isolation probe passed as scoped in `docs/evidence/M3/investigation.md`; production lifecycle and historical baseline remain unverified until implemented.
- Host Apple M4, 16 GiB unified RAM; Docker allocation about 8.2 GB. No CUDA runtime/GPU. User explicitly confirmed no remote compute configured and instructed finishing all independent local work. Do not provision paid compute; CUDA SkyRL/vLLM execution is blocked.
- Pilot human task review must be an actual human record; model code reviewers cannot sign it.
