# Feature-RL current progress

Read this file first after a context reset, then the active owner's brief, actual interface/code, and latest scoped review. Both supplied files were fully read: `feature_rl_pipeline.md` is the architectural specification; `codex_multi_agent_implementation_prompt.md` supplies execution instructions. Their original bytes remain unchanged. Do not stop while authorized code or integration remains.

## Current user instruction

Complete all code, commands, CPU/Docker verification and reproducible training/evaluation commands. GPU execution and experimental results are deferred and unverified, not code blockers. The immediate priority is **one explicitly labeled test fixture through actual M3 runtime, M4 grading, M5 qualification and M6 factory**: baseline fails the feature, correct source passes, broken source fails, reset restores the starting source, qualification gates run without fabricated human approval. Fixture success is integration evidence, not real task-generation success. **No new task-generation model calls in this phase.**

Keep existing module owners and their code. Finish confirmed defects, review changed code/interfaces, and reopen unchanged components only for new evidence. Focused checks during implementation; broader checks at stable integration milestones. Reuse unchanged Docker images/dependency artifacts without weakening fresh source builds or isolation. Consolidate evidence here and link retained logs; do not create duplicate audit/diff packages or speculative hardening/dependency projects. Continue M7/M8 implementation as actual dependencies and the four-agent capacity permit.

## Active work and owners

| Module | Owner | Current deliverable/status |
| --- | --- | --- |
| M0 | `/root/m0_contracts` | Contracts/CAS/provenance/config/bounded reads reviewed and integrated. CPU Torch setup prepared, install only when M7 checks need it. |
| M1 | `/root/m1_sources` | Intake/history/splits reviewed and integrated; actual Click B/H intake retained. |
| M2 | `/root/m2_authoring_recovery` | Provider/contract/scenario code reviewed PASS through6568a11; actual generation failure tracked separately below. |
| M3 | `/root/m3_runtime` | Runtime2190af0 reviewed PASS; real Docker health/build/reset evidence and stable342-test integration retained. |
| M4 | `/root/m4_grading` | Mechanical grading430df6f independently PASS. M2-backed checker proposal/finalization code still due. |
| M5 | `/root/m5_qualification` | ACTIVE implementing qualification, reference projection, controls/resets, attestation and accepted-report verification; coordinate fixture with M6. |
| M6 | `/root/m6_factory` | Registryfa6a1b8 PASS; BUILT/package review found one inventory-size mismatch, fixed71f0fa2 with19focused passes; closure queued. ACTIVE on real Docker fixture path and M5 join. Full factory/budgets/lifecycle/CLI still due. |
| M7 | `/root/m7_learning` | ACTIVE on actual GRPO/SFT update, token/mask, checkpoint/resume and pinned training integration code; isolated CPU Torch is ready. |
| M8 | `/root/m8_evaluation` | Existing owner retained; evaluation/audit/adaptation implementation next as slots permit. Corpus metadata audit exists. |

`/root/review_m6_registry` completed packaging core round1 with only the inventory-size P2. Same-owner71f0fa2 enforces matching producer/reader caps before freeze;19affected tests passed8.10s. Review only that fix at the next free slot; small fixture continues independently. M2 reviewer `/root/review_m2_authoring` FINAL round5 PASS; no further M2 audit needed. M4 reviewer `/root/review_m4_grading` FINAL core round2 PASS. Old `/root/m2_authoring` transferred ownership and FINALed; never return ownership to it. No child agents.

Root coordinates interfaces, review commits and final integration verification. Four active slots including root. Prefer two disjoint product owners plus reviewer; a named small confirmed fix may briefly use an idle review slot under `docs/decisions/independent-code-slices.md`. Latest user prioritizes independent implementation against agreed actual interfaces; do not invent absent APIs or hold completed core integration behind redundant reviews. Same owner fixes each module. **Use `git commit --only -- explicit owned paths`**, since a prior shared-index capture required splitting a temporary commit. Preserve `.DS_Store`, `.superpowers/`, ignored private stores and all old evidence.

## Immediate integrated fixture

M6 owns dedicated fixture/integration test/driver files, M5 owns qualifier code. They must agree a small disclosed test-only Click extension with ordinary closed observations and an existing-behavior preservation check. Use actual cached Click B, six pinned wheels, qualified M3 image and complete resolvable M0 artifacts. Fixture H is explicitly synthetic test source; do not inspect original private H semantics or claim model authorship. Run TaskBuilder, real M3/M4, actual reset and then M5 qualify; absent human/alternative/budget evidence remains explicit provisional, while eligible independent execution continues. One consolidated receipt/log is enough.

Approved M4 APIs: `SubmissionService.create/from_saved`, `GradingService.grade(task,submission,seed)->OperationResult`, `read_grade`. Reward1 is success, measured0 candidate_rejection, invalid/infrastructure null. Seed algorithm `m4-sha256-v1` covers every scenario every seed. Only adapter/current input reach workers; comparisons remain private. BUILT grading grants no admission.

M6 actual API (`docs/interfaces-M6.md`): `TaskBuilder.build(BuildInputs,owner,claim_key)`, recover/retry_publication, `solver_package(T0)->bytes`. The latter validates exact original root/frozen receipt/components/inventory/quarantine and returns frozen tar bytes; use the exact construction revision. M5 may read this actual interface while focused review proceeds and must fix any confirmed integration defect before claiming the milestone. Avoid a second packager and circular imports.

M5 agreed surface: `QualificationService.qualify(T0)`, `accept(review_request_ref,attestation_ref)`, `verify_accepted(task_ref,report_ref)`. It uses reviewed Registry identity/idempotency/quarantine, not another ledger. M5's exact current interface is being implemented. Its reference projection compares every B/H changed path, includes all allowed implementation changes onto B, records excluded docs/tests, and rejects unsupported mixed/build/dependency/unrelated/out-of-policy changes. T0 retains exact raw H. Interrupted reset uses actual M3 interrupt/reset/close, derives the confirmed reset source submission and regrades through M4, binding both receipts.

`docs/lifecycle-admission.md` is binding: complete BUILT T0 and final solver bytes before review; frozen evidence package P; human signs exact T0+P+policy/challenge. Accepted Q.task=T0. Later Tn differs from T0 only state/qualification; every other canonical payload field including provenance/costs remains identical. Transition evidence/cost goes in OperationResult/registry. Consumers resolve Tn→Q→T0 plus legal transitions/quarantine/revocation. No forged state or model signature. M6 alone owns aggregate candidate/stage repair history from actual M2 journals, M3 repairs and Registry; M5 incomplete history stays provisional, never assumed zero.

## Closed focused checks: reuse their evidence

- **M2:** product6568a11e0fd85a9f2f4864acc4025030ff55901f, handoffbc249a9cbaac7a7ddad1a81bce673c541dca4cb5, `docs/reviews/M2-authoring-round5.md` spec/quality PASS. Exact request/schema/context/status/content/usage/cost recovery, failed-call publication and journaling, frozen public-check joins, authentic pre-execution variants and encoded receipt bounds are fixed. Final residual note mismatch covered by real provider registration/replay with zero backend/runner/recovery calls:4passed0.17s; prior149passed1.05s. No broad/native repeat. See `docs/reports/M2-authoring.md` and `docs/interfaces-M2.md`.
- **M4:**430df6f4a2406fd9a669e11de2e963a2dcf73be8, core round2 spec/quality PASS.58focused passes0.53s close task-vs-runtime limit mismatch and invalid trusted B attribution. Earlier45focused, real B diagnostic12tests/14grades192.71s, eight hostile archive rejections and actual failed-build cost fix6.72s retained in `docs/evidence/M4/core/summary.json`. Total26diagnostic grades, no H/model. Do not rerun that closed matrix. Checker finalization remains separate work.
- **M6 registry:**fa6a1b884365a8f89d37beaf6bc180b945d088fa, registry round2 PASS;36focused passes6.51s. Evidence quarantine propagation and prospective materialized bounds fixed. Existing crash evidence is process-crash, not power-loss/external-exactly-once proof.
- **M6 builder:**f70c406d7a876405060a43d61ba3db0893803971;54factory+registry passes13.98s, including pre/post-freeze actual child-process crashes, exact publication replay, source/package/lineage/role checks. Source hashes stable during run. `docs/reports/M6-factory-build.md`, `docs/evidence/M6/factory-build/verification.json`. Review active; fixtures use inert test dependency pins and are not real Docker evidence.

## Real Click generation: failed, no inference retry

M1 actual request is Click PR3228/issue3107, reconstructed_specification. One actual B-only discovery succeeded. Three contract calls exhausted initial+two repairs:120.085s timeout (2469outputtokens),120.113s timeout (2520), then84.458s completed output (1647) rejected for duplicate allowed_changes at line142,column5, plus duplicate requirement IDs/ungrounded evidence. No valid frozen contract/scenario, no privileged H feasibility review, no checker/qualified task. Do not salvage or handwrite a production replacement. Candidate repairs3/4 (M3environment1 + contract2), but contract stage2/2 exhausted. M6 must retain genuine journals/costs without pretending registry claims preceded historical calls.

Final journal af7cee9c0af50eeddcf1a21f23b0e875964796648d20a34659e68a460806ace6; exact response/diagnostic in `docs/reports/M2-authoring.md` and `docs/evidence/M2/authoring-production/`, store `.feature-rl/research/M2/authoring-production/store`. All old provider smokes are also consumed. No new native/model/tokenizer runs needed for current work. Greedy MLX selected-model logprobs are not sampled behavior probabilities; training_eligible must remain false for that backend.

Authoring setup ONLY `docs/evidence/M2/coordinator-authoring-input.json`: exact B, wheels, socket, request and pre-exposure retrieval. M2 owner had two accidental metadata exposures (SourcePair/changed-file and private H execution metadata/base64), disclosed in report; no H bytes decoded/read/used according to owner, and prompts were request+B+sanitized discovery only. Owner not fully blinded. Do not repeat those private reads. Root's old literal-newline interpretation was corrected; actual failure is duplicate JSON key. Detailed failures and historical cost/source hashes remain in linked evidence, not repeated here.

## Environment and remaining code

Workspace `/Users/mukeshreddypochamreddy/Desktop/project x`, branch `implementation/feature-rl`; no ancestor AGENTS.md found. CPython3.13.7/M4 arm64/macOS26.3/16GiB. Use `PYTHONPATH=src .venv/bin/python -m pytest`; bare python is unavailable. Docker Desktop27.4, Linuxarm64. Existing M3 image `feature-rl-m3-less@sha256:b28d3b1eeaa6251e82354fa61dc28a29afead9e2d7179436e77ca8ba612032fa` includes the one pinned neutral less repair. Actual runtime recipe/API/policies and cached wheel paths in `docs/interfaces-M3.md` and sanitized setup. No paid/remote compute, destructive pruning or automatic external messages. Approval never; omit sandbox_permissions. Keep commentary current and waits at most60s.

M3 code2190af0 reviewed/integrated, source-only builds/hooks execute only in isolated workers.512MiB/noSwap,.5CPU+60CPU-seconds,64pids,128MiBdisk,120s+cleanup,networknone,nonroot/capsdrop/read-only/explicitseccomp. Each probe is fresh installed-wheel execution. Last confirmed source survives interruption; reset/close are cleanup-lock protected. Existing real health/import/reset logs and342-test stable integration are linked in `docs/reports/M3.md`; don't rerun unchanged historical suites. Actual application H imports were only import health, not feature validity.

Next M4 same owner completes generated-checker proposals/finalization against reviewed M2 without any model execution. M7 same owner implements real runner plus GRPO **and SFT**, exact tokens/context/masks/behavior probabilities, same-case groups, valid-only>=2, bounded signal probe, optimizer/sync/checkpoint/reload/resume. Pinned SkyRLskyrl-v0.3.0f5bc3b78dfddfb352870d5d7430cd226e5785838 and Harbor3de07a0e01f3368921766437fc7afece3ddec23d (0.13.1), local source audit `docs/evidence/M7/compatibility.md`. Missing real tasks/GPUs gates execution, not implementation. CPU math tests are diagnostic, not feature learning.

M8 same owner implements frozen families/lineages, complete paired assigned trials/A-B-C-D arms, task/family metrics and uncertainty/costs, weighted human validity audits/quarantine traces and external-corpus adaptation. `docs/briefs/M8.md` and metadata-only SWE-Bench++ evidence specify exact sources/license constraints; no rows/weights acquired or experimental rates measured. M6 then wires every real CLI operation and README/runbook. No always-blocked production adapter, fake command success or missing SFT branch qualifies as complete code.

CPU Torch is now installed in `.venv-training-cpu` for M7: prepared10wheel Torch2.11.0 closure plus current locked core/dev (19distributions), exact hashes, offline wheel-only install and package consistency/import/CPU tensor check passed. One setup METADATA-selection failure was fixed before any install; all downloaded wheels reused, no extra version resolution. `docs/evidence/integration/cpu-environment/receipt.json` records actual acquisition under110MiB and commands. NumPy absent; its optional Torch conversion warning is retained. No GPU or feature-learning evidence; existing MLX/core/dev `.venv` not modified. Linux training stack overlay and explicit forwarded arm64-worker endpoint decisions are in `docs/decisions/training-dependency-overlay.md` and `training-worker-endpoint.md`; actual GPU stack/forwarding remains unverified.

## Recovery and completion

This concise record replaces exact predecessor `f70c406:docs/progress.md`; all older detail remains in Git, reports/raw evidence and linked history archives. No evidence deleted. Applied skills were read earlier (subagent development/review/TDD/debugging/verification/worktrees/planning); supplied spec is approved design, user authorization overrides routine skill confirmations. No goal-tool goal or routine permission questions. The shared-index temporary725de10 was corrected preserving bytes as M2d267208/M69497b63; never anchor725de10. Keep exact mechanically derived commit IDs.

No actual generated/qualified/released feature, GPU update or held-out result exists. The current user-authorized fixture is a separate integration test. Do not finalize until remaining module code, independent scoped review, commands and CPU/Docker integration are complete; report unexecuted GPU/experimental gates honestly.
