# M2 proposal and final-artifact boundary

Coordinator ruling after the bounded preparation at a66534bcc5bc995aa28ca7102213eb436f359152. This resolves implementation interfaces before authoring; it does not approve a feature contract, model output or larger resource profile. M3 actual runtime remains under implementation, and first draft generation still follows its review.

Final M0 RequirementContract/ScenarioPlan artifacts include controller-owned metadata, provenance and measured costs unavailable before a model call completes. M2 should generate semantic proposals derived from the actual M0 field definitions and nested types, then construct and validate the complete M0 artifact after resolving evidence and adding real call/discovery receipts. Preserve FieldInfo/Annotated constraints when deriving fields; do not silently lose constraints by copying only a bare annotation, duplicate shared artifact definitions, or ask the model to fabricate post-call measurements. M0 final validators remain authoritative and must run on the assembled artifact. A structurally valid proposal can still fail grounding or joined semantic checks; preserve its actual generation costs and explicit task-construction disposition.

The controller fixes validated source/request identity, provenance classification, public checks, resource/seed policy and final artifact envelope fields where those are supplied by the pipeline. The model authors requirements, evidence-grounded observable behavior, compatibility obligations, ambiguity decisions and scenarios within those inputs. Controller derivation must not introduce a handwritten feature answer or make H the definition of unspecified behavior. Freeze and retain the first complete grounded draft before a separate privileged H feasibility review. Later changes create explicit versions and invalidate dependent evidence.

For Click, license bytes remain deterministic eligibility/provenance evidence outside the behavioral model prompt. No new license context role is necessary, and relabeling license text as a baseline or public check is not the production solution. The preliminary sizing template included license text; its measurement receipt must disclose the actual roles and distinguish that diagnostic template from a production request. Measure the real post-discovery proposal request again before assigning a larger execution profile.

M2's scenario service resolves the exact frozen contract and derives the complete mandatory feature-plus-compatibility ID set. It must require exact equality with ScenarioPlan.mandatory_requirement_ids, reject every unknown scenario requirement ID, enforce meaningful coverage and the fixed same-case seed policy, and then validate/store the real M0 ScenarioPlan. No M0 schema change is required merely because an in-memory model validator cannot dereference another artifact.

All earlier provider-slice native budgets are consumed. A separately declared, bounded nontraining larger-envelope measurement must precede authoring beyond the small observed profile. Existing wall/CPU/memory/input/output enforcement remains intact; a failed measurement is a recorded capacity failure, never permission to silently widen limits or substitute a backend. The coordinator can declare a new local measurement budget under existing user authorization once the exact proposal and discovery inputs are available. No routine permission request is needed for already authorized local work, and no paid or remote allocation is implied.
