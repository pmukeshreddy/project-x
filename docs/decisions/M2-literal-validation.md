# M2 validation boundary — coordinator analysis for the next fix

Observed product: `44e9dcf1079a186a8a0432fa51333d1d07f6422f`. The round4 reviewer is consolidating findings; this memo is an interface decision for the next owner handoff, not a passing review or implementation claim.

The defect is broader than a chain-node allowlist. Removing an after validator can change values consumed by a later chain node or change list cardinality through `PydanticOmit`. Running a complete altered validation graph before the original graph therefore cannot promise arbitrary callback semantics. Returning its result directly also changes model equality/hash behavior: inert objects remain distinct in a set, while restored frozen models deduplicate after the set's size constraint has run. Root and reviewer reproduced both problems; the reviewer retains full-provider evidence.

The architectural requirement is strict M0 and explicitly supported proposal schemas, not general compatibility with every custom Pydantic core program. Preserve valid supported alternatives and refuse unsupported schema structures before backend verification. Refusal must be typed and attributable; it must not consume a model call or masquerade as malformed generated content. No task requirement, checker or release gate is relaxed by an honest provider capability boundary.

The existing M0 analysis (`docs/evidence/M0/literal-type-analysis.md`) found that all46 actual shared Literal fields are strings or string-valued enums. Versions, rewards and Boolean gates use strict primitive constraints. Those actual models do not require a numeric-Literal workaround. The matching JSON string is the intended enum representation. No M0 source or dependency change is required.

Recommended narrow owner direction: distinguish the numeric/Boolean Literal forms that need additional raw-type enforcement from string/null literals that can use the original strict JSON validator unchanged. For schemas needing the extra guard, support a documented, callback-free structural subset rather than strip opaque callbacks and try to reconstruct their semantics afterward. Refuse hash-sensitive set/frozenset reconstruction and other unsupported core/lifecycle forms before execution. Preserve scalar/model/nested union alternatives and patterned string-keyed maps in that sound subset. M0's real requirement/scenario transport and ordinary JSON callback/default behavior can retain their original validator where no numeric Literal repair is needed.

The owner may choose another demonstrably sound, equally small implementation. A chain-only denial does not address the list-omission reproduction; a set-only denial does not address callbacks. Do not replace one incomplete graph approximation with another. Test the actual admitted/refused boundary, including accepted ordinary JSON baselines, exact returned types and zero backend/runner calls on refusal. Retain the already closed R1 scalar/model cases and R2 bounds, all previous evidence, and the one unexecuted coordinator native budget.

Cost if this conservative boundary excludes a future proposal schema: that caller needs a supported explicit proposal or a separately implemented and reviewed provider capability before use. There is no automatic schema conversion, backend fallback or weakened task admission. The first real feature has not yet been authored or frozen.
